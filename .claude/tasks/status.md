# Momo Project Status

Last Updated: 2026-09-12 08:03 UTC
Updated By: main orchestration agent

## Current Phase
**Phase 1 — Step 7 Engineering** (project.md §40). **EPIC-005 — Persistence & Sync Logic DONE (4/4) and MERGED to `main` (`cd22acd`, 515/54 green on the merged state; merged tree content-identical to the branch tip).** EPIC-004 merged (`04d07d6`, 369/43), EPIC-003 merged (`4291f02`), EPIC-002 merged (`eb82184`). **EPIC-006 — Character Rendering COMPLETE (6/6) and MERGED to `main` (`13a75bf`, 825/82 green on the merged state; merged tree content-identical to the branch tip; the owner's PR #7 `aa072ea` reconciled — merge-base `2e84cf7`, remainder 13 commits merged `--no-ff`, no force-push).** EPIC-005 merged (`cd22acd`, 515/54), EPIC-004 merged (`04d07d6`, 369/43), EPIC-003 merged (`4291f02`), EPIC-002 merged (`eb82184`). **EPIC-007 — iPhone Home Experience COMPLETE (9/9): TASK-031 (`257a58f`) · TASK-032 (`53e433f`) · TASK-033 (`f1668b7`) · TASK-034 (`2264f35` — touch & petting; REVIEW-TASK-034 CHANGES_REQUIRED → fix cycle → REVIEW-TASK-034-FIX APPROVED, 887/91 + app suite 13/13) · TASK-035 (`b8d181a` — feed/play/care; REVIEW-TASK-035 APPROVED_WITH_MINOR_NOTES → F-1 fix cycle → REVIEW-TASK-035-FIX APPROVED_WITH_MINOR_NOTES, 907/92 + app suite 17/17) · TASK-036 (`3861c7b` — quest moments + celebrations; REVIEW-TASK-036 APPROVED_WITH_MINOR_NOTES, 925/94 + app suite 21/21 + Watch build green) · TASK-037 (`bcecbed` — room tab; REVIEW-TASK-037 APPROVED_WITH_MINOR_NOTES, 931/95 + app suite 22/22 + Watch build green) · TASK-038 (`4b02be7` — settings + rename + erase; REVIEW-TASK-038 APPROVED_WITH_MINOR_NOTES, 945/96 + app suite 27/27 + Watch build green) · TASK-039 (`614053d` — consolidation + FR-20 AC-2 accessibility audit; REVIEW-TASK-039 APPROVED_WITH_MINOR_NOTES, 949/97 + app suite 35/35 + Watch build green) DONE — **MERGED to `main` (`b73e3eb`, 949/97 green on the merged state; merged tree content-identical to the branch tip; the owner's PR #16 `ad0199e` reconciled — remainder 2 commits merged `--no-ff`, no force-push).** Now **EPIC-008 — Watch App & Sync IN PROGRESS (4/5): TASK-040 DONE (`0719129` — REVIEW-TASK-040 CHANGES_REQUIRED [F-1 HIGH marker-directory divergence] → fresh fix agent → REVIEW-TASK-040-delta DELTA VERIFIED → final APPROVED_WITH_MINOR_NOTES) · TASK-041 DONE (`4407544` — W1 glance + snapshot persistence + AOD; REVIEW-TASK-041 APPROVED_WITH_MINOR_NOTES, 0 Critical/0 Major/2 Minor/2 Notes, F-1/F-2 routed to TASK-042; 1056/104 green) · TASK-042 DONE (`faa8761` — the Watch pat: journaled intents + `transferUserInfo` drain + micro-reaction + haptics; REVIEW-TASK-042 **APPROVED_WITH_MINOR_NOTES** 0 Critical/0 Major/1 Minor/4 Notes, F-R1 sweep → TASK-044, F-R2 comments → TASK-043 ride-along; 1102/108 green, MomoKit 90.79 % lines, MomoWatchUITests 5/5, `Apps/Momo/**` diff-EMPTY) · TASK-043 DONE (`0bd6044` — the Watch quest line renders the LIVE shared cascade over injected clocks at four event legs via the `WatchCascade` seam; provenance tests + 3 scan guards + 2 fixture UI flows; REVIEW-TASK-043 APPROVED_WITH_MINOR_NOTES — 0 Critical/1 Major [F-1 W1 all-done copy → owner]/4 Minor [F-2/F-3 guard ride-alongs → TASK-044]/2 Notes; 1128/110 green, MomoKit 90.81 % lines, MomoWatchUITests 7/7, frozen surfaces incl. `Apps/Momo/**` diff-EMPTY) · TASK-044 DONE (`491777f` — §10.4 matrix audit + launch-sweep drain + erase reset E2E + clock ban; REVIEW-TASK-044 APPROVED_WITH_MINOR_NOTES; 1161/113 green, MomoKit 90.84 % lines, MomoWatchUITests 9/9, frozen surfaces incl. `Apps/Momo/**` diff-EMPTY)) — **EPIC-008 COMPLETE 5/5 and MERGED to `main` (`d460a24`, 1161/113 green on the merged state; merged tree content-identical to the branch tip; the owner's PR #17 `fffa6cf` reconciled — remainder 8 commits merged `--no-ff`, no force-push).** Now **EPIC-009 — Polish, QA & Release Readiness IN PROGRESS (1/6) — TASK-045 DONE (`c43c92b`, pushed `2996fa1..c43c92b`; REVIEW-TASK-045 APPROVED_WITH_MINOR_NOTES, 8 findings dispositioned)** on `feature/EPIC-009-release-readiness` (cut from the merged `main` @ `244b7ea`; epic file `.claude/tasks/epics/EPIC-009-release-readiness.md`, TASK-045…050; TASK-046 — §10.5 edge-case matrix execution — is next; the epic's standing §25 discipline held: physical-hardware legs attempt-then-BLOCKED → owner device-pass backlog, sim-measurable legs measured for real). The epic's EPIC-008 contract commit entered `main` via the owner's PR #9 merge (`74eba4f`) — owner PR-merges pushed branches from the GitHub UI banner; agents never create PRs.

## Current Epic
**EPIC-009 — Polish, QA & Release Readiness — IN PROGRESS (1/6) — TASK-045 DONE (`c43c92b`, pushed `2996fa1..c43c92b`; REVIEW-TASK-045 APPROVED_WITH_MINOR_NOTES)** on `feature/EPIC-009-release-readiness` (cut from the merged `main` @ `244b7ea`; epic file `.claude/tasks/epics/EPIC-009-release-readiness.md`, authored 2026-09-12 from the delivery plan's EPIC-009 rows + project.md §32–35/§44 + 05 §10.5/§11/§12 + 03 §10 + ADR-006 N-1). Task map: **~~TASK-045 performance budget verification (05 §12)~~ DONE (`c43c92b`; device legs BLOCKED §25 → owner backlog)** → TASK-046 §10.5 edge-case matrix (M) → TASK-047 full a11y audit — launch-blocking (M) → TASK-048 privacy & security review (M) → TASK-049 §44 final product test (S; needs 045–048) → TASK-050 release gates + E4 name clearance + App Store posture (M; needs 049) → §14 merge. Standing discipline for the whole epic: physical-hardware legs (energy gauge, sustained-fps device traces, thermal) attempt-then-BLOCKED per §25 (raw `xctrace list devices` capture; owner device-pass backlog) — sim-measurable legs (launch metrics, memory footprint, bundle size, restore timing, cadence logs) measured FOR REAL; budget values normative and uneditable — a miss is a task-level blocker (06 §3), never a note; owner-gated legs (E4, TestFlight submission, store metadata) are packaged as artifacts + escalation, owner submits. **EPIC-008 — Watch App & Sync — COMPLETE (5/5) and MERGED to `main` @ `d460a24` (2026-09-12, §14 direct merge per owner rule; fetch-first re-check: the owner's PR #17 `fffa6cf` had merged the epic branch through the TASK-041 closeout `2e65019`, so the merged remainder was 8 commits — TASK-042 contract `692b5d2` / feat `faa8761` / closeout `898fe09`, TASK-043 feat `0bd6044` / closeout `91c523f`, TASK-044 contract `6bc8c40` / feat `491777f` / closeout `149f1a8`; merged `--no-ff`, merged tree content-identical to the branch tip [diff EMPTY], `swift test` **1161/113** green on the merged state BEFORE push, pushed `fffa6cf..d460a24`; branch retained at `149f1a8` for audit).** Epic history: on `feature/EPIC-008-watch-sync` (cut from the merged `main` @ `b73e3eb`; epic file `.claude/tasks/epics/EPIC-008-watch-sync.md`, authored 2026-09-11 with inheritance verified at source: O1+O5 REVIEW-TASK-023, OBS-1 REVIEW-TASK-024, O4 REVIEW-TASK-025, F-2 REVIEW-TASK-039 owner-gated on TASK-041's template WORDING only). **TASK-040 DONE** — `0719129` (record: `.claude/tasks/completed/TASK-040-iphone-wc-session.md`; REVIEW-TASK-040 **CHANGES_REQUIRED** — F-1 HIGH: the §6.6 reset marker's launch-load read the STORE TREE while the erase-save writes the App Support ROOT, so the marker could never survive a relaunch (the exact §6.6 hazard — stale pre-erase journal applying warm onto a fresh pet; Probe C proved the suite structurally blind: 988/988 green with the load pointed at a nonexistent directory) → fresh fix agent (init-load re-derived through the SAME `StoreRules.watchResetMarkerDirectory()` rule via static `loadResetMarkerEraseCount()` in `MomoAppModel+Watch.swift` + guard 4 `markerDirectoryViolations` + 7 new tests, both-direction fixtures, EXACTLY-ONE-red bite) → REVIEW-TASK-040-delta **DELTA VERIFIED** (own probes D1/D2, 18 hashes reconciled, 14 non-fix files byte-identical) → final status **APPROVED_WITH_MINOR_NOTES** (carried notes: F-3 Watch-side journal self-defense → TASK-041/044; F-4/F-5 inert; F-6 D-7 posture accepted; F-7 D-12 doc comments → orchestrator backlog; F-8 `MomoAppModel.swift` exactly 800/800); 995/100 green, MomoKit 92.47 % lines, both builds green on pinned sims). **TASK-041 DONE** — `4407544` (record: `.claude/tasks/completed/TASK-041-watch-w1-glance.md`; ADR-014 landed — the additive-OPTIONAL `WatchCharacterDTO?` rides the snapshot, no schema bump, OBS-3 not fired; REVIEW-TASK-041 **APPROVED_WITH_MINOR_NOTES** — 0 Critical / 0 Major / 2 Minor / 2 Notes: F-1 the ADR-014 DEBUG-loud nil-character log unimplemented + F-2 "slot skipped vs held" wording divergence [both verified at source by the orchestrator, both routed to TASK-042's receive-path work], F-3 layout-share resolution ACCEPTABLE + UX §6.1 "~40 %" doc-erratum → backlog, F-4 cosmetic Pat-capsule nit; 1056/104 green, MomoKit 91.48 % lines, both builds zero touched-file warnings, MomoWatchUITests 3/3, 4 probes sha256-restored byte-identical, **O4 CLOSED** on the true-size still). Remaining: TASK-042 (Watch pat — the contract folds F-1/F-2 + the TASK-040 F-3 journal self-defense note; **CONTRACT READY** — **ADR-015 ACCEPTED:** D1 reaction binding over the EXISTING `.tap`/`.stir` clips through `MomoRigView`'s existing `reactionMotion` sampler seam — zero MomoCharacter edits; the docs' "tiny heart" has NO art in the frozen vocabulary (MomoProps is GENERATED, food/blanket/2 sparkles only) — delta disclosed to backlog as an owner item; D2 completion haptic by LOCAL ESTIMATION from the held snapshot (`questLine == .wish(.q7)` ∧ Q7 progress + this Watch's pending journal pats + 1 == target → the celebratory double REPLACES the soft tick — estimate decides haptic choice only, false-positive across silent iPhone-side completion disclosed, false negatives impossible from staleness); D3 haptic types VERIFY-AT-BUILD, feel → TASK-044) + the task file (R1 epoch store + journal-wipe-on-generation; R2 journal legs through the persister actor + `IntentJournal.wipe()`; R3 seq formula `next = max(maxJournalSeq, epochMatchedWatermark) + 1`; R4 transport send surface; R5/R6 reaction+haptic fire FIRST, journal append best-effort; R7/R8 folds; R9 UI wiring; R10 guards+bites; **`Apps/Momo/**` must diff EMPTY** — TASK-040 shipped the ENTIRE receive path, this task is Watch-side + MomoKit only) → TASK-043 (cascade + settings/haptics propagation) → TASK-044 (§10.4 matrix + erase-reset E2E + paired-device obligations + live AOD observation) → §14 merge. **TASK-042 DONE — `faa8761`** (feat(watch) atomic commit 2026-09-11, 19 files +2190/−66, pushed `692b5d2..faa8761`; record: `.claude/tasks/completed/TASK-042-watch-pat.md`; REVIEW-TASK-042 **APPROVED_WITH_MINOR_NOTES** — 0 Critical / 0 Major / 1 Minor / 4 Notes: F-R1 journaled-but-never-sent crash window [contract-conformant under the three-leg R2/R3 scope] → routed to TASK-044 as an explicit launch-sweep requirement; F-R2 stale launch-read comments → TASK-043 ride-along; F-R3 citation nit + F-R4 waking→bounce judgment-call pin + F-R5 disclosed estimate micro-windows — recorded, no action. All adversarial targets held; gates re-run green by the reviewer AND the orchestrator; 6 mutations total [3 implementer bites + 3 reviewer probes] each exactly-red then sha256-restored, 18-file sweep byte-identical; 1102/108, MomoKit 90.79 % lines, MomoWatchUITests 5/5, both builds zero touched-file warnings, `Apps/Momo/**` diff-EMPTY). **TASK-043 DONE — `0bd6044`** (feat(watch) atomic commit 2026-09-12, 9 files +1380/−42, pushed `898fe09..0bd6044`; record: `.claude/tasks/completed/TASK-043-watch-cascade.md`; the Watch quest line now renders the LIVE shared derivation — `MomoWatchAppModel.liveQuestLine` recomputed by ONE private `recomputeQuestLine()` at exactly FOUR event legs (init / receive-steady before persist / receive-consume after the clear / scene activation), hour ONLY from the injected `wallClock`/`calendar` (D20), NO timers; `WatchCascade.liveQuestLine(questSet:localHour:)` package seam (`WatchGlanceScan` bans `QuestGeneration.cascade(` in Apps/MomoWatch — scan-mandated); `GlanceView` reads `model.liveQuestLine ?? snapshot.display.questLine` threaded to BOTH the visual quest slot AND the VoiceOver composite; fixtures `w1`/`alldone`/`stale` via the shared `seedFixture`; F-R2 comment ride-along landed; **`Apps/Momo/**` + `Sources/MomoCore/` + `Sources/MomoCharacter/` diff-EMPTY**; REVIEW-TASK-043 **APPROVED_WITH_MINOR_NOTES** — 0 Critical / 1 Major / 4 Minor / 2 Notes: F-1 (Major, NO code change on task) D2 ruled **(B)-refined** — W1's all-done line is normatively "All done — see you soon" (PRD §5.5 item 6; UX §6.1 ×2 + §9 Watch column) while the shipped shared key renders the iPhone M3 card's "Momo had a lovely day." — a pre-existing TASK-041-era conflation; owner ruling on a `momo.line.quest.all-done` key + docs errata required before EPIC-008 closeout (→ owner items); F-2 (probe-demonstrated) the frozen-read census misses the bare-`display.questLine` spelling (the UI stale flow supplies the real teeth) + F-3 (probe-demonstrated) the D20 injected-clock property of the recompute leg is scan-unenforced → **TASK-044 ride-alongs**; F-4/F-5 guard tolerances + F-6 frozen-fallback note recorded; F-7 the CONTRACT's own Context mis-citation [orchestrator-owned, acknowledged]; 6 mutations total [2 implementer bites + 3 reviewer probes, none reused] each exactly-red/demonstrative then sha256-restored; 1128/110 green, MomoKit 90.81 % lines, MomoWatchUITests 7/7, both builds zero touched-file warnings). **TASK-044 DONE — `491777f`** (feat(watch) atomic commit 2026-09-12 — the amended commit after the git-mv index-blob catch — 17 files +1705/−163, pushed `6bc8c40..491777f`; record: `.claude/tasks/completed/TASK-044-sync-matrix-e2e.md`; R0 the §10.4 audit table — all 7 rows mapped to name-precise body-verified tests, exactly 2 real gaps [the launch-sweep drain + the erase-reset Watch-UI E2E], both closed, F-1's Conflict-row suite misattribution corrected at closeout — both named tests live in `WatchSyncGateTests.swift:161`/`:211`; R1 the journal launch-sweep drain — arm at activation, flush at the two receive decision legs AFTER the awaited `consumeWipe`, verbatim re-enqueue through the existing `sendUserInfo` leg, count-recheck as the second defense [a consumption between arm and execution suppresses the sweep], reset-suppression [post-wipe journal → zero send], one-shot, timer-free, O1-clean; the reviewer ruled arm-then-flush the COMPLIANT reading — the contract's own "pending marker ⇒ zero send" rule forbids literal drain-at-activation; R2 the erase reset-marker Watch-UI E2E through the REAL `receiveContext` with `WatchResetConsumption.decide` UNMOCKED — `testResetMarkerFixtureConsumesToTheSettlingInLine` [consume → wipe → settling-in + the phase-2 relaunch-over-same-store disk discriminator] + `testResetHoldFixtureKeepsTheGlanceRendered` [the no-marker control]; the DEBUG `-momo-watch-fixture reset`/`resethold` seam is release-inert by construction [`#else` bare `return nil`]; R3 the F-2 bare-`display.questLine` census widening + R4 the F-3 D20 ambient-clock banned-token scan [`Date()`/`Date.now`/`Calendar.current` banned over Apps/MomoWatch production sources]; R5 the three device obligations attempt-then-**BLOCKED** per §25 — paired-hardware WC-frame-under-suspension, live AOD, haptic FEEL → owner device-pass backlog; R6 the pat send census enumerates BOTH legitimate legs — a forged third site fails alone, no pin weakened; R7 three real-tree mutation bites exactly-one-red, sha256-restored; REVIEW-TASK-044 **APPROVED_WITH_MINOR_NOTES** — 0 Critical / 2 Minor [F-1 audit-table suite misattribution — FIXED at closeout; F-3 `await`-spelling pin tightening → guard-hardening backlog] / 1 accepted Note [F-2 the consumeWipe suspension window — narrow, warm-apply class, honestly disclosed] / 4 verification Notes; reviewer ran 4 OWN probes [none reusing the implementer's bites], all exactly-one-red sha256-restored; gates re-run matching 1161/113 + 90.84 % + builds + 9/9). EPIC-007 — iPhone Home Experience — COMPLETE (9/9) on `feature/EPIC-007-iphone-home` (cut from the merged `main` @ `13a75bf`; the owner PR-merged the epic+contract commit to `main` the same day — PR #9, `74eba4f`). Epic file: `.claude/tasks/epics/EPIC-007-iphone-home.md` (grounded on the delivery-plan EPIC-007 rows TASK-031…039, 05 §4.1–4.2/§5.2, UX §3/§5, FR-1/2/3/5–8/16/19/20). **TASK-031 DONE** — `257a58f` (record: `.claude/tasks/completed/TASK-031-app-model-facade.md`; REVIEW-TASK-031 + disposition under `.claude/tasks/reviews/`). **TASK-032 DONE** — `53e433f` (record: `.claude/tasks/completed/TASK-032-onboarding.md`; REVIEW-TASK-032 APPROVED_WITH_MINOR_NOTES + disposition under `.claude/tasks/reviews/`; suite 857/86). **TASK-033 DONE** — `f1668b7` (record: `.claude/tasks/completed/TASK-033-home-composition.md`; REVIEW-TASK-033 APPROVED_WITH_MINOR_NOTES + the orchestrator dispositions under `.claude/tasks/reviews/`; 868/88 green, app suite 10/10). **TASK-034 DONE** — `2264f35` (record: `.claude/tasks/completed/TASK-034-touch-petting.md`; REVIEW-TASK-034 CHANGES_REQUIRED + REVIEW-TASK-034-FIX APPROVED under `.claude/tasks/reviews/`; 887/91 green, app suite 13/13). **TASK-035 DONE** — `b8d181a` (record: `.claude/tasks/completed/TASK-035-feed-play-care.md`; REVIEW-TASK-035 APPROVED_WITH_MINOR_NOTES + REVIEW-TASK-035-FIX APPROVED_WITH_MINOR_NOTES under `.claude/tasks/reviews/`; 907/92 green, app suite 17/17, Watch build green). **TASK-036 DONE** — `3861c7b` (record: `.claude/tasks/completed/TASK-036-quest-moments.md`; REVIEW-TASK-036 APPROVED_WITH_MINOR_NOTES under `.claude/tasks/reviews/`; 925/94 green, app suite 21/21, Watch build green; F-1 dispositioned in-file, F-2 routed to TASK-039). **TASK-037 DONE** — `bcecbed` (record: `.claude/tasks/completed/TASK-037-room-tab.md`; REVIEW-TASK-037 APPROVED_WITH_MINOR_NOTES under `.claude/tasks/reviews/`; 931/95 green, app suite 22/22, Watch build green; N-1/N-2/N-3 dispositioned — N-1's optional guard-attachment hardening routed to TASK-039). **TASK-038 DONE** — `4b02be7` (record: `.claude/tasks/completed/TASK-038-settings-rename-erase.md`; REVIEW-TASK-038 APPROVED_WITH_MINOR_NOTES under `.claude/tasks/reviews/`; 945/96 green, app suite 27/27, Watch build green; N-1–N-5 dispositioned — N-1's attachment-blindness probe routed to TASK-039's guard-hardening cluster). **TASK-039 DONE** — `614053d` (record: `.claude/tasks/completed/TASK-039-consolidation-a11y-audit.md`; REVIEW-TASK-039 APPROVED_WITH_MINOR_NOTES under `.claude/tasks/reviews/`; 949/97 green, app suite 35/35 incl. the 8-test audit suite, Watch build green; N-1–N-6 dispositioned, F-2 the state-in-words template delta routed to the OWNER). **MERGED to `main` @ `b73e3eb` (2026-09-11, §14 direct merge per owner rule)** — fetch confirmed `origin/main` @ `ad0199e` (owner PR #16, through the TASK-039 contract `d49fe9d`); remainder = 2 commits (`614053d` feat + `8604f5b` closeout); local main ff-only to `ad0199e`, `--no-ff` merge → `b73e3eb` (22 files vs `ad0199e`, +1614/−514); merged tree content-identical to the branch tip (`git diff feature/EPIC-007-iphone-home main` EMPTY); `swift test` **949/97 green** on the merged state; pushed `ad0199e..b73e3eb` SUCCESS. The EPIC-008 task map (TASK-040…044) lives in its epic file; TASK-040's record leads this section. Adjudications SETTLED in-contract (`.claude/tasks/active/TASK-036-quest-moments.md`): `momo.line.moment` lands as a FIXED lookup per OBS-D — no epoch bump, epoch-5 reserve retired as moot; delivery via a new `MomoCharacterEvent.moments([CharacterMoment], at: Double)` case (the second disclosed MomoCharacter touch, R6 precedent), director-side FIFO; greetings stay on the displayState door (TASK-019 pin — the arm filters them from the event); UX-10 once-semantics engine-owned (`highestCelebratedStage`), presentation stateless; haptics at the `.deliverMoments` arm gated on the existing `hapticsEnabled` via an injected sink. Dispatch order: 038 → 039 (consolidation + a11y audit + REVIEW-TASK-033 MINOR-1's verbatim-pin completion + REVIEW-TASK-036 F-2's R9e 5th leg + negative-probe re-bite + REVIEW-TASK-037 N-1's guard-attachment-order hardening + the TASK-034 rendered-motion UI guard routing + the TASK-035 F-3 `@MainActor` housekeeping + the manual VoiceOver check) → §14 merge. **TASK-025 DONE** — `7681498` (record: `.claude/tasks/completed/TASK-025-asset-pipeline-path-constants.md`). **TASK-026 DONE** — `d0955e0` (record: `.claude/tasks/completed/TASK-026-rig-layer-tree-character-clock.md`). **TASK-027 DONE** — `d26b9d3` (record: `.claude/tasks/completed/TASK-027-idle-sequencer-expressions.md`; REVIEW-TASK-027 + REVIEW-TASK-027-FIX1 under `.claude/tasks/reviews/`; ADR-009/010/011/012 under `.claude/tasks/decisions/`). **TASK-028 DONE** — `cc98b06` (record: `.claude/tasks/completed/TASK-028-reactions-choreography.md`; REVIEW-TASK-028 + REVIEW-TASK-028-FIX1 under `.claude/tasks/reviews/`). **TASK-029 DONE** — `9a46641` (record: `.claude/tasks/completed/TASK-029-reduce-motion-theming.md`; REVIEW-TASK-029 under `.claude/tasks/reviews/`). **EPIC-006 record:** COMPLETE 6/6, MERGED to `main` @ `13a75bf` (2026-09-10, §14 direct merge per owner rule; the owner's PR #7 `aa072ea` reconciled — remainder 13 commits `--no-ff`, merged tree content-identical, 825/82 green before push; epic file: `.claude/tasks/epics/EPIC-006-character-rendering.md`; TASK-030 record: `.claude/tasks/completed/TASK-030-character-test-suites.md`, REVIEW-TASK-030 APPROVED_WITH_MINOR_NOTES). Completed record: `.claude/tasks/epics/EPIC-005-persistence-sync-logic.md` (4/4 DONE, merged `cd22acd`); TASK-024 record: `.claude/tasks/completed/TASK-024-kit-test-suites-coverage-floor.md`.

## Overall Progress
Phase 0 complete (TASK-001…007, all pushed). **EPIC-002 complete — all four tasks DONE and merged to `main`** (`eb82184`): TASK-008 (`172bc11`, ADR-008 pins) · TASK-009 (`b28ccb4`, package + app targets + shells) · TASK-010 (`a9b9993`, test harness + scanners) · TASK-011 (`a2a36b7`, design tokens + String Catalog scaffolding; REVIEW-TASK-011 APPROVED_WITH_MINOR_NOTES 0/1/2, disposition applied). **Merge rule (owner, 2026-09-08): reviewed code merges directly to `main` — no PR — then continue immediately to the next epic (recorded in CLAUDE.md §14, commit `f5c1c11`).** **EPIC-003 complete** (`4291f02`): TASK-012 (`bc95cb9`) · TASK-013 (`5bbdd72`). **EPIC-004 complete (7/7)** — engine core → time-fold/wakefulness → interactions/satiety → bond ledger → quests + Watch cascade → read-models + copy selection → §10.3 matrix + meta-determinism + coverage floor. **EPIC-005 complete (4/4, MERGED `cd22acd`)**: TASK-021 (`5cca031`), TASK-022 (`3acc54f`), TASK-023 (`9109457`), TASK-024 (`4372913`). **45 Phase-1 tasks DONE (TASK-001…045; TASK-045 was EPIC-009's first task — committed `c43c92b` and pushed); 7 of 7 engineering epics complete and merged (plus all product-definition epics): EPIC-002 (`eb82184`), EPIC-003 (`4291f02`), EPIC-004 (`04d07d6`), EPIC-005 (`cd22acd`), EPIC-006 (`13a75bf`), EPIC-007 (`b73e3eb`), EPIC-008 (`d460a24`). EPIC-007 (iPhone Home, 9 tasks) COMPLETE 9/9 AND MERGED; EPIC-008 (Watch & Sync, 5 tasks) COMPLETE 5/5 AND MERGED — TASK-040 (`0719129`) + TASK-041 (`4407544`) + TASK-042 (`faa8761`) + TASK-043 (`0bd6044`) + TASK-044 (`491777f`) DONE. **EPIC-009 — Release Readiness — IN PROGRESS (1/6): TASK-045 DONE (`c43c92b`, review APPROVED_WITH_MINOR_NOTES, 8 findings dispositioned); 45 of 50 Phase-1 tasks DONE; TASK-046 (§10.5 edge-case matrix execution) is next.**

- **TASK-045 — Performance budget verification (05 §12) (`c43c92b`, pushed; EPIC-009 1/6; record in `.claude/tasks/completed/TASK-045-performance-budgets.md` + REVIEW-TASK-045 + the 25-file evidence set under `.claude/tasks/evidence/TASK-045/`).** All ten 05 §12 budget rows executed on the pinned device matrix (iPhone SE 3 + 17 Pro sims; Watch SE 3 40mm/44mm + Series 11): **R1 cold launch PASS** (SE 3 sim median 1.100153 s ≈ 55 % of the 2.0 s budget; 17 Pro first-boot descending distribution recorded in full, RSD 12.9 %; device leg BLOCKED §25) · **R2 animation BLOCKED** (the Animation Hitches template verbatim-refuses the sim — "Hitches is not supported on this platform", reproduced by the reviewer; no device; structural: zero blur/shadow/opacity in `MomoRig.swift`, the 21-part transform-only rig) · **R3 iPhone memory PASS** (settle 47 → plateau 56–57 MB ×5 on both sims vs 150) · **R4 Watch memory PASS** (19 MB flat 40mm / 20 MB Series 11 vs 80; comparator floor ~19 MB → Momo's own cost ≈ 1 MB) · **R5 sizes PASS** (Release Momo.app 7,609,627 B / MomoWatch.app 5,979,174 B vs 60 MB; generated art 70,743 B vs 1.5 MB; the +4,696 B MomoCharacter delta = the disclosed TASK-035 playStopped seam) · **R6 energy BLOCKED on the device leg, structurally attested otherwise** (zero timers, AOD glyph binds no clock, next-event schedulers only, zero UIBackgroundModes) · **R7 Watch restore PASS** (MomoWatchUITests 9/9; `restoreBudget = 2.0`, asserts restore − baseline ≤ 2.0 s) · **R8 play round PASS** (22.4 s authored worst case vs the ≤ 30 s bound; `MomoReactionDirector.swift:757-762` pacer) · **R9 sync cadence PASS** (live paired-sim log census: exactly 2 launch + 16 interaction push attempts, ZERO in the 60 s idle window; the on-change gate `AppModelPlan.swift:252`; the WC 7006 environmental failures kept verbatim; intent-send half structural-only — disclosed in the verdict cell) · **R10–R12 discipline DONE** (all ten Budget cells §12-verbatim after the F-1 fix; full distributions; the 35/35 app UI gate honestly CITED, not re-run). Method: host `/usr/bin/footprint <pid>` on the sim app's host process; throwaway /tmp probe projects, zero repo footprint; disk-pressure event disclosed (ModuleCache deletion + owner notified). **Cycle:** fresh Jupiter implementer → §19 gates (orchestrator re-run: swift test 1161/113 exit 0, both builds, Watch UI 9/9) → REVIEW-TASK-045 **APPROVED_WITH_MINOR_NOTES** — the reviewer re-derived the budget list from 05 §12 BEFORE opening the contract, personally re-ran every gate (1161/113 digit-for-digit; Watch UI 9/9 in 62.922 s xcresult-verified), and re-measured independently: own launch probe median 1.111244 s (~1 % from recorded), memory 47 MB flat reproduced digit-for-digit under automation, clean `simctl launch` 25 MB explained AND reproduced as ~+22 MB persistent automation overhead (testmanagerd/a11y injection — now recorded evidence), the Hitches refusal reproduced verbatim, art buckets + census + Debug bundle byte-exact; 8 findings: **F-1 (Minor) the "verbatim" budget strings were paraphrases → FIXED at closeout** (all ten Budget cells now §12-literal, TASK-044 F-1 precedent); F-3/F-4 folded into the table cells (57 MB tails; intent-send structural-only qualifier); F-2/F-5/F-6/F-7/F-8 accepted Notes (phys_footprint label; Momo.app "watchsimulator" label typos — values are iphonesimulator, raw evidence left as-written; +0.62 % Release rebuild delta; gate-time porcelain snapshot; benign xctrace sim-pair-line drift — refreshed capture rides the next device-identity check). No budget value edited anywhere; no fabricated claim found ("every number I attacked reproduced"); frozen surfaces diff-empty by tree identity.
- **TASK-044 — Sync test coverage: §10.4 matrix + erase reset E2E + launch-sweep drain + device obligations (`491777f`, pushed; EPIC-008 5/5, the closer; record in `.claude/tasks/completed/TASK-044-sync-matrix-e2e.md` + REVIEW-TASK-044).** **R0 audit table** — all seven §10.4 rows mapped to name-precise, body-verified EXISTING tests; exactly two REAL gaps (the launch-sweep drain; the erase reset Watch-UI E2E), both closed by R1/R2; the Conflict row's suite attribution corrected at closeout per REVIEW-TASK-044 F-1 (`WatchSyncGateTests.swift:161`/`:211`, NOT `SyncStateTests`). **R1 launch sweep** — the crash-stranded journaled-pat window (REVIEW-TASK-042 F-R1) closed: `WatchSweepPlan` (pure MomoKit) armed at the transport's activation sink, flushed at exactly the two receive decision legs AFTER the awaited `consumeWipe`, verbatim journal re-enqueue through the EXISTING `sendUserInfo` leg, the count-recheck as the second defense (a consumption between arm and execution suppresses the sweep), reset-suppression pinned (post-wipe journal → zero send), one-shot, timer-free, O1-clean; `pendingEvents(directory:)` added to the one-writer persister extension. **R2 erase E2E** — `testResetMarkerFixtureConsumesToTheSettlingInLine` drives the REAL `receiveContext` (decide UNMOCKED): marker frame → consume → wipe → settling-in line, phase-2 relaunch-over-same-store as the disk discriminator; `testResetHoldFixtureKeepsTheGlanceRendered` is the no-marker control; the DEBUG `-momo-watch-fixture reset`/`resethold` seam is release-inert (`#else` bare `return nil`; `resetMarkerFrame()` unreferenced in release). **R3/R4 ride-alongs** — the bare-`display.questLine` spelling joins the quest census (alias dodge closed); the D20 banned-token scan (`Date()`, `Date.now`, `Calendar.current`) over Apps/MomoWatch production sources + the recompute's injected-read pin (`WatchClockScan`). **R5 device obligations** — probes run with raw capture (`xctrace list devices`: exactly ONE physical device, iPhone-class, no paired Watch); three honest **BLOCKED** records (WC-frame-under-suspension on paired hardware; live AOD; haptic FEEL) → owner device-pass backlog, nothing sim-claimed (§25). **R6** — the pat send census enumerates BOTH legitimate legs (pat drain + launch sweep); a forged third site fails alone; no pre-existing pin weakened in any of the four reviewed directions. **R7** — three real-tree mutation bites (init-order, ambient `Date()`, alias second read), each exactly-one-red, sha256 pre/mutated/restored. **Tests:** +33 package (1128/110 → **1161/113**): `WatchSweepPlanTests` NEW 5, `MomoWatchSweepScanTests` NEW 16 + `Support/WatchSweepScan.swift`, `MomoWatchClockScanTests` NEW 10 + `Support/WatchClockScan.swift`, +1 R3 alias direction, +1 R6 census delta; `MomoWatchUITests` 7/7 → **9/9**. **Cycle:** impl gates green → orchestrator §19 verification (all gates personally re-run: 1161/113, coverage 90.84 % measured immediately, both builds, UI 9/9 xcresult-verified, frozen surfaces diff-EMPTY, 761/800 budget, zero timers, O1 census) → REVIEW-TASK-044 **APPROVED_WITH_MINOR_NOTES** (reviewer re-derived the §10.4 mapping AND the R1 hazard analysis from the normative docs BEFORE reading any implementation; 4 OWN probes [count-recheck neutralization, flush-moved-ahead-of-consume, forged third send site, forged direct `IntentJournal(`], all exactly-one-red sha256-restored; every gate re-run with matching numbers) → findings: F-1 (Minor, record-only) audit-table suite misattribution → **fixed at closeout**; F-3 (Minor, non-blocking) the `await`-spelling pin tightening → guard-hardening backlog; F-2 (Note, accepted) the consumeWipe suspension window — narrow, warm-apply class, honestly disclosed in code + task file; F-4 release-inertness, F-5 R6 non-weakened four ways, F-6 R0 audit complete, F-7 evidence authenticated (incl. the `WCSession.h:144` citation resolving true). §19 gate **1161/113 + both builds + UI 9/9** on the exact tree; MomoKit coverage **90.84 % lines** (160/1746 missed; regions 91.50 %; floor 80 % holds).
- **TASK-043 — Watch cascade + settings/haptics propagation (`0bd6044`, pushed; EPIC-008 4/5 — W1's quest line is now LIVE: the wrist shows the quest as it is NOW, not as it was at the last sync; record in `.claude/tasks/completed/TASK-043-watch-cascade.md` + REVIEW-TASK-043).** **R1 live derivation** — `MomoWatchAppModel.liveQuestLine: QuestGeneration.QuestLine?` (nil exactly when `snapshot` nil), recomputed by ONE private `recomputeQuestLine()` at exactly FOUR event legs (init after the launch read / receive-steady immediately after `self.snapshot = snapshot` BEFORE persist / receive-consume immediately after the clear / `scenePhaseChanged` gated `phase == .active` before the background persist); the local hour comes ONLY from the injected `wallClock`/`calendar` (D20 — `calendar.component(.hour, from: wallClock.now())`); NO timers (§4.2 — hour-boundary latency at the next event, governed by UX-9). **R2 seam** — `WatchCascade.liveQuestLine(questSet:localHour:)` in MomoKit, zero-logic forwarding to `QuestGeneration.cascade` — scan-mandated (`WatchGlanceScan.bannedEngineTokens` bans `QuestGeneration.cascade(` in Apps/MomoWatch); package-side, pbxproj untouched. **R3 view** — `GlanceView` reads `model.liveQuestLine ?? snapshot.display.questLine` (one live read + one frozen fallback, census-pinned at exactly one read; the fallback is unreachable at every current leg — belt-and-braces per F-6) threaded to BOTH the visual quest slot AND the VoiceOver composite label. **R4 haptics propagation** — verified functionally LIVE end-to-end (TASK-040 push + TASK-042 honor + the haptic-seam scan); mapped to existing anchors instead of duplicate tests (R5's no-duplicate clause). **R5 UX-13 guards** — 3 new structural guards (`WatchQuestScan` + `MomoWatchQuestScanTests`, 21 tests): quest-slot census (1 Button/1 tap/1 live read/1 `.display.questLine`, display-only slot Text), recompute-legs census (1 def, calls−defs == 4, receiveContext carries the call, `.active` gate BEFORE recompute), no-settings view census (banned tokens + exactly 1 `: View`). **R6 fixtures** — `alldone` + `stale` join `w1` via the shared `seedFixture`; `seededApp(store:fixture:)` refactor; the stale UI flow derives the wish from the runner clock (≥20 ∨ <7 → Tuck-in, <12 → Morning hello, else Mealtime; ±1 h tolerance for the runner-read/render gap). **Tests:** +26 package (1102/108 → **1128/110**): `WatchCascadeTests` NEW 5 (the provenance property re-derives BOTH push-hour lines through the REAL chain `makeDisplayState` → day-record → `makeWatchSnapshot`; `provenanceBitesOnDriftedInputs` negative control; the seam passthrough bindings), `MomoWatchQuestScanTests` NEW 21; `MomoWatchUITests` 5/5 → **7/7**. **Cycle:** impl gates green → orchestrator §19 verification (all gates personally re-run: suite 1128/110, coverage 90.81 % measured immediately, both builds, UI 7/7 xcresult-verified, frozen surfaces diff-EMPTY, 600/800 budget, zero timers) → REVIEW-TASK-043 **APPROVED_WITH_MINOR_NOTES** (reviewer re-derived the spec from normative docs BEFORE reading implementation; 3 OWN probes not reusing the implementer's bites — A: builder threads `questInputs: []` → provenance + field-threading pins red; B: composite reverts to the frozen line → kit scan stayed green [F-2] but the UI stale flow RED; C: ambient clock swap → all green [F-3]; all sha256-restored, full re-run 1128/110 clean) → findings: F-1 (Major, no code change on task) D2 ruled **(B)-refined** — W1's all-done string is normatively "All done — see you soon" (PRD §5.5 item 6; UX §6.1 ×2 + §9 Watch column) while the shipped `moment.02` renders the iPhone M3 card's "Momo had a lovely day." — a pre-existing TASK-041-era conflation; owner ruling (`momo.line.quest.all-done` namespace candidate) + docs errata → **owner items, resolve before EPIC-008 closeout**; F-2 bare-spelling census gap + F-3 D20 banned-token gap (both probe-demonstrated) → **TASK-044 ride-alongs**; F-4/F-5 guard tolerances + F-6 frozen-fallback note recorded; F-7 the contract's own Context mis-citation (orchestrator-owned — the implementer correctly disclosed rather than shipping against the corpus). §19 gate **1128/110 + both builds + UI 7/7** on the exact tree; MomoKit coverage **90.81 % lines** (160/1741 missed; regions 91.41 %; the seam file 100 %; floor 80 % holds, ~flat from 90.79 %).
- **TASK-042 — The Watch pat (`faa8761`, pushed; EPIC-008 3/5 — the first vertical slice is now INTERACTIVE: pat fully offline on the wrist, affection lands exactly once on the iPhone; record in `.claude/tasks/completed/TASK-042-watch-pat.md` + REVIEW-TASK-042).** **R1 epoch + generation wipe** — `WatchSessionEpochStore` (persisted preferred at init; miss mints IN MEMORY + detached save + journal wipe — the journal can never outlive its epoch); **R2 journal legs** — every journal touch (read AND write, incl. the pending-count read) routed through the one-writer `MomoWatchSnapshotPersister` extension (`MomoWatchPersister+Journal.swift`, O1 census-guarded: `IntentJournal(` in exactly ONE Apps/MomoWatch file); `IntentJournal.append` → `@discardableResult -> Bool` durability gate; `wipe()` wired into `consumeWipe` in F-3 order (snapshot wipe → journal wipe → marker record); steady-receive prune a separate mailbox job AFTER persist (TASK-041's persist-census == 2 preserved); **R3 pat flow** — `pat()` = immediate local micro-reaction fold then `Task { finishPat() }`; `finishPat` = mailbox pending-read → haptic (BEFORE journal I/O, gated on the SYNCED `hapticsEnabled` at pat time; estimate `isCompletingPat` → `.celebration` REPLACES tick, strict `==` so overshoot = tick, missing-Q7 = false) → `appendPat` (journal read FIRST in mailbox; watermark pair read main-actor immediately pre-submission) → send ONLY on a landed append (`transferUserInfo`, fire-and-forget — correctness never depends on reachability); seq formula `next = max(epochScopedJournalMax, epochMatchedWatermark) + 1` pinned by named tests incl. FULL-PRUNE MONOTONICITY; **R5 micro-reaction** — wakefulness-distinct (awake/waking → `.tap` bounce; settling/asleep → `.stir`, stays asleep) via the rig's EXISTING sampler seam (`reactionMotion: (Double, Bool) -> MomoReactionMotion` default `.identity`) — zero MomoCharacter edits; RM → `reduceMotionOverlay`; glyph/AOD binds NO sampler; **R6 haptic seam** — `tick→.click`, `celebration→.retry` (double-pulse) resolved against `WKInterfaceDevice.h:16–33` and recorded in code (ADR-015 D3); **R7/R8 folds** — F-1 nil-character DEBUG-loud once-per-`snapshotSeq` memo; F-2 ADR-014 "HELD" wording reconciled; **R9 UI** — pill a real `Button(.plain)`, canvas `contentShape`+`onTapGesture` touch-only inside the composite a11y element; **R10** — 3 new structural guards (journal census / pat-flow order + single send leg / haptic-seam census) with both-direction fixtures + 3 sha256-restored mutation bites. **Tests:** +46 package (1056/104 → **1102/108**): `WatchPatPlanTests` NEW (~16: seq formula 6 cases incl. full-prune/stale-epoch/mixed-epoch/watermark-wins, estimator truth table incl. OVERSHOOT + missing-Q7, pending count, reaction map), `WatchSessionEpochStoreTests` + `IntentJournalWipeTests` NEW, `MomoWatchPatScanTests` NEW 19 + `Support/WatchPatScan.swift`; `MomoWatchUITests` 3/3 → **5/5** (+2 pat flows). **Cycle:** impl gates green → orchestrator §19 verification (all five gates reproduced: suite, coverage digit-for-digit, both builds, UI 5/5; frozen-surface + D-R3 + pbxproj +8 registration-only reads; all load-bearing files spot-read) → REVIEW-TASK-042 **APPROVED_WITH_MINOR_NOTES** (reviewer re-derived the spec from contract + ADR-014/015 + normative docs BEFORE reading implementation; 3 OWN probes not reusing the implementer's bites — estimator Q7-guard removal, `journalMaxSeq` epoch-filter drop [journalMax 99 vs 2], `.waking`→`.stir` flip — each exactly-red; 18-file sha256 sweep byte-identical; gates re-run green) → findings: F-R1 (Minor) journaled-but-never-sent crash window [verified at source; contract-conformant three-leg scope] → **TASK-044 launch-sweep requirement**; F-R2 (Note) stale launch-read comments → TASK-043 ride-along; F-R3/F-R4/F-R5 recorded. §19 gate **1102/108 + both builds + UI 5/5** on the exact tree; MomoKit coverage **90.79 % lines** (160/1738 missed; `WatchPatPlan` 100 %; floor 80 % holds, down from 91.48 % as disclosed — epoch-store + journal defensive legs; trend watched at TASK-043).
- **TASK-041 — MomoWatch W1 glance + snapshot persistence + AOD (`4407544`, pushed; EPIC-008 2/5 — the Watch's half of the first vertical slice: raise the wrist and Momo is THERE, rendered instantly from local storage; record in `.claude/tasks/completed/TASK-041-watch-w1-glance.md` + REVIEW-TASK-041).** **R1 character DTO (ADR-014)** — additive-OPTIONAL `WatchSnapshot.character: WatchCharacterDTO?` (exactly the four `CharacterDisplayState` fields `DisplayState` lacks: moodBand/energyBand/activity/satietyHint), derived iPhone-side from the SHARED `makeCharacterDisplayState(state)` and threaded through `makeWatchSnapshot` (the ONE iPhone-side change, in `MomoAppModel+Watch.swift`); `decodeIfPresent`/`encodeIfPresent`, NO schemaVersion bump, OBS-3 not fired (the TASK-040 D-1 pattern). **R2 snapshot store** — `WatchSnapshotStore` (watch-snapshot.json + one `.prev`, atomic demotion writes with a no-temp invariant, current→prev→nil read chain, KB-scale). **R3 transport twin** — receive-only ADR-013 per-target WC wrapper (`MomoWatchTransport.swift`; NO send surface — journaled pats are TASK-042). **R4 receive path** — WCSession delegate queue hops to the main actor; decode-or-skip → §6.6 marker consumption → latest-wins render → persist; **R5 consumption** — `WatchResetConsumption.decide` (pure: incoming erase-count > consumed → wipe + settle + record; ≤ → no re-wipe; self-defense reasoning recorded), fused wipe-then-record in ONE `MomoWatchSnapshotPersister` mailbox step so the order cannot interleave; exactly TWO `persister.persist(` production legs (receive + background-gated transition — guard-pinned); the main actor's only synchronous I/O is the KB-scale launch read (OBS-1). **R6 W1 view** — `GlanceView.swift` per UX §6.1: status words + stage word + the single quest line + ≥44 pt INERT pat targets (capture is TASK-042); the settling-in line pre-first-sync via catalog law (`WatchCopyKeys` app-target internal, banned-vocab green); the nil-character degraded path HOLDS the canvas slot with the palette blanket (F-2's "skipped vs held" wording reconciliation routed to TASK-042); VoiceOver composite per UX §10 over SHIPPED strings (the F-2 owner item gates wording only). **R7 AOD** — foreground `.glance` tier / `isLuminanceReduced` → static `.glyph` tier (never binds a clock); **O4 CLOSED: PASS** — stageSide glance 68 / glyph 28, pixel-measured ink ≈36×52 pt inside the §2.1 band mapping, evidence `.claude/tasks/reviews/aod-preview-still-TASK-041.png`; SE-has-no-AOD §25 honesty — live observation routes to TASK-044. **R8 catalog** — settling-in line "Momo is settling in — meet Momo on iPhone." + the Pat label as fixed-lookup keys (no copyEpoch bump — `Sources/MomoCore/` diff empty, epoch 4 untouched). **R9 guards** — three new structural guards in `MomoWatchGlanceScanTests`+`WatchGlanceScan` (persist-on-receive+census / no-engine tokens / AOD-glyph branch), each exactly-one-red sha256-restored. **Tests:** +61 package (995/100 → **1056/104**): `WatchCharacterDTOTests`, `WatchResetConsumptionTests`, `WatchSnapshotStoreTests`, `MomoWatchGlanceScanTests` all NEW + catalog pins + law coverage; `MomoWatchUITests` rewritten **3/3** (settling-in-only fresh launch; fixture-seeded W1 content + ≥44 pt targets; restore-within-budget over a GENUINE disk restore — the DEBUG `-momo-watch-fixture w1` seam seeds via the REAL store pre-launch, release never seeds). **Cycle:** impl gates green → orchestrator verification (suite reproduced; D-R3 re-read at pbxproj source — both app targets `dependencies = ()`; iPhone-side diff read: the `+Watch` extension only, `MomoAppModel.swift` 800/800 untouched) → REVIEW-TASK-041 **APPROVED_WITH_MINOR_NOTES** (reviewer re-derived the spec BEFORE reading implementation files; one-writer census + blind-spot hunt clean; Bite C re-executed exactly-one-red over the final tree + 3 own probes [Probes 1/3 exactly-one-red, Probe 2 honestly 2-red — the boundary is double-covered], all sha256-restored, 28-file final sweep byte-identical; every gate independently reproduced digit-for-digit) → findings dispositioned: F-1 (Minor) DEBUG-loud nil-character log not implemented [verified at source — routed to TASK-042]; F-2 (Minor, doc-only) skipped-vs-held wording [verified — routed to TASK-042]; F-3 (Note) layout-share ACCEPTABLE + UX §6.1 "~40 %" doc-erratum → backlog; F-4 (Note) cosmetic Pat-capsule ~2 pt nit, no action. §19 gate **1056/104 + both builds zero touched-file warnings + UI 3/3** on the exact tree; MomoKit coverage **91.48 % lines** (down from 92.47 % — disclosed: the store's defensive legs + consumption guards; floor 80 % holds).
- **TASK-040 — iPhone-side WC session (`0719129`, pushed; EPIC-008 1/5 — the first vertical slice's iPhone half; record in `.claude/tasks/completed/TASK-040-iphone-wc-session.md` + both review records).** **R1 context push** — the `.pushWatchSnapshot` seam (reserved since TASK-031) is REAL: latest-wins `WatchSnapshot` (DisplayState + today's questInputs via the SAME day-record lookup the cascade reads + haptics + the epoch-scoped watermark pair + the §6.6 marker count while non-zero) built by `WatchSnapshotBuilder.makeWatchSnapshot` at the plan's IFF-changed push step; the spent `nextSnapshotSeq` persists through the ONE writer (`MomoWatchSyncPersister` actor — O1's single-writer rule, fed only by `persistWatchSyncState()`, exactly two call sites: the push arm + the receive path). **R2 intent receive** — `receiveWatchEvent` (decode-or-skip [garbled/future-version frames skipped silent-by-design] → `WatchReceivePlan.decide` through the accessor-driven `SyncState.shouldApply` [O5; zero `WatchSyncGate` tokens in comment-stripped `Apps/Momo`, guard-pinned] → record BEFORE apply [the F-1 wiring leg a decision test cannot see, pinned behaviorally + structurally] → the SAME facade `apply(trigger: .interaction)` path → UNCONDITIONAL sync persist [a no-outcome-change apply still lands its watermark — D-4 verified sound]); FIFO exactly-once + 3-seed shuffle at-most-once integration tests (O2's adjudicated shape, not strengthened). **R3 reset-marker leg** — additive OPTIONAL `WatchSnapshot.resetMarkerEraseCount: Int?` (`decodeIfPresent`/`encodeIfPresent`; NO schemaVersion bump — OBS-3 correctly not fired); the marker lives OUTSIDE the store tree (App Support root via `StoreRules.watchResetMarkerDirectory()` = `defaultDirectory().deletingLastPathComponent()`, D-2) so the erase's tree deletion can never touch it; signal-first erase ordering (marker read → +1 → save → THEN delete). **R4 VERIFY-AT-BUILD record** — all WC transport determinations resolved against real SDK headers (the cited iPhoneOS.platform `WCSession.h` byte-identical to the simulator SDK copy; delegate-before-activate :45, context launch delivery :106-107/:182, latest-wins :111, userInfo enqueue/persists-across-exit :115, non-main serial delegate queue :136-139, three required delegate methods :143-152; the Swift-overlay rename `activateSession()`→`activate()` erratum honestly recorded; the entitlement/capability/`UIBackgroundModes` sweep over all 7 framework headers + apinotes = ZERO hits — nothing required, nothing added; runtime paired-hardware proof deferred to TASK-044 per §25). **R5 placement (ADR-013)** — `MomoWatchTransport.swift` (protocol + `LiveWatchTransport`) + `MomoAppModel+Watch.swift` (executor) are app-target code; pbxproj explicit PBXFileReference registrations (4, hand-minted IDs); D-R3 verified at source (both app targets `dependencies = ()`). **Tests:** +46 package (949/97 → 995/100): `WatchReceivePlanTests` NEW 8 (decide matrix, per-epoch max-semantics recording, FIFO exactly-once stream, 3-seed shuffles, no-op-leaves-untouched, the F-1 AppModelPlanCore propagation loop, decode compatibility), `WatchResetMarkerTests` NEW 13 (roundtrip, garbled→nil, `markerOutlivesTheErasedStoreTree`, compat pins), `MomoWatchWiringScanTests` 14→21 (+7 guard-4 fixtures both directions + standing real-tree), `StoreRulesPinnedTests` +4 (marker-directory rules); MomoKit coverage **92.47 % lines** (91.65 % regions / 92.62 % functions — floor 80 % holds, up from 91.42 %). **Cycle:** impl 988/100 + both builds green → orchestrator verification (R4 header re-diffs, pbxproj registration + D-R3 reads, marker/store greps) → REVIEW-TASK-040 **CHANGES_REQUIRED**: F-1 HIGH — the marker's launch-load site (`MomoAppModel.swift` init, store tree) and erase-save site (`+Settings`, App Support root) read/write DIFFERENT directories, so the marker could never survive a relaunch (the exact §6.6 hazard: stale pre-erase journal draining over the FIFO stream and applying warm onto the fresh pet); Probe C proved the suite structurally blind (988/988 green with the load pointed at a nonexistent directory); F-2 MEDIUM — the enabling test gap → fresh fix agent (F-1: the init load re-derived through the SAME `watchResetMarkerDirectory()` rule via static `loadResetMarkerEraseCount()` in `+Watch`, DEBUG-loud + `Momo-marker-fallback` throwaway dir mirroring the erase-site discipline; F-2: guard 4 `markerDirectoryViolations` — 4 comment-stripped legs + 7 new tests with both-direction non-vacuity; bite = helper flipped to `StoreRules.defaultDirectory()` → EXACTLY ONE red) → REVIEW-TASK-040-delta **DELTA VERIFIED** (independent probes D1/D2 with different mutations, 18 handoff hashes reconciled, 14 non-fix files byte-identical) → final **APPROVED_WITH_MINOR_NOTES** → §19 gate **995/100 + both builds green** on the exact tree. Carried: F-3 (release-build silent marker-save accepted; Watch-side journal self-defense note → TASK-041/044), F-4/F-5 (inert records), F-6 (D-7 no-app-target-test posture accepted — protocol seam the right investment), F-7 (D-12 stale `AppModelPlan.swift` doc comments → orchestrator doc-only backlog), F-8 (`MomoAppModel.swift` exactly 800/800 — zero headroom; all further executor work in extension files).
- **TASK-035 — Feed / Play / Care flows (`b8d181a`, pushed; EPIC-007 5/9 — the care-interaction loop is COMPLETE: the three action-row families produce their engine-distinct flows end-to-end; record in `.claude/tasks/completed/TASK-035-feed-play-care.md` + both review records).** **R1 report drain** — `MomoDirectorState.drainReports()` (the exactly-once bridge had to live director-side: `reports` is `public private(set)`, so the app model can read but never clear) + `MomoAppModel.foldDirector` submits each entry via `submit(_:)` in emission order; the play count (the FIRST report-consumed effect) now lands through `.characterReport`. **R2 play round UX** — in-flight drags stream director `.fingertip(offset:moving:at:)` events (center-origin y-down grid units, 1-pt travel threshold, down-bookend `moving: false` sample) INSTEAD of pat intents, reverting after cease; the quiet "Done" pill (authored `donePillDelaySeconds = 5.0`, inside UX-3's 4.5–6 s band, real-time Task so it works under the frozen clock; DERIVED visibility `isPlayRoundInFlight && isDonePillWindowOpen` so it closes on ANY end; hosted OUTSIDE the canvas's flattened a11y element as a separate VoiceOver button) routes `stopPlayRound()` → R6. **R6 the ONE disclosed `MomoCharacter` seam** — `case playStopped(at: Double)` (+ `eventTime`) in the frozen enum, `applyPlayStopped(at:)` reusing `applyHidden`'s displacement-cancel machinery, `handshakeCancelled(.play)` exactly-once only when a round was in flight (tolerated no-op otherwise); `MomoReduceMotion.swift` UNTOUCHED — its switches are over presentation state, not events; the RM twin corpus pins the cancelled-round fold instead (+1 test, 2 corpora). **R4 pill parity** — `actionPills` gating restated to read EXACTLY the engine rules (tuck-in ⟺ `isTuckInWindow` ∧ wakefulness ≠ `.waking` ∧ ¬playing; nap ⟺ (drowsy ∨ exhausted) ∧ ¬(asleep ∨ napping) ∧ ¬settling ∧ ¬playing; designed pin movements: the 06:30 tuck-in shoulder moved `HomeReadModelTests` 2→3 pills). **R5 copy landing** — 21 catalog keys (18 react verbatim, U+2019 incl. the O1 `touch.03` normalization, authority comments; 3 care-moment FIXED lines 01/02/03, fixed-lookup never drawn); `slotLineCount(.careMoment)` stays 1; `HomeCopyKeys.careMomentLineKey(for:)` mirrors `greetingLineKey`; contextual priority careMoment > greeting > ambient (UX-12 verbatim) with in-memory retention disclosed (rides the OBSERVATION-A owner item); `copyEpoch` 3→4 — **NOT same-residue**: ten-line slots moved `.02`→`.07`, touch `.02` SURVIVED (mod5=2), 6-line pools pin `.01` (mod6=1); day-fixture discipline: `CareInteractionTests`' `2026-09-09` day draws care index 4 (`.04`), its `2026-09-08` pins `.01` — epoch-parity re-pins in four suites, each commented in place. **R7** — `SpokenReaction.announcementKey` widened to all four react families (the type lives in `Sources/MomoKit/CanvasTouch.swift`, not the contract's stated app path — erratum disclosed). **R8** — §10.4 verbatim a11y labels via an exhaustive no-default switch ("Pat"/"Feed Momo"/"Play with Momo"/"Tuck Momo in"/"Nap time"); visible chrome unchanged. **R9 structural guards** — now FIVE total: R9a drain→submit, R9b in-flight fingertip surface, R9c Done-pill stop wire (identifier AND `appModel.stopPlayRound()`), R9d the F-1 fix's stillness-ticker four-leg conjunction (cadence constant + sleep-on-constant + `moving: false,` fold + start wire); each bites on its violation fixture, mutation-bitten to exactly-1-failure with sha256-proven restores. **Tests:** +21 package (906/92 impl → 907/92 post-fix: `CareMomentTests` NEW 7, handshake 4, plan-core 2, rig 4, RM twins 1, verbatim 1, contextual priority 1, epoch-parity re-pins) + 4 UI (feed-refusal line, Done-pill flow, 20:30 tuck-in flow, a11y labels; nap-pill gate not deterministically reachable under a frozen clock — disclosed per §25). **Cycle:** impl (incl. the interim standalone UI test file consolidated into `MomoHomeUITests` before the final run; pbxproj byte-identical with HEAD) → REVIEW-TASK-035 APPROVED_WITH_MINOR_NOTES (independent epoch-4 re-derivation with a replica validated against epochs 2/3 goldens FIRST; three guard bites; UI 17/17 + Watch green reproduced) → **F-1 (MEDIUM) FIXED in-task**: the play stillness ticker (`playTickerSeconds`, `runPlayTicker`'s `moving: false` fold, the reconcile start wire) is the passive round's ONLY fold source — load-bearing for AC-2's solo auto-end — yet unguarded (zero Tests/ references; verified personally); fresh fixer added R9d (+38 lines, zero deletions elsewhere) → REVIEW-TASK-035-FIX APPROVED_WITH_MINOR_NOTES (delta provably +43/0 test-file-only; THREE bites; `MomoAppModel.swift` sha256-identical to the reviewed tree; UI/Watch carry-over valid per §5) → §19 gate **907/92 + UI 17/17 + Watch build green**. Findings F-2 (double-start guard is instance-state, safe under main-actor FIFO) + F-3 (19 pre-existing actor-isolation warnings in diff-empty `MomoUITests.swift`, whole-module recompile debt) + N-3 (substring guards can't catch comment-entombment — accepted trade; optional hardening recorded) → TASK-039 carries F-3's `@MainActor` housekeeping.
- **TASK-034 — Touch & petting (`2264f35`, pushed; EPIC-007 4/9 — the composed canvas becomes INTERACTIVE; record in `.claude/tasks/completed/TASK-034-touch-petting.md` + both review records).** Every §6.1 gesture×zone class now renders its engine-distinct clip through the frozen director ON the composed Home: **MomoKit** — `CanvasTouch.swift` (pure vocabulary: `CanvasTouchLaws` with per-constant authority labels, TOTAL `CanvasZoneHitTest.zone(normalizedY:)` (NaN→belly, negatives→head), `CanvasTouchClassifier`, `CanvasTapSequence` (pairing requires the second tap to END after the first, so the zero-on-pause clock never pairs across a reset), `CanvasCustomAction` (Pat/Cuddle → zone-less `.pat(.tap,.nil)`/`.pat(.longPress, nil)`), `SpokenReaction.announcementKey` (the UX-8 gate admitting `react.touch.*` ONLY); imports Foundation + MomoCore, no MomoCharacter); **Apps/Momo** — `HomeCanvasTouchLayer.swift` (the transparent `DragGesture(minimumDistance: 0)` overlay with a `@GestureState` press seam → `resolveAsCancellation()`, R9), `MomoAppModel` director ownership (`director` beside `canvasClock`; `touchBegan` force-close recovery; `reactionMotion()` factory capturing the director BY VALUE (RM-selecting); `foldDirector`; the announcement seam gated on `UIAccessibility.isVoiceOverRunning`; `scenePhaseChanged` with the CORRECTED semantics — `.background` → `.appHidden` + `backgrounded()`, `.inactive` → `.appHidden` ONLY), `HomeView` `.accessibilityActions` custom actions + the `reactionMotion:` wiring; **catalog** — the 5 contract-pinned `momo.line.react.touch.00–04` lines + `reactLineCount(.touch)` 1→5 + `copyEpoch` 2→3 (the R4 carveout, MomoCore diff = CopyRules only; epoch 3 is SAME-RESIDUE — draw index 2, exactly as derived); **tests** — +18 package (CanvasTouchTests 11, PatPlanBondTests 2 incl. G2 zero-bond, MomoTouchSemanticsTests 4, the F-1 wiring guard) + 3 UI (route-through, ONE-element + label, no-stage-word movement). **Cycle:** impl 886/91 + UI 13/13 + Watch build green → orchestrator verification CLEAN (purity file set, carveout diff, epoch derivation) → REVIEW-TASK-034 **CHANGES_REQUIRED**: F-1 HIGH — `HomeView` never passed `reactionMotion:`, so the frozen identity default served EVERY frame: no reaction, press layer, or clip ever rendered despite fully-green suites (they assert navigation/labels, not rendered motion — the wiring leg was untested); F-2 MEDIUM — `.inactive` routed through `backgrounded()` beyond disclosure (c), beneath a contradicting comment. Both findings verified personally; disclosure (b) verified against the SDK swiftinterface (`.accessibilityCustomActions` does NOT exist; `.accessibilityActions` is the surface). → fresh fixer: the one-line wiring + a STRUCTURAL GUARD (`RigDisciplineTests` reads `HomeView.swift`, asserts the construction passes the factory, non-vacuity pinned, mutation-bitten exactly-1-failure) + the F-2 case split with the corrected comment and the disclosed behavioral delta (`.inactive` no longer cancels `boundaryTask` — adjudicated ACCEPTABLE by the delta reviewer: boundary consumed not replayed, `@MainActor` serialization, idempotent segment catch-up) → REVIEW-TASK-034-FIX **APPROVED** (reviewer reproduced the bite, re-ran everything: 887/91, UI 13/13, build green) → §19 gate at the final tree (orchestrator: **887/91 + UI 13/13, 229.9 s**). Carried: N1 (superseded maturation task — benign, cosmetic), O1 (straight apostrophe `touch.03` is contract-verbatim; normalization rides TASK-035's catalog), O2 (pbxproj exact). The rendered-motion UI assertion was rejected as structurally flaky (XCUITest cannot see canvas pixels; the idle animation defeats screenshot diffing) and routed to TASK-039.
- **TASK-033 — Home composition (`f1668b7`, pushed; EPIC-007 3/9 — the placeholder shell becomes the product's front door).** The five-region UX §5.1 S4 composition is REAL over the TASK-031 facade: **catalog landing** — 70 entries into `MomoCopy.xcstrings` (40 verbatim slot lines = 04 §10.3's ten morning/day/evening/night lines, 3 greetings, 12 vocabulary, 8 status words, 7 quest wishes verbatim PRD §5.2; 72 keys total with the seed era), `copyEpoch` 1→2 per §4.10 with the R4 `CopyRules` carveout as the ONLY MomoCore edit (slot pools 10/1; the picker's ZERO-BASED truth over real pools documented in the `CopyRules` header); **MomoKit** — `HomeCopyKeys` (type-safe builders incl. the greeting fixed-lookup 01–03) + `HomeReadModel` (`HomeStatusBand` glyph+WORD never numbers, `HomeContextualLine` UX-12 priority greeting > ambient slot, `HomeQuestRow` UX-5 window rendering Q1 morning-only / Q6 from 20:00) over `appModel.interact` routing; **Apps/Momo** — `HomeView` five regions with the AC-1a 305-pt screen-keyed canvas floor (rig ≥ ~45 % via `MomoRigView(displayState:tier: .full, clock: appModel.canvasClock)`), the AC-1b conditional ScrollView, per-region views 15–132 lines, `PlaceholderHomeView` removed. Enablers: DEBUG `-momo-fixed-clock` (UTC-pinned fixed calendar — the local-hour windows need it) + the disclosed launch-open evaluation kick in `MomoApp.body` (05 §4.2 row 1; a real TASK-031 gap — onChange never fires when the scene is already active at install; §22-justified). Tests: +11 package (7 `HomeReadModelTests`, 2 `CatalogCopyLawTests`, 1 `VocabularyKeyTests`, +1 net from the re-pinned `CopySelectionPinnedTests`) + 5 NEW `MomoHomeUITests` on the pinned sim (composition, AC-1a no-scroll, AC-1b scroll, WORDS-not-numbers, pills routing). **Cycle:** impl 868/88 + app 10/10 → orchestrator verification CLEAN (tree matches the contract file plan; frozen purity held — `Sources/MomoCore/` diff = the R4 carveout ONLY, `Sources/MomoCharacter/` EMPTY; suite reproduced; catalog spot-checks verbatim; the CONTRACT'S OWN "3 rows at 20:30" line CONFIRMED UNREACHABLE and annotated at the task file's R8 — the day-1 quest set is exactly [Q1, X, Q6] with disjoint windows, so the tests' 2+2 law is correct) → REVIEW-TASK-033 **APPROVED_WITH_MINOR_NOTES** (docs-first re-derivation; catalog byte-compared string-by-string; 868/88 + the app suite reproduced ×2; 5 mutation bites; independent seed→index draw re-derivation — the orchestrator reproduced the epoch-2 derivation bit-for-bit) → **dispositions applied by the orchestrator** (each verified personally): NITPICK-1 `-momo-fixed-clock` WITHOUT a value now DEBUG-loud (guard split + `assertionFailure`, same discipline as unparseable); NITPICK-2 `.lineLimit(2)` documented (a11y wrap, not truncation); NITPICK-3 care-moment routing "TASK-034/035"→"TASK-035" ×2; NOTE-2 `rawKeysPinned` same-residue comment. **MINOR-1** (verbatim VALUE pins beyond the 12 vocab entries — the slot-line rewrite survived all 36 catalog-facing tests) → routed to TASK-039's contract; **OBSERVATION-A** (greeting domination — `lastGreeting` persists for the day so the ambient 40-line pools rarely surface) → OWNER product item; **OBSERVATION-B** → doc errata at 03 §5.5 (Q6 opens UNCONDITIONALLY hour ≥ 20 ∨ < 7; engine normative); **OBS-C DISCHARGED** — dated reconciliation note at 04 §10.3 (the doc's Day 12:00–16:59 / Evening 17:00–21:59 headers are ILLUSTRATIVE; the engine's `CopyRules.timeSlot` cut-offs 12/18 are NORMATIVE; no line moved). §19 gate green at the final tree: **868/88 + app 10/10**.
- **TASK-032 — Onboarding S1→S2→S3 (`53e433f`, pushed; EPIC-007 2/9 — record backfilled 2026-09-10; full detail in the completed task file + REVIEW-TASK-032).** The front door is REAL over `requiresOnboarding`: the sixth plan-core trigger `onboardingCompleted(petID:name:)` dispatched before engine routing (grants nothing, UX-6), the atomic completion write (FR-13 AC-1), the three-step flow UI with INV-1 validation (whitespace rejected, name pre-filled "Momo", changeable later) + a11y groundwork, the `-momo-store-directory` UI-test enabler. 857/86 green (+6/+1 incl. `AppModelOnboardingPlanTests`); UI battery incl. the new `MomoOnboardingUITests` flow/restart/a11y pins; REVIEW-TASK-032 APPROVED_WITH_MINOR_NOTES.
- **Doc-clarification backlog cleared (2026-09-10, orchestration housekeeping).** Five standing adjudications recorded into the normative doc chain as dated additive notes citing their review records — zero normative numbers changed: (1) PRD §4 feed errata under the response matrix — band→BEAT / satiety-phase→EFFECTS, "smaller effect" realized only via the ×0.25 recently-fed nibble (REVIEW-TASK-016 adjudication 8); (2) PRD §5.3 + 05 §4.8 Q6-credit clarification — the STRONG reading normative (both priors carry Q6, `count == 2`, unknown priors force Q6, fresh install opens with Q6; the ≥4-pairs arithmetic coherent only under it) (REVIEW-TASK-018 open question 1); (3) 04 §4.2 + §7.1 — Drowsy low-key play variant authored ≈ 14.4 s, deliberately below the 15–30 s band (REVIEW-TASK-028 NOTE-5; both rows carry the band so both carry the note); (4) 04 §8.5 — "sparkles moment-scoped" scopes the animation channels; the sparse static at-rest sparkles are shipped baseline pre-dating TASK-028, at-rest rendering unchanged (identity opacity — verified true against `RigPropPose.rest`, transform `.identity` / opacity 1, `RigPose.swift:112-117`) (NOTE-6); (5) 05 §4.9 OBS-D resolution bullet + 04 §8.4 Copy bullet — the fourth `momo.line.vocab.<field>.<band>` keyspace ratified as a fixed lookup, not a selection; no seed/epoch machinery (REVIEW-TASK-019 T3/OBS-D). Drafted by a fresh agent (docs-only, no git); every note verified claim-by-claim against its citing record, dates, cross-references (§2.2 props row, §4.11 `*Key` fields, the ×0.25 I-2 cell), and the code by the orchestrator before commit. **OBS-C is now the only open doc-chain item.**
- **TASK-031 — App model facade: evaluate/apply loop + persistence wiring + boundary scheduling (`257a58f`, pushed; EPIC-007 1/9 — the LANE A × LANE B convergence opens).** 05 §4.1–4.2 executable behind the placeholder shell. Pure plan core in MomoKit: `AppModelPlan`/`AppModelTrigger`/`AppModelPlanCore` (persist-IFF-changed → response-IFF-non-nil → moments-IFF-non-empty → push-IFF-changed over the five §4.2 triggers; `choreographySeed` = `DaySeed.make(…, salt: .choreography)` over the fold instant with `choreographyEpoch = 1` single-sourced); `NextBoundaryRules` (napping ⇒ earliest calendar boundary relabeled `.napEnd`; asleep ⇒ the coming 07:00 even past midnight; else earliest of {22:00, 07:00, midnight} — ALL strictly-after via `calendar.enumerateDates(.nextTime)`, hours ONLY from `FoldRules`); `AppModelLaunch` presence probe (§5.3 invisible-recovery reading documented). Thin executor `Apps/Momo/MomoAppModel.swift` (`@MainActor @Observable`; owns clock/store/calendar/scenePhase/time-change; `apply` = step-0 state → schedule → fixed-order steps; ONE cancellable boundary `Task` carrying the boundary instant with `isForeground` guard + cancel on background; the launch read is the ONE sanctioned synchronous main-thread I/O — OBS-1 discharged; `requiresOnboarding` surfaced for TASK-032; `deliverResponse`/`deliverMoments` seams for TASK-032/033; `pushWatchSnapshot` = the reserved EPIC-008 no-op) + `MomoApp.swift` wiring. **Cycle:** impl 850/85 → orchestrator verification (tree purity; frozen modules byte-untouched; zero WatchConnectivity; FoldRules-sourced hours grep; MY OWN simulator relaunch proving the loaded-store branch + the save→load roundtrip; suite reproduced) → REVIEW-TASK-031 **APPROVED_WITH_MINOR_NOTES** (reviewer re-derived 05 §4.1–4.2 BEFORE comparing; §5.2/§5.3 launch-path reading verified; 3 sha256-proven mutation bites; independent build + launch + coverage reproduction) → both MINORs verified personally then applied (MINOR-1: the choreography-seed pin was tautological — wrapper-referencing — replaced with a literal pin against `DaySeed.make` + `choreographyEpoch == 1`; MINOR-2: `scheduleBoundary` moved BEFORE the awaited effect loop so two racing applies can never strand a one-event-stale resume) → §19 gate **851/85 ×3**, MomoKit coverage **91.42 %** (341/373), zero source warnings (the machine-env `ld` notice disclosed), BUILD SUCCEEDED + sim launch (fresh-store AND relaunch lines).
- **TASK-030 — Character test-suite audit + determinism-property coverage record (`4f8e3ac`, pushed; EPIC-006 6/6 — the epic closer).** The audit-and-record task executed per the TASK-024 precedent, WITHOUT a coverage floor (none exists for MomoCharacter — the deliverable per 05 §10.2): audit tables A–E resolving every in-scope 04-clause (§2.1, §3.2–3.5, §5.1–5.3, §6.1–6.3, §7.1–7.4, §8.3–8.5, §9.4–9.5 + 05 §10.1–10.2) to named green tests; the ONE real gap (§3.5's missing in-suite anchor) closed with the new `grayscaleLegibilityAcrossExpressionCells` — all 16 §3.2–3.3 cells, 120 pairs over the 12 hue-free carriers that ARE `MomoExpression`'s 12 fields (R4's state-free coloring as the load-bearing precondition), non-vacuity `#expect(cells.count == 16)`, failure names the colliding pair; the pure-file determinism-property record (view file explicitly excluded with its pure-seam pins; generated/constants files dispositioned constants-only with their pinning suites named; per-suite `@Test` counts — three stale counts corrected per NOTE-2); §8.3 budgets re-measured exact. **Cycle:** impl 825/82 → orchestrator verification (scope exact; budgets digit-for-digit; 8 table mappings spot-resolved to real pinning bodies) → REVIEW-TASK-030 **APPROVED_WITH_MINOR_NOTES** (reviewer re-derived all five tables from the docs BEFORE comparing; body-read every table-C row; reproduced every budget digit; executed the sanctioned bite in TWO stages — stage 1 DISPROVED the task file's own single-gate recipe as INERT (`yawnEnabled` flip left the §3.5 anchor green because `headNodEnabled` is the same Drowsy-only gate; A12's yawn census failed live instead, proving the mutation was in-tree), and the corrected two-gate bite failed on exactly the predicted Low+Drowsy/Low+Exhausted pair — unique because Low's 0.95 × 0.97 = 0.9215 clamps back to the 0.95 posture floor; `MomoExpressions.swift` sha256 `92902afb…` pristine before == after; MINOR-2 caught an INHERITED "11 corpus + 7 adversarial" twin-stream miscount — actual 13 = 8 named + a 5-value hold sweep — rooted in the TASK-029 records) → all 5 findings verified personally by the orchestrator against source and dispositioned as record/comment-only edits (zero production diff; the new test's weakest-pair doc comment corrected to "three tempo carriers, not cycle-only") → §19 gate 825/82 green, zero warnings. Production `Sources/MomoCharacter/` diff ZERO throughout. Routings: none new (NOTE-3's inherited epic phrasing corrected in this closeout's housekeeping, at the epic + completed-TASK-029 records).
- **TASK-029 — Reduce Motion mapping + token-driven theming (`9a46641`, pushed; EPIC-006 5/6).** Reduce Motion is EXECUTABLE as a render-only policy: the full §7.3 binding table realized at the overlay/pose seams (`MomoReduceMotion.swift`, NEW, 516 ln — constants with doc authority labels, the R5 crossfade tracker, the `reduceMotionOverlay(at:)` mirror of the overlay layer structure, pose-path + report/verification discipline), the fold/clock/CharacterReport stream UNTOUCHED (R15 render-only law by construction — `apply(_:)` takes no flag — and pinned behaviorally over the TASK-028 adversarial corpus plus RM-specific streams), state crossfade at the AUTHORED 0.15 band floor of §7.3's 0.15–0.20 (tracker overwrites in-window second changes and the snap is pinned + registered, item 13), press = end-pose swap with the `min()` law (hold-keyframes 0.8/0.45 + release crossfade at the exact 0.15 digit; a short press renders the pose it EARNED — bite-proven), look-around = the L1 press glance itself (flag-invariant, pinned), idle events masked + breath amplitude 0 + wag gated at the pose seam, celebrations = settled moment holds + per-greeting expressive instants (0.88/0.2/0.875/0.7 — deviations disclosed, register 14 covers the glance-up exit cut), waking/settling = end-state statics with reports landing at the unchanged 3.0/2.0 s instants, play = milestone stills at the pacer's boundaries with the round completing identically (FR-7 ACs intact; the post-payoffEnd branch in `reduceMotionPlayMotion` is proven dead — the fold prunes the `.play` layer at `payoffEnd`, director :712-718), R11 static set pairwise-distinct except the DISCLOSED Content+Energetic == Content+Relaxed collision (Energetic's only rendered deltas are RM-masked — owner routing), R12 grayscale evidence (25 committed PNGs via a rerunnable BT.709 harness; byte-identical across runs; ImageIO-verified pure gray — the "23–26 levels" metric retracted honestly; ImageIO counts 146–248 gray values incl. AA), R13 token audit clean (hex confined to the two palette files; `momo.ear.inner` unused status stands; the 8 `MomoColorToken` slots match §8.4). View: `reactionMotion` closure now `(Double, Bool)`, RM params defaulted — zero existing consumers (grep-verified), `accessibilityReduceMotion` is the ONE ambient read (discipline-pinned), glyph tier byte-identical (the AOD branch renders the constant `.rest`). Disclosures register now 15 items. **Cycle:** impl 823/82 → orchestrator verification (suite reproduced; the new source + all seam diffs line-checked; the `(Double, Bool)` compatibility claim resolved via zero-consumer grep; PNG grayscales decoded personally via ImageIO after the hand-rolled decoder false alarm) → REVIEW-TASK-029 **APPROVED_WITH_MINOR_NOTES** (reviewer re-derived §7.3 from 04 BEFORE reading code; verified every constant's authority by opening the docs; 7 own adversarial streams; 2 MINOR + 4 NOTE, all test/doc completeness, none behavioral; 2 sha256-proven bites — the `min()` removal fails exactly `shortPressIsContinuous`, the 0.15→0.2 bump fails exactly the 3 predicted pins) → every finding verified personally by the orchestrator (invite plateau [0.5, 1.7] and freshMorning mid-plateau [0.25, 1.15) / bright ≈ 98 % re-derived from the choreography arithmetic; the unpinned-overwrite diagnosis corroborated by corpus grep — no twin-corpus displayState pair inside 0.15 s) → fresh fixer (8 items: +1 overwrite-snap pin test, Disclosure 4 honesty rewrite + owner line, register items 13–15, 2 comment corrections, evidence-metric retraction) → orchestrator bite on the NEW pin (keep-older-window mutation → exactly the new test fails at both overwrite assertions; restore sha-proven) → **824/82** at commit. Routings: **→ owner (§36/§24)** — 11/15 non-press keys render no visible touch acknowledgment under RM (an authored acknowledge-static is the owner's call); the Content+Energetic static collision (expression design). **→ EPIC-007** — §2.4 continuous pupil-follow; haptics delivery at the presentation seam (the moment surface still fires); Home is the first real consumer of the new view API. Standing: O4 → EPIC-008, O2 → TASK-025 owner, ADR-013 reservation unchanged.
- **TASK-028 — Reaction vocabulary + state choreography + CharacterReport (`cc98b06`, pushed; EPIC-006 4/6).** The character is INTERACTIVE per 04 §4/§6/§9.2: the 21-key `MomoReactionKey` clip table (raw values digit-for-digit the `ReactionKeys` strings; per-clip `RigChannel` masks, tempo scope ×1.4-doc/×1.5-AUTHORED, press/cycle shape, AUTHORED `pressReleaseSeconds` (belly 0.45 / head 0.6); exhaustive no-`default` switches in BOTH the spec table and the choreography — a new engine key is a census-test + build failure), the pure timeline-injected fold director (Rule 1–6 coherence law; the REAL bounded one-shot L3 queue — chain-in-turn, ≤ 2 pending, drop-oldest absorbed silently with survivor re-chain, L2/hide clears pending; the press lost-boundary cap 5.0 s; coalescing 500 ms 1–2 full / 3–4 abbreviated / 5+ one beat / 6+ absorbed; glance-up gated on activity + touch family; hide/show epoch discipline), exactly-once CharacterReport emission for every report kind (`reactionFinished`/`playRoundFinished`/`handshakeCancelled(.settle|.play)`/`settleFinished`/`wakeFinished`/`momentFinished`), handshake choreography (settle 3.0 s — blanketAdjust rides ADDITIVELY per the doc's L3 care-beat classification, no cancel, settle completes; wake 2.0 s never cancelled; play pacer ≤ 22.4 s worst case with evidence-based cease), L4 moments (quest sparkle 1.0, celebration 1.8, 4 greetings) with dedupe + supersession, 8c prop-channel wiring through the TASK-026 single write path. **Cycle:** impl 779/79 → orchestrator verification (suite reproduced; all sources line-checked; engine mints verified in `InteractionSemantics` incl. the :357 settle warm-reaffirm + `HandshakeMachine`) → REVIEW-TASK-028 **CHANGES_REQUIRED** (2 MAJOR / 3 MINOR / 7 NOTE; reviewer re-derived the doc tables BEFORE reading sources; 6 adversarial probes; 2 sha256-proven mutation bites) → every finding verified personally by the orchestrator (both MAJORs impl-side: blanketAdjust routed to the exclusive L2 slot cancelled the engine's "settle still completes" warm reaffirm — the doc classes it L3; the glance-up gate over-scoped Rule 5 to ALL 16 L3 keys vs the touch-scoped doc text — mid-meal politelyFull rendered as a generic glance-up; MINOR-3's queue vacuity was partially seeded by the contract's own ambiguous phrasing, tightened as part of disposition) → fix round 1 by a fresh agent (8 items, +10 tests, re-chain defect self-caught and disclosed) → REVIEW-TASK-028-FIX1 **APPROVED_WITH_MINOR_NOTES** (all 8 items REMEDIATED behaviorally via 15 probes from the original repro recipes; 1 sha256-proven bite; 789/79 ×2) → FIX1-NOTE-2 one-token hardening applied by the orchestrator (`supersededAt == nil` on drop-oldest's firstIndex) + final gate green **789/79** → commit `cc98b06`. Routings: **FIX1-NOTE-1 → EPIC-007** (a NAKED lost-boundary press — touchBegan with no reaction arrival and no boundary — holds the L1 press feedback indefinitely; touch-cancel seam `touchesCancelled` or press-fade cap, touch wiring's seam); **NOTE-5 (drowsy round 14.4 s vs §4.2's 15–30 row — contract-mandated SHORTER variant) + NOTE-6 (static at-rest sparkles pre-date TASK-028) → doc-clarification backlog** (EPIC-008 O4-adjacent).
- **TASK-027 — Deterministic idle sequencer + expression system (`d26b9d3`, pushed; EPIC-006 3/6).** The character is FULLY ALIVE: deterministic idle sequencer (4 independent SplitMix64 substreams — blink/gaze/variant/yawn, seeds = master draws 1–4, pinned per-scheduler draw orders, log sorted by `(start, kind)`; authored intervals/subranges all inside the §5.2/§7.1 doc bands, each pinned by distribution tests), full 7-band × 8-row expression system (§3.1 posture band pre-clamped in the expression layer; §3.2–3.4 posture-led; compound-rule conflict law), greedy priority admission arbiter (`MomoIdleArbiter.admitted` — render-level only, schedule stays doc-exact, blink aperture never displaced, budget ≤ 3 channels, property-OptionSet groups), unclamped multiplicative body-scaleY composition (**ADR-011** — §3.1's band governs the STATIC posture channel only; the §7.2 breath stays a pure sine on every band, verified by a 64-row × 720-sample analytic-equality sweep), zero-on-pause resume discipline pinned through the full pipeline (**ADR-010** — real-time replay-from-0, no burst, seed unchanged; TASK-026 clock pins untouched), two-tier occupancy budget (**ADR-012** — Content/baseline doc-exact mean+p95 ≤ 0.15 measured 0.110/0.142; cross-state authored mean ≤ 0.20/max ≤ 0.25 after the disposition's provisional 0.20 ceiling was FALSIFIED by measurement, Joyful 0.232), plus the four blocking TASK-026 routings landed (R1 composition reconciliation before any head/ear/tail channel; R2 no-bounce tolerance restated; R3 settle ease-in constant + pin; R4 ear ±25°/tail ±10° clamp homes) and O6's §3.1 lower-lid pose shapes. **Cycle:** impl 716/71 → orchestrator verification (suite reproduced; all 9 sources line-checked against 04 §3/§5/§7.1–7.2; pin digit-checks; expression-sweep spot-probes) → REVIEW-TASK-027 **CHANGES_REQUIRED** (2 MAJOR/2 MINOR/3 NOTE; reviewer independently reproduced MAJOR-1's posture×breath clip with matching numbers; 4 sanctioned mutation bites sha256-proven) → fix round 1 by a fresh agent (all 6 disposition items; both deviations vindicated by bites) → REVIEW-TASK-027-FIX1 **APPROVED_WITH_MINOR_NOTES** → the delta MINOR (mis-attributed 0.2231) personally re-measured by the orchestrator (probe: content/drowsy + content/exhausted both max 0.1365 at 100 and 1000 seeds; 0.2231 is joyful/energetic's runner-up) and corrected in 3 places → **721/72 green** at commit (+94/+7 vs the 627/65 baseline). Routings: **→ TASK-028** — reactions/§6 vocabulary + state choreography + `CharacterReport` (04 §4, §9.2); 8c prop wiring; NOTE-2 stale no-op `clampedPostureScaleY` wraps in `MomoIdleRenderTests` (~:227, :456-459) → housekeeping. Standing: O4 → EPIC-008; O2 → TASK-025 geometry owner; NOTE-1 scanner extensions → opportunistic backlog. ADR numbering: standalone-watch-packaging reservation renumbers ADR-009→**ADR-013**.
- **TASK-026 — MomoRig layer tree + CharacterClock + LOD tiers (`d0955e0`, pushed; EPIC-006 2/6).** The rig is ALIVE-able per 04 §2/§7.4/§9.5: transform-only §2.2 layer tree (`RigLayerTree` — 21 rig constants + 4 props in TASK-025 evidence z-order, 11 anchors, outermost-first stage composition so child-local applies before ancestors; props carry channels with no stages — TASK-028 wiring), `CharacterClock` (R3 — injected `any EngineClock`, NSLock-guarded; pause-zeroes/resume-from-zero/idempotent/frozen-while-stopped; `elapsed(at:)` side-effect-free), pure LOD tiers (`RigLOD` — watch-never-full pinned enumeration-wide; §2.1 bands 220–280/60–80/24–32 pt; 21/11/3 disjoint part sets), §7.2/§7.1 curve constants + executable no-bounce law (O7 discharged — `isSingleSoftOvershoot`), pure `RigMotionModel` (rest = identity; pupil clamp ≤ 30 % eye radius; displayState inert until TASK-027), Content breath wired clock → model → view (4.9 s / 2 % pure sine, bottom-anchored — pixel-probed +2.02 %, feet planted), scenePhase → the ONE clock gate (glyph tier never binds the clock — stillness is the AOD posture), 8 committed size-ladder PNGs + re-runnable `render_rig_evidence.swift`. **Cycle:** impl 627/65 → orchestrator verification (suite reproduced ×2; R1/R4 greps; all 8 sources line-checked; evidence vision-read fresh-URL) → REVIEW-TASK-026 **APPROVED_WITH_MINOR_NOTES** (0 MAJOR/4 MINOR/6 NOTE; §2.2 re-derived name-for-name; own clock-interleaving harness; pins digit-for-digit; BOTH sanctioned mutation bites sha256-proven restored, exactly the predicted pins failing; O2 full-tier drowsy CONFIRMED → geometry owner, glance/glyph ACCEPTED; O3 accepted with polish note) → disposition, every finding verified personally by the orchestrator (orchestrator probe reproduced MINOR-1's counterexamples digit-for-digit AND overturned the orchestrator's own earlier "equivalent" derivation; MINOR-4 lid math probed; MINOR-2 confirmed analytically — underdamped steps cross twice for any ζ<1): REQUIRED doc-only fixes applied pre-commit (composition-order comments corrected — view = T→R→S ancestor-first vs harness = S→R→T child-first, EXACT-equal only while the breath is the sole non-identity channel; `lidScaleY` semantics corrected — smaller scales LIFT the lid, 0 = no lid; ear.inner disclosure corrected — the `momo.ear.inner` token is unused, no generated constant exists), test name renamed to match its pinned numbers. **Blocking routings → TASK-027 contract:** MINOR-1b composition-order reconciliation; MINOR-2 predicate tolerance/intent; MINOR-3 settle/sleep ease-in constant; 8b ear ±25°/tail ±10° clamp homes. **→ TASK-028:** 8c prop wiring. **627/65 green** (baseline 549/59; +78/+6) — impl ×2, reviewer ×2 (pre/post-bite), orchestrator ×3 incl. post-disposition.
- **TASK-025 — Asset export pipeline + generated rig Path constants (`7681498`, pushed; EPIC-006 1/6 — first LANE B task).** The repo-local build-time pipeline is real and dependency-free: `Tools/character-pipeline/` (Python stdlib-only — `geometry.py`, `parts.py` as the geometry source of truth, `verify_geometry.py` gate incl. compound-fill winding checks, `emit_swift.py`, `render_svg.py`, `generate.py` CLI, `render_evidence.swift` CoreGraphics rasterizer) → **44 generated `Path` constants** committed as reviewable Swift (full rig 21 = the 19 counted §2.2 parts + ADR-001's hind feet, mouth as a 3-pose slot; LOD-glance 11; glyph 3; room 5; props 4), §8.4 `MomoRig.<part>` naming + GENERATED headers on all 11 files, the hand-written `MomoRig.swift` anchor (1,810 B) kept separate, plus 6 new test suites (geometry / inventory / variants / generated-discipline / pipeline-reproducibility / art-budget) and 3 deterministic PNG evidence canvases proven byte-exact projections of the committed constants. **Cycle:** impl 547/59 → orchestrator verification green (12 checks) → REVIEW-TASK-025 **CHANGES_REQUIRED** (2 MAJOR: M1 — the ADR-001 ear-rule pin measured bbox width (22.49 %) instead of the at-base chord at the ear-root line (true 12.12 % vs the ≥ 12 % floor); M2 — counter-wound compound subpaths created nonzero-fill cancellation holes at the glyph's ear/head and foot/body seams and in the food mound) → both diagnoses verified personally by the orchestrator (bézier chord measurement; shoelace signed areas: glyph head −142,008 vs ears +25,499 / feet +10,336) → §11 fix round 1 (at-base `widthAtY`/`width_at_y` measurement in BOTH checkers; uniform winding + `HOLE_COMPOUNDS = ("window",)` + seam-fill pins `glyphSeamsFill`/`foodMoundFills`; explicit subpath starts; `__pycache__/` gitignored; sparkle nudge on evidence canvases only) → delta review **APPROVED_WITH_MINOR_NOTES** (independent Swift parser + real `PathMeasuring` + pixel forensics; the one-ear winding mutation bite fails exactly the named pin at `MomoRigVariantsTests.swift:76`; 12/12 sha256-proven restore) → **549/59 green ×2**. Budgets measured, not asserted: rig 50,096/307,200 B, room+props 20,647/256,000 B, total 70,743/1,572,864 B. Standing lessons recorded under Important Context: pipeline-side verify is a self-consistency gate (Swift pins are the landmark authority — mutation-proven); winding math beats visual inspection (vision models self-contradicted across all three PNGs; pixel forensics + winding numbers authoritative). Routings: O2 (drowsy-eye margin) + O3 (tail refinement) → TASK-026+ surface verification; O4 (glyph true-size 24–32 pt) → EPIC-008; O6 (§3.1 lower-lid poses) → TASK-027 contract; O7 (no-bounce pin) → TASK-026 contract; O8 (budget-bucket basis) noted; O5 room-composition nits recorded.
- **TASK-024 — MomoKit suite audit + §10.4 mapping + coverage floor (`4372913`, pushed; EPIC-005 closer).** The audit-and-floor task executed literally: audit table 1 (all three §32 persistence rows — save/load, migration, corruption/recovery — resolved to named green tests with pin-verification lines; every named test's body read, not existence-grepped), audit table 2 (all seven §10.4 sync-matrix rows — Kit-share mapping + explicit non-Kit routing to EPIC-008 Watch-UI shares / EPIC-007 iPhone relaunch UI / EPIC-002 VERIFY-AT-BUILD device obligations / EPIC-004-landed Core halves, nothing silently dropped), the NFR-7 fresh-vs-upgrade parity candidate **verified PINNED, not a gap** (`migratedStateEqualsTheFreshlyBuiltState` MigrationChainTests.swift:291 from TASK-022, non-circular — two independent constructors must agree as whole `Equatable` values — composed with `singleHopWalkMigratesBelowCurrentGeneration` + `migratedStateRepersistsAtCurrentVersion` + the fresh-path roundtrip matrix), and the coverage floor **MomoKit 88.86 % lines** (llvm-cov TOTAL 691/77; regions 90.77 %, functions 91.30 %; every per-file row + all-77-missed-lines classified with reasons; floor ≥ 80 % MET — **epic AC-6 satisfied**). **Zero new tests written (the honest audit outcome), zero production diff, zero MomoCore diff**; one disclosed test-file edit (pre-existing `try #require(try…)` warning from `3acc54f`, behavior-identical, warning census clean). `swift test` **515/54 green ×2** (delta 0/0 vs baseline `9109457`). Cycle events: orchestrator verification (117 named audit identifiers all resolved to real test funcs; parity test body read personally — non-circular; coverage table reproduced line-for-line; warning provenance verified to `3acc54f`) → REVIEW-TASK-024 **APPROVED_WITH_MINOR_NOTES** (0 MAJOR / 1 MINOR / 3 OBSERVATIONs; reviewer re-derived BOTH tables from 05 §10.4 + project.md §32 BEFORE comparing — no clause missed, no unpinned mapping; coverage reproduced digit-for-digit; mutation bit `WatchSyncGate.swift:68` `>` → `>=` and the exact named test `boundarySeqEqualsWatermarkIsNoOp` failed with 11/11 others green — executable-truth proof; restore sha256-proven, re-verified by the orchestrator). Disposition §8: **MINOR-1 applied** (both NFR-7 pin-verification sentences narrowed to the POPULATED fixture fields — the parity fixture carries `processedIntents: []` / `pendingHandshake: nil` / `lastGreeting: nil`, so the walk has no teeth over them; diagnosis reproduced personally against StoreFixture.swift:210–215 before applying); OBS-1 routed (below); OBS-3 recorded as a standing obligation (below); OBS-2 confirmed.

- **TASK-023 — Sync DTOs + intent journal + watermark arithmetic (`9109457`, pushed; EPIC-005 3/4).** The sync layer's three pure halves executable, transport-free (EPIC-008 owns wrappers): `WatchSnapshot`/`IntentEvent` versioned DTOs (§6.2 sketch verbatim, WRAPPING `InteractionIntent`/`[QuestProgress]` — no MomoCore invention; hand-written Codable + field-wise `IntentEvent.==` DISCLOSED per Requirement 1 — MomoCore members aren't Codable/Equatable and the freeze held, `Sources/MomoCore/` diff 0 bytes verified at every checkpoint; exhaustive case-name maps make a new MomoCore case a BUILD ERROR; equality-gated `decoded(from:)` → nil/skip), `IntentJournal` (NDJSON one canonical line per event; torn trailing line skipped never fatal + seal-before-append so a tear can't glue onto the next event; per-event epoch (the §6.2 sketch's own field — no second format to version); epoch-matched `watchSeq ≤ watermark` prune as a pure core + atomic rewrite; empty survivors → file removal with the exists-guard), `SyncState` (per-epoch watermark table; THE single 0-init accessor `watermark(for:)`; `recordingApplied` max-semantics so out-of-order recording can't regress the gate; `consumingSnapshotSeq` strictly monotone per file lifetime — reset is a documented display no-op, seq never gates; `shouldApply` wrapper so no call site resolves the wrong epoch's watermark), `WatchSyncGate` (UUID-first + strict `>` — the §6.4 cell matrix in the header), `WatchSnapshotBuilder` (ambient-free; watermark pair coherent from the ONE epoch input), `SyncStateStore` (plain-JSON regenerable bookkeeping, garbled→fresh, **replace-when-present/move-when-absent commit point**), 6 authority-labeled StoreRules constants. **Cycle events:** orchestrator verification caught 1 MAJOR the green suite missed — `SyncStateStore.save`'s plain `moveItem` fails Code-516 over an existing destination (first save fine; every later save crashed DEBUG via the DEBUG-loud trap and would have frozen the watermark file in release; SnapshotStore is immune only because generation rotation vacates the name; the contract's re-persistence pin, as written, only exercised a first save). §11 fix-loop round 1: replace-when-present/move-when-absent + 2 structural pins (`secondSaveOverExistingFileLands` — a DEBUG crash-trap if the defect returns; `secondSaveLeavesNoTempBehind`) + 2 latent var→let fixes. `swift test` **515/54 green** (baseline 451/49; +64/+5). REVIEW-TASK-023 **APPROVED_WITH_MINOR_NOTES** (0 MAJOR/0 MINOR/1 NITPICK/5 OBSERVATIONs; derivation-BEFORE-code re-derivation with zero divergence; 4/4 sanctioned mutations bit incl. the fix-loop revert crashing on the exact Code-516; restores sha256-proven, re-verified independently by the orchestrator). Disposition §8: NITPICK-1 fixed with a diagnosis refinement (bare DEBUG-loud would false-positive on the pinned absent-journal case → exists-guard; 515/54 re-verified post-fix), O2 ACCEPTED as the doc-faithful INV-10 deviation (exactly-once under the FIFO contract stream / at-most-once under seeded shuffles — full-application-under-shuffle would pin a falsehood against §6.4's `>` formula; contract letter over-broad, doc wins), O1+O5 routed to EPIC-008, O3/O4 accepted as documented posture.

- **TASK-022 — Migration chain + retention/pruning (`3acc54f`, pushed; EPIC-005 2/4).** 05 §5.4 + §5.5 executable. `LedgerRetention` — the pure/total/deterministic prune: 7 newest dayKeys (`StoreRules.retainedDayCount = 7`, "05 §5.4" authority label + raw-literal pin; DayKey's zero-padded `"YYYY-MM-DD"` makes lexicographic == chronological, format-pinned; Set-FIRST distinct-key selection so a duplicate can never crowd a distinct day out of the window, first-occurrence collapse, survivor order preserved) + the last-64 intent belt REUSING MomoCore's `processedIntentsCapacity` (zero new literals in MomoKit). Applied on the SAVE path (on-disk payloads always satisfy the caps; read path never prunes — loads-never-write intact); engine stays append-only (the REVIEW-TASK-015 routing is discharged). `MigrationChain` — injected VALUE-DATA chain of `{from, apply}` steps (to = from+1 derived; nil on missing hop or above head; first-declared-wins on duplicate `from`); production ships `MigrationChain.empty` — **no schemaVersion bump** (additive evolution is the norm; schema 1 correct as shipped; raw `== 1` pin intact). SnapshotStore amended: gate order decode → version RANGE check → checksum over disk bytes → payload decode → migration walk → serve (checksum precedes migration BY CONSTRUCTION — a corrupt below-current generation falls through, never migrates, pinned by name); prune-before-encode keeps the single `clock.now()` so TASK-021's savedAt-adjacency pins hold untouched; re-persistence pin (load-migrated → save → load == same state at current version). NFR-7 fresh-vs-upgrade parity pinned. All three TASK-021 routings landed: **OBS-1** golden-bytes pin (1827-byte generation recorded OUT-OF-PROCESS via the production save path, literal in the test with separately pinned hex + in-test re-derivation, length-preserving tamper proves the gate refuses; documented maintenance obligation: canonical-byte drift breaks it BY DESIGN — re-record, don't loosen), **OBS-5** unknown-version tests rewritten against the chain (versions as current+delta; above-head = the chain's rule, not a special case), **OBS-3** documented limit (toolchain-canonical Codable bytes; a future toolchain byte change fails every generation's checksum → fresh default; self-consistent, stated in the SnapshotStore header). `swift test` **451/49 green** (baseline 417/46; +14 retention / +16 migration / +3 golden / +1 constants pin). REVIEW-TASK-022 **APPROVED_WITH_MINOR_NOTES** (0 MAJOR; retention arithmetic re-derived BEFORE comparing; sanctioned mutations A/B/C — version 1→2 → 10 designed bites, gate-order swap → the named corrupt-checksum pin, retainedDayCount 7→6 → raw + format pins — all hash-proven restored; MomoCore diff 0 bytes throughout). Disposition pre-commit: MINOR-1 dead fixture deleted; NITPICK-2 unused Foundation imports removed; **NITPICK-1 resolved with a diagnosis correction — the reviewer had the Set-ordering backwards**: the shipped selection was Set-LAST (dedup after the window), so the documented no-crowding claim was false; reordered to Set-first, comment fixed, and the duplicate-dayKey defect pin strengthened with the boundary case (8 distinct + dup of newest → exactly 7 retained), bite-proven LIVE (fails under the shipped ordering; `554e342b…` hash-proven across the mutation). Multi-hop walk honestly pinned at unit level only (store-level untestable while v1 is pinned — disclosed).

- **TASK-021 — SnapshotStore: envelope, atomic writes, generational recovery (`5cca031`, pushed; + routed-warning chore `0ca305f`; first EPIC-005 task).** MomoKit is real. `SnapshotStore`: actor saves (total-ordered) with `nonisolated` loads so the atomic-rename race is genuinely exercised; envelope `{schemaVersion=1, savedAt (injected clock), checksum = lowercase-hex `MomoCore.SHA256` over the payload's `.sortedKeys` JSON — `.sortedKeys` REQUIRED (unsorted encoder output is non-canonical even within one process), payload = `EngineState`}` (envelope itself `.sortedKeys`; Date on disk = Foundation default `.deferredToDate`, a JSON number); write = 5-step destination-free demotion (rm prev2 → prev→prev2 → current→prev → write tmp → tmp→current atomic commit point) with the crash-window table in the header, DEBUG-loud failures, survivors untouched; read = current → prev → prev2 → injected fallback, never throws, gate order decode → schemaVersion → checksum → payload, unknown version = generation skipped. `StoreRules` constants home (authority labels + raw-literal pins; `defaultDirectory()` = the one sanctioned ambient-path site). Discipline scans (no ambient time, no ambient paths, Foundation+MomoCore imports only) non-vacuous both directions. Disclosed enabler landed as contracted: 16-type additive Codable closure — 15 synthesized + `DayRecord` hand-written (its `Set<QuestFamily>` bytes are non-canonical — per-process hash-seeded AND per-encode-call variance — without declaration-order `persistenceOrder`, exhaustive no-default switch). `swift test` **417/46 green** (baseline 369/43). Review chain: REVIEW-TASK-021 **APPROVED_WITH_MINOR_NOTES** (0 MAJOR; recipe re-derived + cross-process-verified over 4 fresh processes; 11 unnamed crash-window intermediates via the public API; 2 sanctioned mutations with exact attribution) → post-disposition **suite flake (~14 %)** root-caused to the TEST, not the store (old pins assumed actor FIFO issue order; execution order is an arbitrary permutation σ) → §11 two-round fix loop, test-file only, production byte-identical to pristine hashes throughout: round 1 order-independent pins → delta verification round 1 **CHANGES_REQUIRED** (MAJOR: pins missed Requirement 4's convergence clause — an executable freeze counterexample passes them; the silent early-return shape at `SnapshotStore.swift:148-154` is real) → round 2 savedAt **adjacency** pins (`TickingClock` 1 s/locked read, exactly one read per save: 24 saves → base+23/22/21; 128 → base+127; subsumes descent, fails ANY frozen/dropping store — the verifier's executable freeze mutation killed exactly the weak pins) → **round 2 APPROVED**. Stability at close: fixer 20/20 focused + 3/3 full, verifier 16/16 + 3/3, orchestrator reproduced twice. Dispositions: both review MINORs doc-only (applied); OBS-1 (cross-process/golden-bytes load pin) + OBS-5 (unknown-version tests ride the migrate chain rewrite) routed to TASK-022's contract; OBS-3 (toolchain-coupled canonical bytes = documented limit) rides TASK-022; OBS-2/4 recorded; EngineClockTests var→let warning FIXED in `0ca305f`; toolchain `ld` path is machine-level.
- **TASK-020 — §10.3 test matrix + meta-determinism + coverage floor (`0825c97`, pushed; EPIC-004 closer).** The §10.3 domain-matrix audit is complete: every row and every named clause of the six rows resolved to named green tests (the gap table lives in the completed task file — ~130 named tests existence- and body-verified by the reviewer), with 4 clauses that had no literal home closed by new named tests: `couplingSelfReversesOnEnergyRecovery` (FR-9 AC-3's verbatim "self-reverses" clause — mood inflects back up at the attractor τ rate), `midnightRolloverExactlyOnceAcrossFoldedSpans` (the exactly-once crossing pinned via ALL THREE fold entry points: evaluate, interaction, report), `absentDayLedgerIsEmpty` (FR-12 AC-1's emptiness semantics: six absent dayKeys → zero records, landing day full-reset), `monotonicUnderReplay` (FR-10 AC-2: SAME-id re-delivery across midnight is a whole-state no-op). `MetaDeterminismTests` — the §10.2 property through the production `reduce`: 6 seeds × 80-step mixed sequences spanning ≥ 3 local midnights + every night window by schedule arithmetic (80 × 57–114 min ≥ 76 h), full step surface (5 interaction kinds, evaluates, quest-award mechanism calls, kind-matched reports incl. cancellations), WHOLE-trajectory twin equality (every intermediate `EngineOutcome` compared), teeth-proven (`differentSeedsDiverge` + the reviewer's schedule-perturbation bite attributing first divergence to a named step 41–46 per seed). Coverage floor MET and recorded: **MomoCore 97.68 % lines** (94.91 % regions, 94.90 % functions) via llvm-cov, per-file table verbatim in the task file, every sub-floor file's residual lines explained (DEBUG-loud guards, the EPIC-007 haptic seam). **Coverage bimodality note (REVIEW-TASK-020 MINOR-1):** the llvm-cov reading of QuestTick.swift 82–85 — the logically-unreachable `assertionFailure` guard — is nondeterministic across runs (a profiling-counter artifact renders ~10^19 on those lines in some fresh instrumented runs, reading 97.92 % vs the recorded 97.68 %); the ≥ 90 % floor holds under BOTH readings; 97.68 % is recorded as the conservative, reproducible choice. +6 tests / +1 suite. REVIEW-TASK-020 **APPROVED_WITH_MINOR_NOTES** (0 MAJOR; matrix re-derived before comparing with no clause missed; both contract bites landed with exact attribution). Disposition: MINOR-1 bookkeeping (this note), MINOR-2/NITPICK/OBS-A–C recorded with no test change required. Post-disposition **369/43 green**.
- **TASK-019 — Display read-models + copy-key selection (`24e1a03`, pushed).** 05 §4.9–§4.11 fully executable. `DisplayState` per the §4.11 sketch (petName, moodWordKey, energyPhraseKey, bondStage, bondDescriptorKey, questLine, wakefulness, greeting) + `makeDisplayState(_:at:calendar:)` — absent day record → cascade over the empty set → `.allDone` (recorded reading); `makeCharacterDisplayState(_)` over state alone, `momentRequest` = the greeting stamp's kind. `CopyRules` = the §4.9 constants home (`copyEpoch = 1`, 0 = placeholder marker + epoch-bump obligation; slot cut-offs 12/18 engine-owned; pool counts = 1 placeholder each) + `LineSelection` (day-stable `momo.line.react.<family>.<nn>` via `DaySeed.make(…, salt: .copy)` + the PINNED one-draw `%` recipe, filling TASK-016's nil seam at all 21 plan-mint sites; D11-exact night 22:00–07:00 referenced from `FoldRules`, no `q6WindowEndHour` aliasing) + `VocabularyKeys` (OBS-1 fixed lookup under the dedicated `momo.line.vocab.<field>.<band>` keyspace — Wistful key band-named with the "quiet" remap pinned by name; four PRD §3.3 descriptors + four 04 §3.5 energy phrases verbatim-pinned as the EPIC-007 catalog obligation). Greeting selector (evaluate-only, PRE-stamp read): 5-min floor → `.missedYou` ≥ 36 h (outranks hour AND dayKey) → `.nightGlance` (night window, outranks dayKey) → `.freshMorning` → `.welcomeBack`; emits `.greeting(kind)` FIRST and stamps `EngineState.lastGreeting` (the only L4 moment that outlives its event — necessity independently proven). +49 tests / +6 suites incl. the `BondLedgerPropertyTests` ≤ 3-moment re-pin (greeting evaluate-only, FIRST). REVIEW-TASK-019 **APPROVED_WITH_MINOR_NOTES** (0 MAJOR — ten adversarial targets all survived, four sanctioned mutation bites with exact attribution, license (a)–(e) audit clean). Disposition pre-commit: MINOR-1 `catalogObligationsAreRecorded` EPIC-007 pin; MINOR-2 ReactionKeys seam-header fix (orchestrator-authorized doc touch-up); NITPICK-1 daytime missedYou pin; OBS-A/B carried into the EPIC-006 epic file, OBS-C/D standing doc-chain notes. Post-fix **363/42 green**.
- TASK-001 — Repository & orchestration bootstrap (`557c936`)
- TASK-002 — Step 1 Product Review, D1–D20 + E1–E4 (`f72b78b`)
- TASK-003 — Step 2 MVP PRD (`c766ffe`)
- TASK-004 — Step 3 UX Architecture (`40c4b77`)
- TASK-005 — Step 4 Character System (`ce84811`, record fix `39d6bab`)
- E2 — Character direction C "Round Rabbit" (ADR-001; `2cfba30`)
- TASK-006 — Step 5 Technical Architecture + ADR-002…007 (`8fb9653`)
- Owner decisions OPEN-1 (§5.5 cascade rule) + I-2/OPEN-5 (nibble) — PRD amended (`95e7649`)
- TASK-007 — Step 6 Delivery Plan (8 epics / 43 tasks) (`71a510b`)
- **TASK-008 — Toolchain verified + ADR-008 bootstrap pins (`172bc11`, first commit on `feature/EPIC-002-foundation`, pushed).** Review chain: REVIEW-TASK-008 CHANGES_REQUIRED (MAJOR-1: smallest-iPhone mis-pin) → fresh fixer re-pinned iPhone-small to **iPhone SE (3rd generation)** → REVIEW-TASK-008-VERIFY: FIXED — CLEARED FOR COMMIT (three independent probe pairs agree).
- **TASK-009 — Swift package + app targets with placeholder shells (`b28ccb4`, pushed).** MomoCore (Foundation-only) / MomoCharacter / MomoKit, zero external deps; Momo 3-tab shell + MomoWatch glance launch on pinned sims (750×1334 / 324×394 exact); `swift test` green (3 empty Swift Testing suites); swift-test-hostability VERIFY item RESOLVED. REVIEW-TASK-009 **APPROVED** (0/0/3; nitpicks dispositioned in the review record).
- **TASK-010 — Test-target scaffolding + import-whitelist + banned-vocabulary scans (`a9b9993`, pushed).** Five-target architecture per 05 §10.1 (3 package suites + MomoUITests + MomoWatchUITests); scheme TestActions added; both scanners shipped as pure fixture-tested functions (import whitelist = Foundation-only documented; banned list = 04-character-system §10.2 verbatim, byte-verified, vacuously green until TASK-011's catalogs); self-tests caught 2 real defects during implementation; coverage wired (llvm-cov; xccov can't read SwiftPM profdata). All verifications green: swift test 20/5 suites; both xcodebuild test runs TEST SUCCEEDED on pinned sims. REVIEW-TASK-010 **APPROVED** (0/0/2, both mechanical — disposition in review file).
- **TASK-011 — Design-system token pass + String Catalog scaffolding (`a2a36b7`, pushed).** Token module in `MomoCharacter` (8 §8.4 slots verbatim + §18 UI tokens; light/dark compile-enforced; hex confined to the 2 palette files, scan-tested); `Apps/Shared/MomoCopy.xcstrings` in both app targets; type-safe `CopyKey`/`MomoCopy` lookup (bundle-injected, NUL-sentinel missing-key, DEBUG-loud resolve); both shells token-fed and pixel-verified exact (incl. dark); contrast recorded, all 12 pairs reviewer-recomputed exact; banned-vocab scan green and proven non-vacuous both directions. One real defect found+fixed in verification (watch canvas 32 pt strip). swift test 35/9; both builds + both UI smoke tests green. REVIEW-TASK-011 **APPROVED_WITH_MINOR_NOTES** (0/1/2 — disposition in review + task file).
- **TASK-012 — MomoCore domain model (`bc95cb9`, pushed; first EPIC-003 task).** All 05 §3.1 value types + all five 04 §9.2 interface types in `Sources/MomoCore/` (11 files): `Sendable`, `let`-immutable (zero `var` in module), Swift 6 strict-concurrency clean. Pure derivations `makeMoodBand`/`makeEnergyBand`/`makeBondStage` with every PRD number single-sourced in `Thresholds.swift` (bands 20/45/75; bond stages 150/400/750 first-value form = PRD's 0–149/150–399/400–749/750–1000; quest windows 12/20/7). `DayKey.make(from:calendar:)` injected-calendar-only (D20, DST + non-Gregorian tested). INV-1…11 dispositioned (type/representation-enforced where possible; engine/store halves honestly deferred to EPIC-004/005). Placeholder pair removed (self-documented EPIC-003 condition). swift test **64 tests / 11 suites green**; D-R1 + banned-vocab scans green. REVIEW-TASK-012 **APPROVED_WITH_MINOR_NOTES** (0/1/3 — MINOR-1 wording + NITPICK-3 doc note fixed; NITPICK-1/2 declined; disposition in review file). Contract commit `f5c1c11` recorded the owner merge rule in CLAUDE.md §14.
- **TASK-013 — Exhaustive domain-model property tests (`5bbdd72`, pushed; EPIC-003 complete).** 1203 parameterized cases: full-range mood/energy sweeps (0…100 ×2) + bond-stage sweep (0…1000) with every boundary; `ThresholdsPinnedToPRDTests` (PRD-literal pins + tiling + edge mappings — anti-echo proven by mutation probe: silent constant 45→46 fails pins 4/4 while constant-fed sweeps stay green); `NoNumericLeakageTests` (4-layer check incl. compile-time case-set pins via default-free exhaustive switches). Review chain: REVIEW-TASK-013 **CHANGES_REQUIRED** (MAJOR-1: `case historic(Int)` evaded the leakage suite — case sets unpinned) → fresh fix agent (test-file-only) → same reviewer's verification: Probe D re-run fails the build (`switch must be exhaustive`) — **REVISED VERDICT APPROVED_WITH_MINOR_NOTES**, commit-ready. Final: `swift test` **80 tests / 14 suites green**; coverage informational: Bands.swift 100 %, TOTAL 92.06 % lines. NITPICK-1 citation fixes applied at contract source (task file + epic AC-5).
- **TASK-016 — Interaction semantics + satiety + repetition curve (`f61093a`, pushed).** The PRD §4 matrix is executable: `InteractionSemantics` derives one `ResponsePlan` + new state per intent — warm in every cell (D18, INV-6: no mood-loss path exists), beat keyed by band, effect by satiety class (reviewer-adjudicated harmonization; PRD errata note recorded). Counting honesty I-1 (feed always incl. refusal/asleep-decline; play on round completion ONLY; pat always; care when performed; expired-dayKey keeps effects, drops ALL ledger attribution). Satiety half-open 30/90 derivation at every fold instant (`full` 0–30 / `recentlyFed` 30–90 / `hungry` >90, nil → hungry); repetition curve 1.0/0.6/0.25/0.0 per family per local day, read before increment, care exempt; ceiling 92 clamps the GAIN (`min(mood+gain, max(mood, 92))` — never lowers). Seeded 2-draw `.play` token; **unified cease** (`playRoundFinished` ∨ `handshakeCancelled(.play)` → one `completePlayRound`, effects + count exactly once — §9.6 item 4 discharged, TASK-006 answered **confirm**); **§9.6 item 8 discharged** — during `.settling` everything declines warm, nothing queues (single-slot structural argument recorded in the `InteractionSemantics` header, pinned across 5 cells); waking choreography declines play/tuck-in (the never-cancelled `.wake` token is preserved) while pat/feed/nap apply; 20:00-local tuck-in gate (DST/:30-offset proven); nap = fold-completed (`activity = .napping`, +20 at the next fold, lands `.waking`/`.asleep`). New: `InteractionRules` (constants home, authority labels), `InteractionEffects`, `InteractionSemantics`, `ReactionKeys` (§8.4 keys; lineKey/haptic seams nil — TASK-019/presentation); `ReactionKeys` extension-header enumerates all eight non-table keys for the EPIC-006 catalog manifest. 71 new tests / 6 new suites + `InteractionFixture`. REVIEW-TASK-016 **APPROVED_WITH_MINOR_NOTES** (0 MAJOR — independent re-derivation of every number, scanner bite re-proven, six adversarial probes). Disposition pre-commit: MINOR-1 fixed (zero-elapsed `wakeFinished` preserved `activity` on `.wake` completion + regression pin `wakeFinishedAfterMidWakingNapPreservesNap`); NITPICK-1 fixed. Post-fix **236/29 green**, zero warnings.
- **TASK-017 — Bond ledger, variety bonus, stage moments (`4a9170c`, pushed).** Bond is executable per 05 §4.6 / PRD §3.3: `BondLedger.award` is the ONLY bond writer in the engine — clamp-at-award `min(event, dailyBondCap − bondAwarded, 1000 − bond)` with the day ledger recording the ACTUALLY-applied amount, making INV-5 (per-dayKey ≤ +20), INV-3 (monotonic), and the 1000 plateau structural. Hello +8 on the day's first pat (device-agnostic — the ledger never reads `intent.source`; never window-gated — 14:00/23:50 pinned; idempotent via `helloAwarded`); quest +4 as mechanism-only (`awardQuestCompletion` — detection/ticking/moments are TASK-018's); variety +6 fires at the trio-completing event (feed/play/care recorded on EXACTLY the I-1 counting events: every feed intent incl. refusals, the unified play cease, settle-auth/blanket-adjust/nap-accept); `.bondStageReached` emitted exactly once per crossing under the `highestCelebratedStage` once-guard on all three event paths (UX-10 closed-period deferral). `BondRules` = constants home; `Thresholds` keeps thresholds/range (cap pinned equal across homes per house discipline). New: `BondRules`, `BondLedger`; `EngineEvent.swift` moments/response doc-comments corrected. 37 new tests / 3 new suites + property suite (cap-by-construction over seeded randomized sequences, monotonicity across midnight, whole-trajectory twin equality, per-step ledger exactness). REVIEW-TASK-017 **APPROVED_WITH_MINOR_NOTES** (0 MAJOR — every clause re-derived before comparing, 16 adversarial probes, both scanner bites proven with exact attribution). Disposition pre-commit: MINOR-1 (dead-code build warning + two false prose lines) fixed; NITPICK-2/3/4 prose/comment touches rode the commit. Post-fix **273/32 green**.
- **TASK-018 — Quest generator + completion windows + Watch cascade (`d4820a7`, pushed).** 05 §4.8 fully executable. `QuestGeneration.generate` — candidate pool = the 15 unordered pairs of {Q2…Q7} filtered BEFORE drawing (consecutive-pair ban by SET equality, then the Q6 3-day restriction under the STRONG credit reading adjudicated by REVIEW-TASK-018: credit iff BOTH known priors carry Q6; unknown priors force Q6, so fresh-install day 1 always contains Q6; tightest pool == 4 pairs, exhaustively pinned over all 225 prior configurations) — then exactly two seeded draws of a per-call `SeededGenerator` (the fold's choreography-rng lineage untouched, `twoDrawPin`). Wired into `TimeFold.rollover` (petID threaded through `apply`, priors NEWEST FIRST, exactly-once under backward clocks, never regenerates a past day, `QuestGeneration.currentEpoch = 1` stamped). Window-checked completion detection (`QuestTick.tick`) rides all four TASK-016 counting sites AFTER counter + family record: qualifying-only progress (the window gates the tick — Q1 post-noon pats and daytime care tick nothing), the event's OWN timestamp hour (offline-pat 11:30-applied-12:30 ticks Q1 — pinned verbatim), event-dayKey attribution (D20 00:30 tuck-in → the new day's Q6), expired-dayKey no-op, bare `.questCompleted` + `awardQuestCompletion` (+4 through the clamp), multiples looped (a pat CAN complete Q1+Q7 on one event — reviewer probe through the full production path; `doubleCompletionLoops` is the live coverage). Six-rule §5.5 cascade (owner-amended rule 1: ≥ 20:00 ∨ < 07:00) as one shared pure `QuestGeneration.cascade(questSet:localHour:) -> QuestLine` (`.wish`/`.allDone` enum), rules 3–4 in-set-scoped, the named test "a 02:00 tuck-in with Q1 done selects Q6" green. +41 tests / +4 suites. REVIEW-TASK-018 **APPROVED_WITH_MINOR_NOTES** (0 MAJOR; both open questions adjudicated independently, 5 seeded mutations bit with exact attribution incl. the two-draw pin and the tightest-case sweep). Disposition pre-commit: MINOR-1 `BondLedgerPropertyTests` moment re-pin amended to ≤ 3 moments / ≤ 2 `.questCompleted` / ≤ 1 `.bondStageReached` last (the ≤ 1-completion contract premise was disproven and corrected); NITPICK-1 uniformity doc scoped to the two star-shaped scenarios; NITPICK-2 bookkeeping line. Post-fix **314/36 green**.
- **TASK-015 — Time-fold catch-up + wakefulness machine + handshakes (`7b5d735`, pushed).** Real time semantics replace TASK-014's bookkeeping honesty: segment-fold per §4.3 (waking decline −1.5/h; night ramp anchored to the containing night's FULL real duration — DST-correct, proven necessary by the reviewer: endpoint anchoring makes the PRD ≥ 75 wake clamp vacuous; wake clamp; attractor τ=3 h target 60; coupling 35/floor 25 at SEGMENT-START energy), dayKey-keyed once-only rollover (landing-day-only adjudicated CORRECT vs FR-11/FR-12), harmless absence (7-day equilibrium re-derived from scratch), §4.7 wakefulness machine under INV-8 (night onset lands `.asleep` handshake-free with zero rng draws; morning lands `.waking`, wake-stretch mint deferred to the next event, exactly 2 draws), idempotent kind-matched handshakes (04 §9.2's token-less reports; stale reject + un-strand; settle preemption → `.awake`), calendar injected as documented third parameter (determinism tuple: state, event, clock, calendar, seed). New: `FoldRules` (constants home with PRD-normative/engine-owned authority labels), `TimeFold`, `HandshakeMachine`; `Reduce` rewritten; `CharacterReport` +`wakeFinished` (disclosed, both normative sources cite it). Named tests: 22:00 onset, 07:00 wake + clamp (exact 74.625), midnight ×1, DST fall-back 36000 s / spring-forward 28800 s (OS-verified), timezone change ≤ 1 rollover, backward clock no-double, 7-day absence, attractor exact `60 − 30/e`, coupling/floor exact. swift test **163/23 green** at handoff. REVIEW-TASK-015 **APPROVED_WITH_MINOR_NOTES** (0 MAJOR; strongest signal: a probe where the reviewer's adversarial expectation was wrong and the code was right). Disposition applied pre-commit: MINOR-1 code restored to the documented segment-start coupling + the reviewer's probe adopted now as the discriminating pin `couplingBandReadsSegmentStart`; MINOR-2 implemented (§4.7 settle-preemption edge → `.awake`); NITPICK-1 commented for TASK-016, NITPICK-2/3 fixed, NITPICK-4 coverage on record (TOTAL 97.77 % lines). Post-fix **164/23 green**.
- **TASK-014 — Engine core: reduce, EngineClock, seeded RNG, day-stable seeds (`696d8dd`, pushed; first EPIC-004 task).** 7 new `MomoCore` sources: repo-owned FIPS 180-4 SHA-256 (zero imports — D-R1 bans CryptoKit; NIST vectors OS-verified), SplitMix64 `RandomNumberGenerator` (canonical vectors; seed-9 constant computed and corrected before landing), `DaySeed` (length-framed injective preimage petID‖dayKey‖epoch‖salt, SHA-256 truncated first-8-BE; golden-byte + round-trip pins), `EngineState`/`EngineEvent`/`EngineOutcome` per §4.1 (all-`let`, `Equatable`), `EngineClock` protocol + `SystemEngineClock` (the one sanctioned ambient `Date.now`, scanner-exempted) + value-semantic `ManualEngineClock`, and pure `reduce` with honest bookkeeping-only semantics (owner tasks documented; clock/rng proven unread by probes). VERIFY item resolved: stdlib `Clock` rejected (monotonic instants can't see §4.3 wall-clock changes; `Date(ContinuousClock.now)` compile-fails). Engine-purity scan over all of `Sources/MomoCore` proven non-vacuous both directions (seeded violation RED, restored). swift test **125/20 green**; coverage 7 new files 100 % lines, TOTAL 97.12 %. REVIEW-TASK-014 **APPROVED_WITH_MINOR_NOTES** (0 MAJOR — every crypto/arithmetic constant independently re-derived by the reviewer: shasum 8/8 + 12/12 unseen, SplitMix64 9/9 from-scratch, preimage hand-built byte-identical). Disposition applied: MINOR-1 narrowed (exact frozen hex counts 72/3; full allowlist declined as redundant with the NIST/canonical vector pins), MINOR-2 occurrence-pinned (exactly one `Date` in EngineClock.swift), NITPICK-1/2 fixed (capacity==64 pin; distinct fixture intent id). Post-fix **126/20 green**.

## Work In Progress
- **TASK-046 — §10.5 edge-case matrix execution (05 §10.5) — EPIC-009's second task.**
  - Status: **IN PROGRESS** — fresh Jupiter implementer (`task046-impl`) dispatched 2026-09-12 and running (R0 audit table + R1 gap-closing + R3 non-vacuity complete per its own tracker; §19 gates mid-run at last report). Contract: `.claude/tasks/active/TASK-046-edge-matrix.md` (committed `1f2d350`, pushed).
  - Branch: `feature/EPIC-009-release-readiness` (tip `1f2d350`; the TASK-047 contract + this status edit land as `(this commit)` below).
  - Scope: R0 body-level audit table over all ten §10.5 rows (TASK-044 R0 precedent — name-precise, body-verified pointers; ALREADY-COVERED / GAP / N/A-PHASE-1 verdicts), then close ONLY the real gaps. Candidate gaps named in the contract (hypotheses to verify, not verdicts): Watch-unavailable leg (no named fire-and-forget test found), offline UI smoke, fresh-install Enter-atomic-write leg, rollover catch-up path. Contract-time census: the Core fold rows are heavily covered by name (TimeFoldTests :552/:176/:209/:386/:410/:469/:504; GreetingSelectionTests :39/:54) and the upgrade row by MigrationChainTests incl. NFR-7 parity :290.
  - Discipline: zero production-source changes expected (Tests/ are not frozen); stop-on-failure against production behavior (R4 — a frozen-surface bug is escalated, never opportunistically fixed); §25 surface honesty; disk discipline (watch armed at 200 MiB floor; NOTE — the pressure EASED mid-run: ~5.4 GiB free at 08:03 UTC, see Known Issues).
  - Honest recording: owner items remain OPEN and non-blocking — the D2/F-1 W1 all-done copy ruling, the TASK-045 device-pass rows added to the owner's backlog, and the disk-pressure situation (owner notified).
- **TASK-047 — Full accessibility audit, launch-blocking (03 §10; FR-20 AC-2; NFR-6) — contract authored READY; dispatch AFTER the TASK-046 cycle closes.**
  - Contract: `.claude/tasks/active/TASK-047-a11y-audit.md` (NEW, this commit). Normative sources re-read at source before authoring: 03 §10 (:425-443 per-screen table + global commitments + the audit-scope sentence), 04 §3.5 (the binding VoiceOver formula), the epic TASK-047 row.
  - Contract-time census (starting pointers, VERIFY AT BODY LEVEL): the 8-test `MomoAccessibilityAuditUITests` surface suite + `testStatusRowComposesTheStateInWordsTemplate` (:242), `MomoUIColorContrastTests` (4 body-text pairs), `MomoReduceMotionTwinTests` (engine half), `GlanceView.swift:72-82` composite VoiceOver + :145 pat label, `MomoWatchUITests` 44-pt fixture asserts.
  - Candidate real gaps the contract names as hypotheses (R0 must verify, not trust): per-family interaction + quest-completion a11y legs; W1 composite VERBATIM vs the 03 §10 formula; Dynamic Type AC-1a/AC-1b (likely the largest gap); UI-level Reduce Motion toggle leg; VoiceOver ordering; the REVIEW-TASK-039 N-2 ride-along (44-pt assert on quest rows — contracted regardless).
  - Hard constraints: audit pins encode the SHIPPED strings (the REVIEW-TASK-039 F-2 template delta stays owner-gated — never "fixed" in-task; catalog frozen); frozen surfaces stay diff-EMPTY (a frozen-surface a11y defect is BLOCKED-pending-adjudication, escalated — Watch-side `Apps/MomoWatch/**` MAY be fixed in-task); launch-blocking verdict requires ZERO open blockers; manual legs honest per §25 (device-requiring legs → owner backlog).
- **TASK-048 — Privacy & security review (05 §11; FR-20 Privacy/AC-1/AC-3/AC-4; NFR-5; CLAUDE.md §27) — contract authored READY; dispatch after the TASK-047 cycle.**
  - Contract: `.claude/tasks/active/TASK-048-privacy-security.md` (NEW, this commit). Normative sources re-read at source: 05 §11 (:641-665), FR-20 Privacy + AC-1/AC-3/AC-4 + NFR-5 (02-mvp-prd.md :373-394).
  - Contract-time census (orchestrator-verified 2026-09-12): ZERO banned-framework imports (StoreKit/Firebase/Mixpanel/Amplitude/AppsFlyer/CoreLocation/HealthKit/AdSupport) across `Apps/`+`Sources/` (residue = 14 comment mentions + 18 `breathAmplitude` false-positives — the word-boundary lesson now contracted); zero `CODE_SIGN_ENTITLEMENTS`; zero `.entitlements` files; zero `UsageDescription` keys; transport twins import exactly Foundation+WatchConnectivity+os.
  - Named real-gap candidates: (a) `PrivacyInfo.xcprivacy` ABSENT vs 05 §11.2 and its landing REQUIRES a pbxproj edit (zero synchronized groups verified) → frozen-surface ⇒ the implementer drafts the manifest + registration recipe as adjudication artifacts and records BLOCKED-pending-adjudication; the ORCHESTRATOR mints the landing task post-review; (b) no named network-traffic audit test exists vs 05 §11.2's EPIC-002 citation (both an AC-1 execution gap and a doc-erratum candidate — recorded, never edited in-task); (c) no standing AC-3 banned-token guard; (d) no view-literal (AC-4) scanner over Apps views; (e) no secrets scan (CLAUDE.md §27). AC-1 runtime leg = structural census + paired-sim `lsof`/`nettop` capture; true-device leg attempt-then-BLOCKED §25 → owner backlog.

## Next Tasks
1. **~~TASK-045~~ DONE (`c43c92b`, pushed) — EPIC-009 1/6. ~~TASK-046 contract authoring + dispatch~~ DONE — contract committed `1f2d350` (pushed `caf6051..1f2d350`) and the fresh Jupiter implementer `task046-impl` IS RUNNING. NEXT: await its §28 handoff → authenticate at source → dispatch the independent `review-task-046` reviewer (§10/§33 — re-derives the §10.5 checklist from 05 BEFORE reading Implementation Notes) → fix loop → atomic commit + push → closeout (mv to completed/, epic 2/6, status refresh). TASK-047's contract is authored READY (`.claude/tasks/active/TASK-047-a11y-audit.md`, this commit) — dispatch it ONLY after the TASK-046 cycle completes (one task = one fresh agent, §9; do not run two implementers against the same test targets/sims). Then TASK-047 → 048 → 049 → 050 in dependency order → §14 merge. Standing epic discipline (recorded in the epic file): §25 attempt-then-BLOCKED for physical-hardware legs; budget values normative, a miss is a task-level blocker; owner-gated legs (E4, TestFlight, store metadata) packaged as artifacts + escalation. Contract-template lesson applied: do NOT ship the literal `HANDOFF-COMPLETE` marker line in contract templates — line-anchored handoff monitors grep `^HANDOFF-COMPLETE TASK-0XX` and embedded template text causes false fires (both the TASK-046 and TASK-047 contracts describe the marker without the literal assembled string).
2. ~~ADR-013 (standalone-watch packaging)~~ **PROMOTED (2026-09-10)** — `.claude/tasks/decisions/ADR-013-standalone-watch-packaging.md`; EPIC-008's sync design inherits it. E4 name clearance gate stands at TASK-050.
3. **Owner items pending (§36, non-blocking):** **NEW (TASK-044 R5): three device-pass obligations, BLOCKED-recorded (§25 — no paired hardware in this environment; `xctrace list devices`: exactly ONE physical device, iPhone-class): (a) WC frame-delivery-under-suspension verified on PAIRED HARDWARE (05 §10.4's device obligation row — simulators carry development, the device session is the explicit deliverable); (b) the live AOD observation on capable hardware (TASK-041's O4 live leg — SE has no AOD); (c) the haptic FEEL judgment on-wrist (ADR-015 D3 — WKInterfaceDevice feel is not simulator-observable).** Then **(REVIEW-TASK-043 F-1/D2): the W1 all-done copy ruling** — W1's all-done quest line renders the shared `momo.line.moment.02` "Momo had a lovely day." (the iPhone M3 card's line; the binding shipped in TASK-041 and was inherited) while the corpus normatively assigns W1 **"All done — see you soon"** (PRD §5.5 item 6; UX §6.1 ×2 + §9 Watch column; "see you soon" appears ZERO times in the shipped catalog); the review's ruling is **(B)-refined** — the corpus does NOT sanction superseding the PRD string for W1, and neither fix was available on TASK-043 (frozen `Apps/Momo`; new keys owner-gated). Owner decides: (i) mint a W1 key — the `momo.line.quest.*` namespace exists, `momo.line.quest.all-done` is the natural slot — plus a Watch-side key swap in a future task, or (ii) adopt the shared line everywhere → docs errata to PRD §5.5 item 6 + UX §6.1/§9 instead (note UX §9's iPhone M3 row currently misquotes the PRD string against §5.5 either way). Recorded honestly at the §14 merge: still OPEN, non-blocking per REVIEW-TASK-043. Then **(REVIEW-TASK-039 F-2): the state-in-words template delta** — the shipped stage element reads "{stage}. {descriptor}." without 03 §10's "You two are" carrier (the mood·energy half matches the template; the audit pins encode the SHIPPED strings, so the recorded evidence is what VoiceOver actually reads); owner copy decision needed BEFORE EPIC-008's Watch template work. Then the TASK-029 RM acknowledgment gap (11/15 non-press keys render no visible touch acknowledgment under RM — an authored acknowledge-static is the owner's call, §24), the Content+Energetic static collision (expression design), and **NEW (REVIEW-TASK-033 OBSERVATION-A): greeting domination** — `EngineState.lastGreeting` persists for the day while the ambient slot is a once-per-day draw, so the 40-line ambient pools rarely surface; an ambient-visibility retune (slot rotation, post-morning demotion) is a PRODUCT call (§24) — owner's. **EPIC-007-carried:** §2.4 continuous pupil-follow (TASK-034 seam); haptics delivery at the presentation seam (TASK-036); the naked lost-boundary press touch-cancel seam (FIX1-NOTE-1 → TASK-034); ~~OBS-1 main-thread-I/O property~~ **DISCHARGED in TASK-031** (the launch read is the ONE sanctioned synchronous main-thread I/O — documented structurally in `MomoAppModel.swift`'s type header, review-verified; §12 numbers re-measure at TASK-045).
4. Follow-up candidates (no urgency): **O2 full-tier eye refinement → TASK-025 geometry owner** (authored-geometry causes: flat lid top slicing 26/112 eye units, glint ~57 % of visible eye height, eye–cheek overlap, mouth nearly disappearing at full tier — frozen files, routed not blocking); **NOTE-1** extend `RigDisciplineTests` scanner patterns (`.strokedPath`/`.union`/Shape-init/`Instant.now`/`Date(timeIntervalSinceNow:)` etc.) opportunistically; ~~PRD feed errata + Q6-credit note + NOTE-5/NOTE-6 + OBS-D~~ **DISCHARGED 2026-09-10** (recorded into the doc chain — see the Completed Work bullet); **~~OBS-C~~ DISCHARGED (2026-09-10, TASK-033 closeout):** dated reconciliation note at 04 §10.3 (headers illustrative, engine cut-offs normative) + the 03 §5.5 Q6-early-render errata (OBSERVATION-B) — the doc chain now has NO open items; extend `TokenPurityTests` to component initializers + SwiftUI named colors outside the token module; optionally pin the four body-text contrast pairs with a test luminance helper (full audit remains TASK-047). REVIEW-TASK-014's full exact-literal hex allowlist was **declined** — no follow-up owed. **REVIEW-TASK-039 rides:** N-1 (one-word U+2019 doc-comment fix in the landing pin — next scaffolding touch), N-2 (44-pt glass assert on quest rows — next audit-suite touch), N-4 (the census guard's closed 5-file list — fold into the guard-family hardening cluster with the 037 N-1/038 N-1 precedents); `RigDisciplineTests.swift` at 1032 ln (optional test-file split — the 800 budget is authored for production files). **REVIEW-TASK-043 rides:** F-2/F-3's one-line guard additions folded into TASK-044 (above); F-4 (gesture-bypass spellings: `.gesture(TapGesture()`, `.highPriorityGesture`, `.simultaneousGesture`, `.contextMenu`) + F-5 (the recompute census counts calls, not `liveQuestLine` writers — mitigated by `private(set)`) recorded as guard tolerances for the future guard-family hardening cluster; F-6 (the frozen fallback would silently mask a future nil-coherence break in release — a `debugLoud` tradeoff) recorded, no action. **REVIEW-TASK-044 rides:** F-3 — `WatchSweepScan.swift:136` pins `persister.consumeWipe(` presence but not the `await` spelling (a revert to `Task { await … }` + immediate flush would keep the leg green while widening F-2's window) — one-token tightening at the next guard touch, folded into the guard-family hardening cluster; F-2's consume-suspension window ACCEPTED as an honestly-disclosed residual (narrow, warm-apply class, receiver-side §6.6 semantic — no action). MomoKit coverage trend now 92.47 → 91.48 → 90.79 → 90.81 → **90.84 %** (floor 80 holds).

## Blocked Tasks
- None. Owner gates pending but non-blocking: E1 (monetization) and E3 (location) closed for Phase 1 (D8/D19); E4 (name/trademark clearance) scheduled as TASK-050 release gate.

## Recent Commits
- `(this commit)` — docs(orchestration) — TASK-047 contract READY — full accessibility audit (03 §10; FR-20 AC-2; NFR-6 launch-blocking); status refresh marks TASK-046 IN PROGRESS (resolves the TASK-046 contract commit `1f2d350` + its push range `caf6051..1f2d350`)
- `1f2d350` — docs(orchestration) — TASK-046 contract READY — §10.5 edge-case matrix execution (resolves the TASK-045 closeout commit `caf6051` + its push range `c43c92b..caf6051`)
- `caf6051` — docs(orchestration) — TASK-045 closeout — DONE, EPIC-009 1/6 (record to completed, epic status, status refresh; resolves the TASK-045 docs commit `c43c92b` + its push range `2996fa1..c43c92b`)
- `c43c92b` — docs(qa) — TASK-045 complete: task record + REVIEW-TASK-045 + 25-file evidence set (all ten 05 §12 budget rows; APPROVED_WITH_MINOR_NOTES; F-1 §12-verbatim fix applied at closeout; no production diff)
- `2996fa1` — docs(orchestration) — TASK-045 contract READY + status refresh (resolves the post-merge refresh `244b7ea` + its push range `d460a24..244b7ea`; records the EPIC-009 epic commit `fa1a479`)
- `fa1a479` — docs(orchestration) — EPIC-009 contract READY — Polish, QA & Release Readiness epic (TASK-045…050; resolves the post-merge status refresh `244b7ea` + its push range `d460a24..244b7ea`)
- `244b7ea` — docs(orchestration) — post-merge status refresh — EPIC-008 MERGED to `main` (resolves the TASK-044 closeout placeholders: the closeout docs commit `149f1a8` + its push `491777f..149f1a8`; records the §14 merge `d460a24` + its push `fffa6cf..d460a24`)
- `d460a24` — §14 merge — Merge feature/EPIC-008-watch-sync into main (the remainder after the owner's PR #17 `fffa6cf` through the TASK-041 closeout `2e65019`: TASK-042 contract/feat/closeout + TASK-043 feat/closeout + TASK-044 contract/feat/closeout = 8 commits; merged tree content-identical to the branch tip; 1161/113 green on the merged state BEFORE push)
- `149f1a8` — docs(orchestration) — TASK-044 closeout (completed task record, epic 5/5, status refresh) + the stale-DONE TASK-001 bootstrap record moved to completed/ (resolves the TASK-044 feat `491777f` + its push range `6bc8c40..491777f`)
- `491777f` — TASK-044 — feat(watch): TASK-044 sync-matrix audit, launch-sweep drain, erase reset E2E, clock ban (the amended commit after the git-mv index-blob catch: 17 files +1705/−163 — 9 modified + 6 new implementation/test files + the completed task record + REVIEW-TASK-044)
- `6bc8c40` — docs(orchestration) — TASK-044 contract READY — sync matrix + E2E + launch sweep + guard ride-alongs; status refresh (resolves the TASK-043 closeout `91c523f` + its push range `0bd6044..91c523f`)
- `91c523f` — docs(orchestration) — TASK-043 closeout — review APPROVED_WITH_MINOR_NOTES, §19 gates green, status refresh (resolves the TASK-043 feat `0bd6044` + its push range `898fe09..0bd6044`)
- `0bd6044` — feat(watch): TASK-043 watch cascade — live shared derivation + UX-13 propagation (9 files, +1380/−42; implementation + tests + the REVIEW-TASK-043 record + the task file moved to `completed/`)
- `898fe09` — docs(orchestration) — TASK-042 closeout — status refresh (resolves the TASK-042 feat `faa8761` + its push range `692b5d2..faa8761`, and the TASK-042 contract `692b5d2` + its push range `2e65019..692b5d2`)
- `faa8761` — feat(watch): TASK-042 watch pat — journaled intents, drain, micro-reaction + haptics (19 files, +2190/−66; implementation + tests + the REVIEW-TASK-042 record + ADR-014 wording + the task file moved to `completed/`)
- `692b5d2` — docs(orchestration) — TASK-042 contract READY — watch pat + journaled intents + drain (ADR-015: reaction binding over the frozen clips + completion-haptic estimation; resolves the TASK-041 closeout `2e65019` + its push range `4407544..2e65019`)
- `2e65019` — TASK-041 closeout — docs(orchestration): TASK-041 closeout — review APPROVED_WITH_MINOR_NOTES (F-1/F-2 → TASK-042), §19 gates green, status refresh (resolves the TASK-041 feat `4407544` + its push range `fffbc5f..4407544`)
- `4407544` — TASK-041 — feat(watch): W1 glance + snapshot persistence + AOD — character DTO (ADR-014), WatchSnapshotStore, receive path, settling-in copy (atomic; 29 files, +3096/−88; incl. REVIEW-TASK-041 + the AOD evidence still + the task file moved to completed/ with its Completion Evidence; resolves the TASK-041 contract commit `fffbc5f` + its push range `b1576b7..fffbc5f`)
- `fffbc5f` — docs(orchestration) — TASK-041 contract READY — W1 glance + snapshot persistence + AOD (ADR-014: watch character read-model on the wire; resolved the TASK-040 closeout `b1576b7` + its push range `c5430f3..b1576b7`)
- `b1576b7` — docs(orchestration) — TASK-040 closeout — review cycle complete (CHANGES_REQUIRED → fix → DELTA VERIFIED → APPROVED_WITH_MINOR_NOTES); status + epic refresh (also resolves the TASK-040 contract commit `c5430f3` + its push range `a54be43..c5430f3`)
- `0719129` — TASK-040 — feat(sync): iPhone-side WC session — context push, intent receive, reset-marker leg (atomic; 22 files, +2395/−162; incl. REVIEW-TASK-040 + REVIEW-TASK-040-delta + the task file moved to completed/ with its Completion Evidence)
- `c5430f3` — docs(orchestration) — TASK-040 contract READY — iPhone-side WC session (resolved the EPIC-008 authoring commit `a54be43` + its push range `b73e3eb..a54be43`)
- `a54be43` — docs(orchestration) — EPIC-008 authored — Watch App & Sync READY (TASK-040…044); EPIC-007 merged to `main` (§14) (resolved the TASK-039 closeout hash `8604f5b` + its push range `614053d..8604f5b`, and the §14 merge commit `b73e3eb` + its push `ad0199e..b73e3eb`)
- `b73e3eb` — merge — EPIC-007 iPhone Home Experience (TASK-031–039) into `main` — direct merge per owner rule 2026-09-08 (owner PR #16 `ad0199e` reconciled; remainder 2 commits `--no-ff`; merged state verified 949/97 green, tree content-identical to the branch tip)
- `8604f5b` — docs(orchestration) — TASK-039 closeout — review APPROVED_WITH_MINOR_NOTES, dispositions applied, §19 gates green, status refresh (resolved the TASK-039 contract hash `d49fe9d` + its push range `5ffe634..d49fe9d`; the TASK-039 feat is `614053d`, pushed `d49fe9d..614053d`)
- `614053d` — TASK-039 — test(audit): TASK-039 consolidation + accessibility audit evidence + app-model split (atomic; 19 files, +1473/−375; incl. REVIEW-TASK-039 + the task file with its dispositions)
- `d49fe9d` — docs(orchestration) — TASK-039 contract READY — consolidation + a11y audit (also resolves the TASK-038 closeout hash `5ffe634` + its push range `4b02be7..5ffe634`)
- 5ffe634 — docs(orchestration) — TASK-038 closeout — review APPROVED_WITH_MINOR_NOTES, dispositions applied, §19 gates green, status refresh (also resolves the TASK-038 contract hash `7340cd2` + its push range `3376769..7340cd2`; the TASK-038 feat is `4b02be7`, pushed `7340cd2..4b02be7`)
- `4b02be7` — TASK-038 — feat(settings): TASK-038 settings — rename, haptics, erase-all-data, about (atomic; 18 files, +1794/−65; incl. REVIEW-TASK-038 + the task file with its dispositions)
- `7340cd2` — (TASK-038 contract) — docs(orchestration): TASK-038 contract READY — settings + rename + erase (also resolves the TASK-037 closeout placeholders: closeout hash `3376769`, push range `bcecbed..3376769`)
- 3376769 — docs(orchestration) — TASK-037 closeout — review APPROVED_WITH_MINOR_NOTES, dispositions applied, §19 gates green, status refresh (also resolves the TASK-037 contract hash `2b486b3` + its push range `d790065..2b486b3`; the TASK-037 feat is `bcecbed`, pushed `2b486b3..bcecbed`)
- `bcecbed` — TASK-037 — feat(home): TASK-037 room tab — static scene, single a11y element, D12 tab labels (atomic; 13 files, +632/−46; incl. REVIEW-TASK-037 + the task file with its dispositions)
- `2b486b3` — (TASK-037 contract) — docs(orchestration): TASK-037 contract READY — room tab (also resolves the TASK-036 closeout placeholders: closeout hash `d790065`, push range `3861c7b..d790065`)
- `d790065` — (TASK-036 closeout) — docs(orchestration): review APPROVED_WITH_MINOR_NOTES, dispositions applied, §19 gates green, status refresh
- `3861c7b` — TASK-036 — feat(home): wire quest moments + celebrations — event door, FIFO, banner, M1/M3 surfaces
- `94bf69b` — (TASK-036 contract) — docs(orchestration): TASK-036 contract READY — quest moments + celebrations (resolved the TASK-035 closeout placeholders: feat hash `b8d181a`, push range `3a1698b..b8d181a`)
- `b8d181a` — TASK-035 — feat(home): TASK-035 wire feed/play/care — pools, play round, report drain (atomic; 28 files +1 new test suite + 2 review records; incl. REVIEW-TASK-035 + REVIEW-TASK-035-FIX + the F-1 fix-cycle record + the task file moved to completed/ + the status/epic refresh)
- `3a1698b` — docs(orchestration) — TASK-035 contract READY — feed/play/care (resolves TASK-034's hash `2264f35`; status/epic refresh)
- `2264f35` — TASK-034 — feat(home): TASK-034 wire canvas touch & petting — gestures, director, spoken reactions (atomic; 27 files, +2188/−315; incl. REVIEW-TASK-034 + REVIEW-TASK-034-FIX + the fix-cycle records + the status/epic refresh + the task file moved to completed/)
- `134284a` — docs(orchestration) — TASK-034 dispatched — status WIP + next-action refreshed
- `3a89779` — docs(orchestration) — TASK-034 contract READY — touch & petting (206-line contract: R1 pure vocabulary, R2 one engine path + director events, R4 §6.2 gating, R5 coalescing, R6 custom actions, R7 spoken pool + epoch, R9 cancel seam, R12 scope pins)
- `f1668b7` — TASK-033 — feat(home): TASK-033 compose Home — status row, live rig canvas, contextual line, quest card, action pills (atomic; 27 files, +2733/−136; incl. REVIEW-TASK-033 + the dispositions + the OBS-C/OBSERVATION-B doc errata + the closeout records)
- `8bf6ff4` — docs(orchestration) — TASK-033 contract READY — Home composition (210-line contract: R1 MomoKit HomeCopyKeys/HomeReadModel, R2 app-model computed, R3 catalog landing 70 entries, R4 CopyRules carveout, R5 catalog-law tests, R6 HomeView composition, R7 DEBUG fixed-clock enabler, R8 five UI tests)
- `8bf6ff4` — docs(orchestration) — TASK-033 contract READY — Home composition (210-line contract: R1 MomoKit HomeCopyKeys/HomeReadModel, R2 app-model computed, R3 catalog landing 70 entries, R4 CopyRules carveout, R5 catalog-law tests, R6 HomeView composition, R7 DEBUG fixed-clock enabler, R8 five UI tests)
- `31b2953` — housekeeping — record TASK-032 completion (EPIC-007 2/9 DONE) — task file to completed + epic 2/9 + status refresh
- `53e433f` — TASK-032 — feat(app): onboarding S1→S2→S3 — three calm steps, atomic completion flag (atomic; 15 files, +1187/−16; incl. REVIEW-TASK-032 + the disposition + the housekeeping-era NOTE-2 hardening)
- `c0ad5ec` — docs — record adjudicated errata into the doc chain (016/018/019/028 backlog; the doc-clarification cycle, task #74)
- `323b348` — docs(architecture) — ADR-013 standalone watch packaging — TASK-009 decision promoted
- `8b3b120` — docs(orchestration) — TASK-032 contract READY — onboarding S1→S2→S3
- `257a58f` — TASK-031 — feat(app): app model facade — evaluate/apply loop, persistence wiring, boundary scheduling (atomic; 12 files, +1938/−9; incl. REVIEW-TASK-031 + the disposition)
- `a410813` — docs(orchestration) — EPIC-007 epic + TASK-031 contract READY — app model facade (first commit on `feature/EPIC-007-iphone-home`; also entered `main` via the owner's PR #9 `74eba4f`)
- `74eba4f` — owner PR #9 — merged `feature/EPIC-007-iphone-home` through `a410813` (epic + TASK-031 contract) to `main`
- `13a75bf` — merge — EPIC-006 Character Rendering (TASK-025–030) into `main` — direct merge per owner rule 2026-09-08 (owner PR #7 `aa072ea` reconciled; remainder 13 commits `--no-ff`; merged state verified 825/82 green, tree content-identical to the branch tip)
- `3930f5a` — docs(orchestration) — TASK-030 closeout — task file DONE + epic 6/6 + status refresh
- `4f8e3ac` — TASK-030 — test(character): TASK-030 character suite audit + determinism-property coverage record (atomic; 3 files, +383/−7; incl. REVIEW-TASK-030 + the disposition)
- `a249913` — docs(orchestration) — TASK-029 closeout — task file DONE + review record + epic 5/6 + status refresh
- `9a46641` — TASK-029 — feat(character): TASK-029 Reduce Motion mapping + token theming audit (atomic; 34 files, +2233/−24)
- `34c2e04` — docs(orchestration) — TASK-029 contract READY — Reduce Motion mapping + theming audit
- `0de23f8` — docs(orchestration) — resolve TASK-028 closeout commit hashes in status.md
- `4a713e7` — docs(orchestration) — TASK-028 closeout part 2 — epic 4/6 + status refresh
- `b203b71` — docs(orchestration) — record TASK-028 completion (EPIC-006 4/6 DONE) — task file to completed + both review records
- `cc98b06` — TASK-028 — feat(character): TASK-028 reaction vocabulary, state choreography, CharacterReport emission (atomic; 21 files, +4617/−26)
- `283310a` — docs(orchestration) — TASK-028 contract READY — reactions, choreography, CharacterReport
- `d26b9d3` — TASK-027 — feat(character): TASK-027 deterministic idle sequencer + expression system (atomic; 28 files, +4637/−160; incl. REVIEW-TASK-027 + REVIEW-TASK-027-FIX1 + ADR-009/010/011/012 + the disposition's corrections)
- `2e84cf7` — docs(orchestration) — TASK-027 dispatched — status WIP + next-action refreshed
- `b663726` — docs(orchestration) — TASK-027 contract READY — idle sequencer + expressions, four TASK-026 routings carried as blocking requirements
- `d0955e0` — TASK-026 — feat(character): TASK-026 rig layer tree, CharacterClock, LOD tiers (atomic; 26 files, +2971/−4; incl. REVIEW-TASK-026 + the disposition's pre-commit doc fixes)
- `912b255` — docs(orchestration) — TASK-026 contract READY, impl agent dispatched
- `7681498` — TASK-025 — feat(character): TASK-025 asset pipeline + generated rig Path constants (atomic; 41 files, +5371/−40; incl. REVIEW-TASK-025 + the fix-round-1 delta review)
- `cd22acd` — merge — EPIC-005 Persistence & Sync Logic (TASK-021–024) into `main` — direct merge per owner rule 2026-09-08 (review chains complete; merged state verified 515/54 green, tree content-identical to the branch tip)
- `1d3819b` — housekeeping — record TASK-024 completion (EPIC-005 4/4 DONE), task file to completed
- `4372913` — TASK-024 — test(kit): TASK-024 MomoKit suite audit, §10.4 mapping, coverage floor (incl. REVIEW-TASK-024 + disposition)
- `9baa49d` — docs(orchestration) — TASK-024 contract READY, impl agent dispatched
- `47f77cf` — housekeeping — record TASK-023 completion (EPIC-005 3/4), task file to completed
- `9109457` — TASK-023 — feat(sync): TASK-023 sync DTOs, intent journal, watermark arithmetic (incl. REVIEW-TASK-023 + disposition)
- `2cc01cb` — docs(orchestration) — TASK-023 contract READY, impl agent dispatched
- `b5a390e` — housekeeping — record TASK-022 completion (EPIC-005 2/4), task file to completed
- `3acc54f` — TASK-022 — feat(persistence): TASK-022 migration chain + retention/pruning (incl. REVIEW-TASK-022 + disposition)
- `241f9cf` — docs(orchestration) — TASK-022 contract READY, impl agent dispatched (epic errata: §5.4/§5.5 refs unswapped, AC-4 INV-9 mis-citation fixed)
- `0ca305f` — chore — chore(core): resolve var→let warning in EngineClockTests (routed from the TASK-021 review)
- `5cca031` — TASK-021 — feat(persistence): TASK-021 SnapshotStore — envelope, atomic writes, generational recovery (incl. both review records)
- `04d07d6` — merge — EPIC-004 Pet State Engine remainder (TASK-019–020) into `main` — direct merge per owner rule (see the merge episode note under Important Context)
- `00f5d77` — owner PR #6 — merged `feature/EPIC-004-engine` through `8201b6d` (TASK-018 + TASK-019 dispatch) to `main`
- `0a9bd82` — owner PR #5 — merged `feature/EPIC-004-engine` through `8cf46e2` (epic + TASK-014/015 contracts) to `main`
- `b75f25a` — housekeeping — record TASK-020 completion (EPIC-004 7/7 DONE), task file to completed
- `0825c97` — TASK-020 — test(engine): TASK-020 §10.3 matrix, meta-determinism property, coverage floor
- `13d2734` — docs(orchestration) — TASK-020 contract READY, impl agent dispatched
- `d91fad0` — housekeeping — record TASK-019 completion (EPIC-004 6/7), task file to completed
- `24e1a03` — TASK-019 — feat(engine): TASK-019 display read-models and copy-key selection
- `8201b6d` — docs(orchestration) — TASK-019 contract READY, impl agent dispatched
- `d52c146` — housekeeping — record TASK-018 completion (EPIC-004 5/7), task file to completed
- `d4820a7` — TASK-018 — feat(engine): TASK-018 quest generator, completion windows, Watch cascade
- `60e4fe5` — docs(orchestration) — TASK-018 contract READY, impl agent dispatched
- `928f9a3` — housekeeping — record TASK-017 completion (EPIC-004 4/7), task file to completed
- `4a9170c` — TASK-017 — feat(engine): TASK-017 bond ledger, variety bonus, stage moments
- `38ece67` — docs(orchestration) — status.md: TASK-017 dispatched, exact next action = review cycle
- `b045af4` — docs(orchestration) — TASK-017 contract READY, impl agent dispatched
- `f166ff6` — housekeeping — record TASK-016 completion (EPIC-004 3/7), task file to completed
- `f61093a` — TASK-016 — feat(engine): TASK-016 interaction semantics, satiety window, repetition curve
- `9b04621` — housekeeping — record TASK-015 completion (EPIC-004 2/7), task file to completed
- `7b5d735` — TASK-015 — feat(engine): TASK-015 time-fold catch-up, wakefulness machine, handshakes
- `860764a` — docs(orchestration) — TASK-016 contract READY, impl agent dispatched
- `5fdbda1` — housekeeping — record TASK-014 completion (EPIC-004 1/7), task file to completed
- `696d8dd` — TASK-014 — feat(engine): TASK-014 engine core — reduce, clock, seeded randomness, day-stable seeds
- `8cf46e2` — docs(orchestration) — EPIC-004 epic + TASK-014/015 task files READY
- `4291f02` — merge — EPIC-003 Pet Domain Model (TASK-012–013) into `main` — direct merge per owner rule 2026-09-08
- `a334cba` — housekeeping — record TASK-013 completion (EPIC-003 2/2 DONE), task file to completed
- `5bbdd72` — TASK-013 — test(domain): TASK-013 exhaustive domain-model property tests
- `6d08f08` — housekeeping — record TASK-012 completion (EPIC-003 1/2), task file to completed
- `bc95cb9` — TASK-012 — feat(domain): TASK-012 define MomoCore domain model (05 §3.1)
- `f5c1c11` — docs(orchestration) — record owner direct-merge rule in CLAUDE.md §14
- `feb2898` — owner PR #4 — merged `feature/EPIC-003-domain-model` docs-only prefix (through `8ae4970`) to `main`
- `2973eb8` — housekeeping — record TASK-011 commit; EPIC-002 DONE (4/4), task file to completed
- `a2a36b7` — TASK-011 — feat(design): TASK-011 design-system token pass and String Catalog scaffolding
- `a9b9993` — TASK-010 — test(bootstrap): scaffold test targets with import-whitelist and banned-vocabulary scans
- `b28ccb4` — TASK-009 — feat(bootstrap): create Swift package and app targets with placeholder shell
- `172bc11` — TASK-008 — chore(bootstrap): verify toolchain and record deployment pins (ADR-008) — **first commit on `feature/EPIC-002-foundation`**
- `71a510b` — TASK-007 — docs(product): TASK-007 delivery plan with Phase 1 epics and task breakdown
- `95e7649` — owner decisions — OPEN-1 §5.5 rule-1 + I-2/OPEN-5 nibble applied (PRD amended)
- `8fb9653` — TASK-006 — docs(architecture): TASK-006 technical architecture and ADRs
- `2cfba30` — E2 record — Direction C gate resolution + ADR-001 + TASK-006 task file READY
- `39d6bab` — housekeeping — TASK-005 record completion
- `ce84811` — TASK-005 — docs(design): TASK-005 character system specification
- `40c4b77` — TASK-004 — docs(design): TASK-004 UX architecture for Phase 1
- `c766ffe` — TASK-003 — docs(product): TASK-003 MVP product requirements document
- `f72b78b` — TASK-002 — docs(product): step 1 product review of project spec
- `557c936` — TASK-001 — chore(orchestration): bootstrap Momo agent team contracts and task structure

## Recent Pushes
- `(this push pending)` — `feature/EPIC-009-release-readiness` → origin — the TASK-047 contract + status commit (recorded here at the next status edit per the "(this push pending)" convention)
- `caf6051..1f2d350` — `feature/EPIC-009-release-readiness` → origin — **SUCCESS** (the TASK-046 contract docs commit `1f2d350`)
- `c43c92b..caf6051` — `feature/EPIC-009-release-readiness` → origin — **SUCCESS** (the TASK-045 closeout docs commit `caf6051`: completed record + epic 1/6 + status refresh)
- `2996fa1..c43c92b` — `feature/EPIC-009-release-readiness` → origin — **SUCCESS** (the TASK-045 docs commit `c43c92b`: task record + REVIEW-TASK-045 + the 25-file evidence set; zero production diff)
- `d460a24..2996fa1` — `feature/EPIC-009-release-readiness` → origin — **SUCCESS** (the EPIC-009 epic `fa1a479` + the TASK-045 contract + status refresh `2996fa1`)
- `d460a24..244b7ea` — `main` → origin — **SUCCESS** (the post-merge status refresh `244b7ea`)
- `fffa6cf..d460a24` — `main` → origin — **SUCCESS** (the §14 EPIC-008 merge `d460a24`; fetch-first re-check found the owner's PR #17 `fffa6cf`, remainder 8 commits merged `--no-ff`, no force-push)
- `491777f..149f1a8` — `feature/EPIC-008-watch-sync` → origin — **SUCCESS** (the TASK-044 closeout docs commit `149f1a8`)
- `6bc8c40..491777f` — `feature/EPIC-008-watch-sync` → origin — **SUCCESS** (the TASK-044 atomic feat `491777f`, incl. the git-mv amend)
- `91c523f..6bc8c40` — `feature/EPIC-008-watch-sync` → origin — **SUCCESS** (the TASK-044 contract commit `6bc8c40`)
- `0bd6044..91c523f` — `feature/EPIC-008-watch-sync` → origin — **SUCCESS** (the TASK-043 closeout commit `91c523f`)
- `898fe09..0bd6044` — `feature/EPIC-008-watch-sync` → origin — **SUCCESS** (the TASK-043 atomic feat `0bd6044`)
- `faa8761..898fe09` — `feature/EPIC-008-watch-sync` → origin — **SUCCESS** (the TASK-042 closeout commit `898fe09` — recorded here at the next closeout per the "(this push pending)" convention)
- `692b5d2..faa8761` — `feature/EPIC-008-watch-sync` → origin — **SUCCESS** (the TASK-042 atomic feat `faa8761`)
- `2e65019..692b5d2` — `feature/EPIC-008-watch-sync` → origin — **SUCCESS** (the TASK-042 contract commit `692b5d2`)
- `4407544..2e65019` — `feature/EPIC-008-watch-sync` → origin — **SUCCESS** (the TASK-041 closeout `2e65019`)
- `fffbc5f..4407544` — `feature/EPIC-008-watch-sync` → origin — **SUCCESS** (the TASK-041 atomic feat `4407544`)
- `b1576b7..fffbc5f` — `feature/EPIC-008-watch-sync` → origin — **SUCCESS** (the TASK-041 contract commit)
- `c5430f3..b1576b7` — `feature/EPIC-008-watch-sync` → origin — **SUCCESS** (the TASK-040 atomic feat `0719129` + the closeout commit `b1576b7`)
- `a54be43..c5430f3` — `feature/EPIC-008-watch-sync` → origin — SUCCESS (the TASK-040 contract commit)
- `b73e3eb..a54be43` — `feature/EPIC-008-watch-sync` → origin — SUCCESS (new branch, tracking set — the EPIC-008 authoring commit: epic file + status refresh)
- `ad0199e..b73e3eb` — `main` → origin — SUCCESS (the §14 EPIC-007 merge commit; merged tree content-identical to the branch tip, 949/97 green on the merged state before push)
- `614053d..8604f5b` — `feature/EPIC-007-iphone-home` → origin — SUCCESS (the TASK-039 closeout commit)
- `d49fe9d..614053d` — `feature/EPIC-007-iphone-home` → origin — SUCCESS (the TASK-039 atomic feat commit: R1 verbatim landing pins + R2/R3 guard extensions + R4 warning housekeeping + R5 UI consolidation + R6 audit/contrast suites + view a11y fixes + R7 rename re-sync + R8 app-model split, REVIEW-TASK-039 + the task-file dispositions)
- `5ffe634..d49fe9d` — `feature/EPIC-007-iphone-home` → origin — SUCCESS (the TASK-039 contract commit)
- `4b02be7..5ffe634` — `feature/EPIC-007-iphone-home` → origin — SUCCESS (the TASK-038 closeout commit)
- `7340cd2..4b02be7` — `feature/EPIC-007-iphone-home` → origin — SUCCESS (the TASK-038 atomic feat commit: Settings surface + plan triggers + executor erase + R8 guards + the 5-test UI suite + REVIEW-TASK-038 + the task-file dispositions)
- `3376769..7340cd2` — `feature/EPIC-007-iphone-home` → origin — SUCCESS (the TASK-038 contract commit)
- `feature/EPIC-007-iphone-home` → origin — **success** (`2b486b3..bcecbed`, the TASK-037 atomic feat commit: RoomView static scene + D12 tab labels + placeholder deletion + REVIEW-TASK-037 + the task-file dispositions)
- `bcecbed..3376769` — `feature/EPIC-007-iphone-home` → origin — SUCCESS (the TASK-037 closeout commit)
- `feature/EPIC-007-iphone-home` → origin — **success** (`d790065..2b486b3`, the TASK-037 contract commit)
- `feature/EPIC-007-iphone-home` → origin — **success** (`2264f35..3a1698b`, the TASK-035 contract commit + status/epic refresh)
- `feature/EPIC-007-iphone-home` → origin — **success** (`3861c7b..d790065`, the TASK-036 closeout commit: task file → completed/ with evidence, status + epic refresh)
- `feature/EPIC-007-iphone-home` → origin — **success** (`94bf69b..3861c7b`, the TASK-036 atomic feat commit: event door + FIFO + banner + M1/M3 surfaces, the review record, and the task-file dispositions)
- `feature/EPIC-007-iphone-home` → origin — **success** (`b8d181a..94bf69b`, the TASK-036 contract commit)
- `feature/EPIC-007-iphone-home` → origin — **success** (`3a1698b..b8d181a`, the TASK-035 atomic commit incl. REVIEW-TASK-035 + REVIEW-TASK-035-FIX + the F-1 fix-cycle record + the closeout records)
- `feature/EPIC-007-iphone-home` → origin — **success** (`134284a..2264f35`, the TASK-034 atomic commit incl. REVIEW-TASK-034 + REVIEW-TASK-034-FIX + the fix-cycle records + the closeout records)
- `feature/EPIC-007-iphone-home` → origin — **success** (`8bf6ff4..f1668b7`, the TASK-033 atomic commit incl. REVIEW-TASK-033 + the dispositions + the closeout records)
- `feature/EPIC-007-iphone-home` → origin — **success** (`53e433f..8bf6ff4`, TASK-032 closeout housekeeping + the TASK-033 contract)
- `feature/EPIC-007-iphone-home` → origin — **success** (`c0ad5ec..53e433f`, TASK-032 atomic commit incl. REVIEW-TASK-032 + the disposition)
- `feature/EPIC-007-iphone-home` → origin — **success** (`323b348..c0ad5ec`, doc-errata adjudications + ADR-013 + the TASK-032 contract)
- `feature/EPIC-007-iphone-home` → origin — **success** (`a410813..257a58f`, TASK-031 atomic commit incl. REVIEW-TASK-031 + the disposition)
- `feature/EPIC-007-iphone-home` → origin — **success** (new branch, `a410813`, tracking set — EPIC-007 epic + TASK-031 contract; the owner PR-merged this to `main` the same day as PR #9 `74eba4f`)
- `main` → origin — **success** (`aa072ea..13a75bf`, the EPIC-006 merge; merged state verified 825/82 green before push)
- `feature/EPIC-006-character` → origin — **success** (`4f8e3ac..3930f5a`, TASK-030 closeout housekeeping)
- `feature/EPIC-006-character` → origin — **success** (`7174048..4f8e3ac`, TASK-030 atomic commit incl. REVIEW-TASK-030 + the disposition)
- `feature/EPIC-006-character` → origin — **success** (`9a46641..a249913`, TASK-029 closeout housekeeping)
- `feature/EPIC-006-character` → origin — **success** (`34c2e04..9a46641`, TASK-029 atomic commit)
- `feature/EPIC-006-character` → origin — **success** (`283310a..cc98b06`, TASK-028 atomic commit)
- `feature/EPIC-006-character` → origin — **success** (`2e84cf7..d26b9d3`, TASK-027 atomic commit incl. both review records + ADR-009…012)
- `feature/EPIC-006-character` → origin — **success** (`912b255..d0955e0`, TASK-026 atomic commit incl. REVIEW-TASK-026 + disposition)
- `feature/EPIC-006-character` → origin — **success** (`6a5fe4b..7681498`, TASK-025 atomic commit incl. both review records)
- `main` → origin — **success** (`04d07d6..cd22acd`, the EPIC-005 merge; merged state verified 515/54 green before push)
- `feature/EPIC-005-persistence` → origin — **success** (`4372913..1d3819b`, TASK-024 housekeeping)
- `feature/EPIC-005-persistence` → origin — (this docs commit follows on `main`)
- `feature/EPIC-005-persistence` → origin — **success** (`9baa49d..4372913`, TASK-024 atomic commit incl. REVIEW-TASK-024)
- `feature/EPIC-005-persistence` → origin — **success** (`47f77cf..9baa49d`, TASK-024 dispatch + contract)
- `feature/EPIC-005-persistence` → origin — **success** (`2cc01cb..9109457`, TASK-023 atomic commit incl. REVIEW-TASK-023)
- `feature/EPIC-005-persistence` → origin — **success** (`b5a390e..2cc01cb`, TASK-023 dispatch + contract)
- `feature/EPIC-005-persistence` → origin — **success** (`3acc54f..b5a390e`, TASK-022 housekeeping)
- `feature/EPIC-005-persistence` → origin — **success** (`241f9cf..3acc54f`, TASK-022 atomic commit incl. REVIEW-TASK-022)
- `feature/EPIC-005-persistence` → origin — **success** (`5cca031..0ca305f`, routed-warning chore fix)
- `feature/EPIC-005-persistence` → origin — **success** (`d4c8156..5cca031`, TASK-021 atomic commit incl. both review records)
- `main` → origin — **success** (`00f5d77..04d07d6`, EPIC-004 remainder merge; merged state verified 369/43 green)
- `feature/EPIC-004-engine` → origin — **success** (`0825c97..b75f25a`, TASK-020 housekeeping)
- `feature/EPIC-004-engine` → origin — **success** (`13d2734..0825c97`, TASK-020)
- `feature/EPIC-004-engine` → origin — **success** (`d91fad0..13d2734`, TASK-020 dispatch docs)
- `feature/EPIC-004-engine` → origin — **success** (`24e1a03..d91fad0`, TASK-019 housekeeping)
- `feature/EPIC-004-engine` → origin — **success** (`8201b6d..24e1a03`, TASK-019)
- `feature/EPIC-004-engine` → origin — **success** (`60e4fe5..d4820a7`, TASK-018)
- `feature/EPIC-004-engine` → origin — **success** (`4a9170c..60e4fe5`, TASK-017 housekeeping + TASK-018 dispatch docs)
- `feature/EPIC-004-engine` → origin — **success** (`38ece67..4a9170c`, TASK-017)
- `feature/EPIC-004-engine` → origin — **success** (`f166ff6..38ece67`, TASK-017 dispatch records)
- `feature/EPIC-004-engine` → origin — **success** (`860764a..f61093a`, TASK-016)
- `feature/EPIC-004-engine` → origin — **success** (`9b04621..860764a`, TASK-016 contract dispatch record)
- `feature/EPIC-004-engine` → origin — **success** (`5fdbda1..7b5d735`, TASK-015)
- `feature/EPIC-004-engine` → origin — **success** (`8cf46e2..696d8dd`, TASK-014; branch tracking set)
- `main` → origin — **success** (`feb2898..4291f02`, EPIC-003 integration merge; merged state verified green 80/14)
- `feature/EPIC-003-domain-model` → origin — success (`5bbdd72..a334cba`, TASK-013 housekeeping)
- `feature/EPIC-003-domain-model` → origin — success (`6d08f08..5bbdd72`, TASK-013)
- `feature/EPIC-002-foundation` → origin — success (`a2a36b7..2973eb8`, housekeeping)
- `feature/EPIC-002-foundation` → origin — success (`5389af1..a2a36b7`)
- `feature/EPIC-002-foundation` → origin — success (`983d94d..a9b9993`)
- `feature/EPIC-002-foundation` → origin — success (`81be80b..b28ccb4`)
- `feature/EPIC-002-foundation` → origin — success (new branch, `172bc11`, tracking set)
- main → origin — success (`95e7649..71a510b main -> main`)

## Architecture / Product Decisions
- Binding decision log: `docs/product/01-product-review.md` §6 (D1–D20).
- **ADR-001 (owner, E2):** Direction C "Round Rabbit" — ~11-part rig, ear-thickness rule, posture-led expression.
- **ADR-002–007 (TASK-006):** Codable atomic file store (envelope+checksum, 3-gen recovery, additive migrations); WatchConnectivity-only sync (context latest-wins ↓, FIFO journal ↑, dual idempotency guards scoped per `watchSessionEpoch`); pure event-driven engine `reduce(state, event, clock, rng)`, no timers; 90-min satiety window (0–30 refusal / 30–90 nibble ×0.25 owner-confirmed); SPM packaging MomoCore/MomoCharacter/MomoKit + app targets; SwiftUI-native rig (ADR-007); deployment-target policy (ADR-006, now **ACCEPTED** — status line reconciled during TASK-008's review loop).
- **ADR-008 (TASK-008, bootstrap pins — ACCEPTED):** min **iOS 26.0 / watchOS 26.0** (generation floor per ADR-006; build SDKs 26.5); pairing = both floors ≥ 26.0, current-generation floor; device matrix — iPhone small **iPhone SE (3rd generation)** 750×1334 px (375×667 pt, governs FR-2 AC-1a no-scroll), mid iPhone 17, large iPhone 17 Pro Max; Watch small Apple Watch SE 3 (40mm) 324×394 px (governs ADR-001 ~32 pt glyph; SE 2nd gen 40mm confirmed same geometry), flagship Series 11 (46mm), Ultra 3 optional upper bound; **Swift Testing** + XCUITest; N-1 widening deferred to TASK-050. Current-generation-hardware-only exclusion recorded as an owner lever (§36), **not taken**.
- **TASK-009 build-baseline decisions (recorded in the task file; REVIEW-TASK-009 NITPICK-2 follow-up):** hand-authored committed pbxproj (xcodegen absent; text-diffable, fresh-clone-friendly); **standalone watch target** honoring D-R3 with the companion relation via `WKCompanionAppBundleIdentifier` — **promote to an ADR by EPIC-008 start (ADR-009 candidate)**, before sync design hardens; tools-version 6.2 (adversarially verified minimum for the `.v26` pins); `.macOS(.v26)` is host-only for `swift test` (no macOS product/target); bundle IDs `com.momo.app` / `com.momo.app.watchkitapp` (E4 name clearance remains the TASK-050 gate).
- **TASK-011 design-system conventions (single canonical pass, 04 Appendix B item 3):** hex literals confined to `MomoCharacterPalette.swift` + `MomoUIColors.swift` (allowlist test-pinned); `MomoColorToken` carries non-optional light+dark variants (compile-enforced completeness); catalog = `Apps/Shared/MomoCopy.xcstrings` shared by both targets; `CopyKey` type-safe builders + `MomoCopy.lookup` (NUL-sentinel → nil) / `.resolve` (DEBUG assertionFailure, release falls back to key) with **injected Bundle** — package code never touches `Bundle.main`; placeholder convention = index `00` + `PLACEHOLDER` comment + `extractionState: manual`, seed pinned at exactly 3 keys (one per namespace); react lines VoiceOver-announced, never rendered (04 §10.1 rule 7). REVIEW-TASK-011 follow-ups recorded under Next Tasks.
- **Owner decisions 2026-09-08:** OPEN-1 — §5.5 cascade rule 1 = "(local time ≥ 20:00 or local time < 07:00)"; I-2/OPEN-5 — nibble class normative (PRD FR-6/§4 amended).
- **TASK-018 adjudications (REVIEW-TASK-018, 2026-09-09):** (1) §4.8/PRD §5.3 Q6-credit — the STRONG reading is normative: a new day earns Q6 credit iff BOTH of the two prior sets carry Q6 (`count == 2` required — unknown priors are no credit); §4.8's own "≥ 4 pairs in the tightest case (5 − 1)" arithmetic is only coherent under this reading, while PRD §5.3's window invariant holds under both (mutation-proven), so the non-emptiness parenthetical is the discriminator. (2) Moment multiplicity — a pat serves Q1 (window-gated) AND Q7 (all-day), so ONE event can complete both (§4.8's own offline-pat shape proves it through the production path); the tick loop handles multiples, and the moment contract is ≤ 3 per event (≤ 2 `.questCompleted` + ≤ 1 `.bondStageReached` last) — the TASK-017-era "≤ 1 completion per event structurally" premise is retired. (3) Draw distribution — the two-stage draw's pair weights are uniform in the two star-shaped scenarios (the unrestricted 15-pair space, the Q6-restricted 5-star); ban-active non-star pools skew, which no normative text forbids (NITPICK-1, doc-scoped).
- **TASK-019 adjudications (REVIEW-TASK-019, 2026-09-09):** (1) the `momo.line.vocab.<field>.<band>` fourth keyspace is SOUND — §4.9's "exactly three namespaces" governs the VARIATIONAL classes or 05 contradicts its own §4.11 DisplayState sketch; a fixed lookup is not a "selection", so the seed/epoch machinery rightly does not apply. (2) `EngineState.lastGreeting` is NECESSARY — §4.11's `makeCharacterDisplayState(_ state:)` is state-alone, and the greeting is the only moment that must outlive its event; `GreetingStamp` (kind + instant) is minimal, presentation owns fading. (3) Greeting rule order — missedYou outranks the hour (FR-12 AC-2 unconditional; a night-only usage pattern must still see missedYou), nightGlance outranks the dayKey change (midnight crossing is not a "morning"). (4) The one-draw `%` pick recipe is pinned; cross-context seed lockstep (OBS-A) and declined/warm key sharing (OBS-B) are contract-conformant, catalog-era decisions recorded in EPIC-006. (5) Verbatim catalog text lives in `VocabularyKeyTests.catalogObligations` (test fixtures are not engine output — INV-11 respected).
- **TASK-020 adjudications (REVIEW-TASK-020, 2026-09-09):** (1) Coverage bimodality — llvm-cov's reading of QuestTick.swift 82–85 (the logically-unreachable `assertionFailure` guard, proven unreachable from the `QuestProgress` failable-init contract) is nondeterministic across fresh instrumented runs (a garbage profiling-counter ~10^19 vs 0), yielding TOTAL 97.92 % vs the recorded 97.68 %; the ≥ 90 % floor holds under BOTH readings; 97.68 % is the conservative reproducible choice. (2) Anti-echo division of labor CONFIRMED — behavior tests may be formula-shape pins computed FROM `Thresholds`/`FoldRules` constants (they follow constant changes and stay green), while raw-literal PRD pins carry the constant-VALUE teeth (mutation `moodAttractorTauHours` 3→3.5 bit four raw pins while `couplingSelfReversesOnEnergyRecovery` and the meta-suite stayed green — exactly the designed outcome). (3) The meta-suite's direct `BondLedger.awardQuestCompletion` step is the contract's "quest-award mechanism calls" wording, not a reduce-surface gap (TASK-018's mechanism-only design).
- **TASK-029 RM law (2026-09-10, standing):** Reduce Motion is a RENDER-ONLY policy (R15, 04 §7.3's closing law) — the director fold takes NO flag (`apply(_:)` has no RM input, pinned structurally by the discipline scoping test and behaviorally by the twin corpus); all RM transforms live in `MomoReduceMotion.swift` at the overlay/pose seams; the ONE ambient read is the view's `accessibilityReduceMotion` (discipline-pinned). When a doc gives a BAND (§7.3's 0.15–0.20 s), the authored value is the band FLOOR (0.15) pinned as an exact digit and disclosed in the register — the conservative reading. Authored deviations from full-motion choreography (greeting key instants, milestone stills) are disclosed in the module's Disclosures register (15 items) — that register is the authoritative record of every RM-authored value and its doc authority; extend it, never bypass it.
- **TASK-033 copy-era adjudications (REVIEW-TASK-033, 2026-09-10):** (1) **OBS-C DISCHARGED** — 04 §10.3's Day/Evening sample-line headers are ILLUSTRATIVE groupings; the engine's `CopyRules.timeSlot` cut-offs (day [12,18), evening [18,22)) are NORMATIVE (dated note in the doc; no line moved). (2) 03 §5.5 errata (OBSERVATION-B) — `QuestWindow.eveningAndEarlyMorning` opens Q6 UNCONDITIONALLY (`hour ≥ 20 ∨ hour < 7`); the doc's "2 wish lines" prose holds for 07:00–12:00 only; engine normative. (3) The picker is ZERO-BASED over real pools (`00`–`<count-1>`); the placeholder-era "index 00 reserved for placeholders" convention is superseded (recorded in `CopyRules`' header). (4) REVIEW-TASK-033 MINOR-1 → TASK-039 (verbatim VALUE pins beyond the 12 vocab entries); OBSERVATION-A → owner (see Next Tasks).
- **TASK-007 delivery plan (`docs/product/06-delivery-plan.md`, normative for execution):** Phase 1 = EPIC-002 foundation → 003 domain model → 004 engine → 005 persistence/sync logic → 006 character rendering → 007 iPhone home → 008 watch & sync → 009 polish/QA/release. Slice spine 008→009→012→014→016→017→019→021→031→033→034→040→041(+042) = project.md §40 Step 7; slice completes at end of EPIC-008. Parallel lanes: LANE A (EPIC-005 from 012/014), LANE B (EPIC-006 from 009+011). Coverage floors Core ≥ 90 % / Kit ≥ 80 %; 05 §12 budgets are release blockers (measured on iPhone SE (3rd gen) / Watch SE 3 40mm at TASK-045); NFR-6 accessibility audit launch-blocking. Feature branches per epic from EPIC-002 (`feature/EPIC-00X-slug`); ADR numbering continues at ADR-009+. §3 task tables are the backlog of record — task files created just-in-time per batch.
- PRD-normative numbers: Bond 0–1000 monotonic, stages 149/399/749, +8/+4/+6/+20 cap; Mood bands 20/45/75 (attractor 60, floor 25, ceiling 92); Energy bands 20/45/75; quests Q1–Q7, daily set = Q1 + 2 seeded, Q1 < 12:00, Q6 20:00–07:00.
- **ADR-009 (TASK-027, ACCEPTED):** the composition ORDER is settled and pinned — `MomoRigView` composes stage transforms ancestor-first (T→R→S per listing order), with per-channel groups (body/head/ears/tail/aperture/gaze as an OptionSet) and a ≤ 3-channel render budget; `RigMotionModel` writes channels and `MomoIdleArbiter.admitted` gates them at RENDER level only (the schedule itself stays doc-exact). Established by R1's numeric reconciliation (SwiftUI modifiers apply innermost-first; the breath-only channel was the reconciliation witness).
- **ADR-010 (TASK-027, ACCEPTED):** the resume discipline under the zero-on-pause `CharacterClock` — app-hide zeroes the timeline; on return the seeded schedule REGENERATES identically and plays in real time from zero (no backlog burst, no between-pause replay — there are no between-pause events under zeroing; seed unchanged; TASK-026 clock pins untouched). Adjudicates 04 §5.3's "never replay" as the backlog sense; the TASK-027 contract's "identical continuation from T + gap" clause was an over-translation and is retired.
- **ADR-011 (TASK-027, ACCEPTED):** the body-scaleY composition law — §3.1's posture band (+3 %/−5 %, clamped to 0.95…1.03) governs the STATIC posture channel ONLY (pre-clamped in the expression layer); breath (§7.1) and idle-event motion (§5.2) compose multiplicatively UNCLAMPED, each bounded by its own authored magnitudes. Never re-clamp the composed value: it flattens the §7.2 pure sine on half of every cycle (proven on Joyful/Low/Wistful/asleep before the fix).
- **ADR-012 (TASK-027, ACCEPTED):** two occupancy-budget tiers for 04 §5.3's stillness floor (which the doc scopes to Content/baseline only): Content/baseline mean ≤ 0.15 AND p95 ≤ 0.15 with max ≤ 0.20 as an authored stability ceiling (measured 0.110/0.142/0.170); cross-state authored tier mean ≤ 0.20 / per-run max ≤ 0.25 (worst single run 0.232, Joyful). The disposition's provisional cross-state ≤ 0.20 max was falsified by measurement — pins that their own evidence disproves are flaky tests, not budgets. Non-vacuity floor: battery mean ≥ 0.05.
- **ADR numbering note (TASK-027 housekeeping):** the standalone-watch-packaging ADR reservation (TASK-009 record) renumbers **ADR-009 → ADR-013** — due by EPIC-008 start. **DISCHARGED (2026-09-10): promoted as ADR-013** (below).
- **ADR-013 (TASK-009 decision, promoted 2026-09-10, ACCEPTED):** MomoWatch ships as a STANDALONE watchOS application target (`WKApplication = true`) with NO Momo→MomoWatch target dependency — D-R3's enforcement clause satisfied by construction (both app targets `dependencies = ()`; the only `PBXTargetDependency` entries are the UI-test bundles → their tested apps), with the companion relation DECLARED, not structural (`WKCompanionAppBundleIdentifier = com.momo.app` in the watch Info.plist). Promoted from the TASK-009 record per REVIEW-TASK-009 NITPICK-2; every load-bearing claim re-verified in-tree by the orchestrator before commit. EPIC-008's sync design inherits the packaging (WC sessions live per-app-target); an embedded-shape flip is a distribution change requiring a superseding ADR.
- **ADR-014 (TASK-041 contract decision, 2026-09-11, ACCEPTED):** the Watch's character render source — `WatchSnapshot` gains one additive-OPTIONAL `character: WatchCharacterDTO?` field mirroring exactly the four `CharacterDisplayState` fields `DisplayState` lacks (moodBand, energyBand, activity, satietyHint), derived iPhone-side from the SHARED `makeCharacterDisplayState(state)` and assembled for W1 by a pure MomoKit function; `decodeIfPresent`/`encodeIfPresent`, NO `schemaVersion` bump (the TASK-040 D-1 adjudication pattern; OBS-3 not fired); nil → text-only degraded render (words + quest, canvas skipped, DEBUG-loud) for the cross-version case that cannot ship in Phase 1. Alternatives rejected: full seven-field mirror (redundant, drift-prone), Watch-side band inversion (lossy + Watch-side logic — violates 04 §9.2/AC-5), schema bump (drops payloads, fires OBS-3). **Review delta (REVIEW-TASK-041 F-1/F-2, both verified at source):** the DEBUG-loud nil-character log was NOT implemented (routes to TASK-042's receive path), and the view HOLDS the canvas slot with the palette blanket rather than skipping it — arguably the better render; the "skipped vs held" wording reconciliation routes to TASK-042 (one-line comment fix in `GlanceView.swift` or this ADR).
- **ADR-015 (TASK-042 contract decision, 2026-09-11, ACCEPTED):** the Watch pat presentation over the FROZEN EPIC-006 vocabulary — **D1** the micro-reaction binds the EXISTING `.tap` (awake) / `.stir` (asleep, stays down) clips through `MomoRigView`'s existing `reactionMotion: (Double, Bool) -> MomoReactionMotion` sampler param (default `.identity`; W1's canvas binds one ADDITIVELY at the call site — zero `Sources/MomoCharacter/` edits), sampled over elapsed time and reverting to `.identity` past the clip window (≤ 1 s holds by authored baselines); the glyph/AOD tier binds NO sampler (static by construction) and Reduce Motion returns a static pose (render-only law). **The docs' "tiny heart" is NOT shipped** — no heart shape exists in the frozen vocabulary (`MomoProps` is GENERATED, food/blanket/2 sparkles only); visual delta disclosed → owner backlog, gates nothing. **D2** the completion haptic is decided by LOCAL ESTIMATION at pat time from the HELD snapshot (`questLine == .wish(.q7)` ∧ Q7 `questInputs` progress + this Watch's pending journal pats + 1 == target → the celebratory double REPLACES the soft tick — ONE haptic carries the moment); the estimate decides WHICH HAPTIC PLAYS and nothing else (no state moves, the engine is never told); false-positive across silent iPhone-side completion disclosed, false negatives impossible from staleness (the held view can only UNDER-count — the on-wrist completing pat always celebrates). **D3** haptics play through an injectable seam over `WKInterfaceDevice.play(_:)`; exact `WKHapticType` values resolved against real SDK headers at build time (VERIFY-AT-BUILD, recorded in task notes); haptic FEEL is not simulator-observable — device-feel judgment is TASK-044's paired-hardware obligation or BLOCKED evidence (§25/§27). Both haptics gate on the CURRENT snapshot's `hapticsEnabled` at pat time (the TASK-036 delivery-time discipline). Rejected alternatives: authoring heart art (EPIC-006 frozen surface), porting the director (Watch runs no engine), synthesizing a momentRequest (misrepresents engine state), snapshot-flag completion notice (schema growth for a haptic that still cannot fire at pat time offline), tick+double back-to-back (noise).
- Orchestration: docs under `docs/{product,design,architecture}/`; ADRs under `.claude/tasks/decisions/` (ADR-001…015 present); **feature branches from EPIC-002 onward**; all agents Jupiter.

## Known Issues
- **Host disk pressure (2026-09-12, TASK-045 era; EASED as of 08:03 UTC):** the host volume hit 100 % (~499 MiB free) mid-TASK-045 — the implementer's ENOSPC incident is disclosed in the evidence set (`00-environment.txt`). The orchestrator deleted the regenerable `~/Library/Developer/Xcode/DerivedData/ModuleCache.noindex` (976 MB, pure Xcode compiler cache — no project data; Momo's own DerivedData kept for its xcresults) → ~1.4 GiB free; the reviewer's gate re-runs consumed it → **~790 MiB free at review close**; it sank to **~496–509 MiB during the TASK-046 implementer run** (200 MiB-floor disk watch armed, 20 s cadence — never breached), then **eased to ~5.4 GiB free at 08:03 UTC** (owner-side space freed; no orchestrator deletion). Heavy future sessions (UI suites, Release builds, xctrace captures) can still sink the volume fast — keep the df-check-before-big-gates discipline and the 200 MiB stop floor. Owner freeing more space remains welcome.
- ~~**`MomoUITests/MomoUITests.swift` lacks `@MainActor` (TASK-035 F-3, routed to TASK-039):** 19 pre-existing actor-isolation warnings in the TASK-031-era launch-test file (diff-empty in TASK-035 — zero lines changed); a whole-module recompile (same-target test merge) re-emits its latent diagnostics, which cache hits previously hid.~~ **RESOLVED (TASK-039 R4):** `@MainActor` on all UI-test classes; build-for-testing census zero warnings from touched files (REVIEW-TASK-039 gate 3). Pre-existing debt cleared, no behavior change.
- **Substring structural guards cannot catch comment-entombment (TASK-035 N-3, accepted trade):** R9a-d, the TASK-034 F-1 guard, and the TASK-026 scanners are textual predicates — a leg's text left present but commented out (or `#if false`d) still satisfies them. Threat model is accidental refactor, not adversarial comment-out. Optional future hardening: per-leg "exactly one uncommented occurrence" — a housekeeping item only, never mid-review.
- **App-target environment warnings on FULL builds (REVIEW-TASK-032 NOTE-3, epic-level follow-up candidate):** besides the standing `/opt/extra/lib` linker notice below, a full clean `xcodebuild build` also emits AppIntents metadata-extraction noise. Not any task's code; zero Swift/package warnings everywhere. Candidate: one epic-level environment/project-fix task to silence both.
- ~~TR10 "Xcode availability never verified"~~ **RETIRED AS VERIFIED** (`172bc11`; ADR-008 Consequences; risk R1 mitigated).
- 2026 fall OS churn: all API availability claims VERIFY-AT-BUILD; register in 05 Appendix B, every item has exactly one owning task (009/010, 014, 025, 040/044, 045, 048).
- R9: paired Watch hardware needed for WC delivery obligations (TASK-044) — simulators carry development; device session is the explicit deliverable.
- ~~`.gitignore` minimal~~ **RESOLVED (TASK-009):** Swift/Xcode entries added (`.build/`, `DerivedData/`, `xcuserdata/`, `*.xcuserstate`; the committed `Momo.xcodeproj` is deliberate).
- ~~Schemes carry no TestAction~~ **RESOLVED (TASK-010):** TestActions added to both shared schemes; `xcodebuild test` works on both.
- `xccov` cannot read SwiftPM's raw profdata ("unrecognized file format") — coverage is read via `xcrun llvm-cov report …` (command recorded in the completed TASK-010 file).
- **Standing note (REVIEW-TASK-013 MINOR-1):** the reflection-based no-numeric-leakage check inspects STORED fields only — computed members, subscripts, and extension members on `PetState`/`CharacterDisplayState` are invisible to it (documented in the `NoNumericLeakageTests.swift` header, "KNOWN BLIND SPOT"). Any engine-era numeric computed accessor on a read-model type therefore requires explicit review attention; band/stage CASE sets are compile-time-pinned (a new enum case fails the build).
- ~~**Ledger pruning owner (REVIEW-TASK-015 Observation)**~~ **RESOLVED (TASK-022):** retention is the store's `LedgerRetention.pruned`, applied on the save path — on-disk payloads always satisfy the 7-day/64 caps; the engine stays append-only and TASK-018's quest generation reads the in-memory tail unchanged.
- **Coverage-measurement note (REVIEW-TASK-020 OBS-A):** QuestTick.swift 82–85 — the logically-unreachable `assertionFailure` guard — intermittently reports a garbage execution count to llvm-cov (profiling-counter artifact), so TOTAL coverage readings are BIMODAL (97.68 % / 97.92 %) across otherwise-identical fresh instrumented runs. The Core ≥ 90 % floor holds under both; record 97.68 % as conservative. Do not chase this signal in future coverage measurements.
- ~~**Stale no-op test wraps (REVIEW-TASK-027 NOTE-2):** two `clampedPostureScaleY` wraps in `MomoIdleRenderTests`~~ **RESOLVED (TASK-028):** deleted as contracted housekeeping (byte-equivalent semantics — no-ops under ADR-011); suite green.

## Test Status
- Phase 0: review gates — all six REVIEW records final (002–007 APPROVED).
- TASK-008: verification-as-evidence complete; **three independent probe pairs agree** (reviewer / fixer / verifier: iPhone SE 3rd gen 750×1334 px, Watch SE 2 40mm 324×394 px); Swift Testing probe reproduced exactly.
- TASK-010/011: `swift test` = 35 tests / 9 suites green (superseded); both scheme UI smoke tests green on pinned sims; banned-vocab scan green over the real catalog and proven non-vacuous both directions (REVIEW-TASK-011).
- **TASK-045 (current; docs-only — zero production diff): `swift test` = 1161 tests / 113 suites green, re-run by the ORCHESTRATOR at closeout (exit 0); both scheme builds BUILD SUCCEEDED + `MomoWatchUITests` 9/9 re-run by the §33 reviewer (62.922 s, xcresult-verified on Watch SE 3 44mm `8A854895`, watchOS 26.5); the 35/35 app UI suite honestly CITED as a baseline, NOT re-run (unchanged-inputs argument — the tree diff is task docs + evidence only); MomoKit coverage 90.84 % stands by tree identity.** New measurement-method note for future memory work: an XCUITest-launched app carries ~+22 MB of PERSISTENT automation overhead (testmanagerd/accessibility injection at runner launch) vs a clean `simctl launch` — 47 MB vs 25 MB, reproduced by the reviewer; both far under the 150 MB budget.
- **TASK-044 (prior): `swift test` = 1161 tests / 113 suites green — RE-VERIFIED on the MERGED `main` @ `d460a24` state before the §14 push** (TASK-044: baseline 1128/110 @ `6bc8c40` → +33 tests / +3 suites — `WatchSweepPlanTests` NEW 5 (arm-at-activation, flush-after-awaited-consume ordering, the count-recheck suppression [a consumption between arm and execution suppresses the sweep entirely], one-shot, verbatim re-enqueue), `MomoWatchSweepScanTests` NEW 16 + `Support/WatchSweepScan.swift` (the consume branch's sweep must FOLLOW its awaited `consumeWipe`; verbatim journal replay through the existing send leg; reset-suppression; pending-marker ⇒ zero send), `MomoWatchClockScanTests` NEW 10 + `Support/WatchClockScan.swift` (the R4/F-3 D20 banned-token scan — `Date()`/`Date.now`/`Calendar.current` banned over Apps/MomoWatch production sources + the recompute's injected-read pin, real tree clean, both-direction fixtures), +1 R3 alias direction, +1 R6 census delta; `MomoWatchUITests` 7/7 → **9/9** (+`testResetMarkerFixtureConsumesToTheSettlingInLine` — the R2 E2E through the REAL `receiveContext`, `WatchResetConsumption.decide` unmocked, with the phase-2 relaunch-over-same-store disk discriminator; +`testResetHoldFixtureKeepsTheGlanceRendered` — the no-marker control; the DEBUG `reset`/`resethold` seam release-inert) — green at the final tree by the implementer (1161/113), the §33 reviewer (1161/113 + 4 own probes sha256-restored + every gate re-run), and the orchestrator's §19 gate. **MomoKit coverage floor HOLDS post-TASK-044: 90.84 % lines** (160/1746 missed; regions 91.50 %; floor 80 %). TASK-043 (prior): baseline 1102/108 @ `faa8761` → +26 tests / +2 suites — `WatchCascadeTests` NEW 5 (the provenance property re-derives BOTH push-hour lines through the REAL chain `makeDisplayState` → day-record lookup → `makeWatchSnapshot`; the `provenanceBitesOnDriftedInputs` negative control; the `WatchCascade` seam passthrough bindings), `MomoWatchQuestScanTests` NEW 21 + `Support/WatchQuestScan.swift` (3 structural guards — quest-slot census [display-only slot Text; 1 Button / 1 tap / 1 live read / 1 frozen read] / recompute-legs census [1 def; calls−defs == 4; receiveContext carries the call; `.active` gated BEFORE recompute] / no-settings view census [banned tokens + exactly 1 `: View`] — each with both-direction fixture self-tests + standing real-tree runs); `MomoWatchUITests` 5/5 → **7/7** (+`testAllDoneFixtureRendersTheAllDoneLine`, +`testStaleFixtureRendersTheLiveCascadeLine` — the runner derives the wish from the clock ±1 h) — green at the final tree by the implementer (1128/110), the §33 reviewer (1128/110 + 3 own probes A/B/C sha256-restored, full re-run clean), and the orchestrator's §19 gate. **MomoKit coverage floor HOLDS post-TASK-043: 90.81 % lines** (160/1741 missed; regions 91.41 %; the seam file 100 %; floor 80 %; ~flat from 90.79 %). TASK-042 (prior): baseline 1056/104 @ `692b5d2` → +46 tests / +4 suites — `WatchPatPlanTests` NEW (~16: the seq formula 6 cases incl. FULL-PRUNE MONOTONICITY + stale/mixed-epoch + watermark-wins; the estimator truth table incl. OVERSHOOT strict-`==` + missing-Q7-false; pending count; reaction map), `WatchSessionEpochStoreTests` NEW (roundtrip/garbled/persist-at-init), `IntentJournalWipeTests` NEW (idempotent wipe, only the journal file touched), `MomoWatchPatScanTests` NEW 19 + `Support/WatchPatScan.swift` (the 3 structural guards — journal single-writer census / pat-flow order + single send leg / haptic-seam census — each with both-direction fixture self-tests); `MomoWatchUITests` 3/3 → **5/5** (+`testCanvasTapKeepsTheGlanceRendered`, +`testPatPillTapKeepsTheGlanceRendered`) — green at the final tree by the implementer (1102/108), the §33 reviewer (1102/108 + 3 own probes P1/P2/P3 exactly-red, sha256-restored, 18-file sweep byte-identical), and the orchestrator's §19 gate. **MomoKit coverage floor HOLDS post-TASK-042: 90.79 % lines** (160/1738 missed; regions 91.71 % / functions 95.48 %; `WatchPatPlan` 100 %, `WatchSessionEpochStore` 77.6 %, `IntentJournal` 74.7 % — the defensive legs; floor 80 %; down from 91.48 % as disclosed, trend watched at TASK-043). TASK-041 (prior): baseline 995/100 @ `fffbc5f` → +61 tests / +4 suites — `WatchCharacterDTOTests` NEW (DTO roundtrip + `decodeIfPresent` compatibility pins incl. the nil-character degraded shape), `WatchResetConsumptionTests` NEW (decision boundaries: first consume wipe+settle+record, repeat consume no-re-wipe, newer-count consume, nil-marker skip, count-regression self-defense), `WatchSnapshotStoreTests` NEW (rotation current→prev, corruption→prev→nil read chain, atomic no-temp invariant, one-writer), `MomoWatchGlanceScanTests` NEW (the 3 structural guards with two-direction stub fixtures) + `Support/WatchGlanceScan.swift`; catalog pins + law coverage for the settling-in + Pat keys; `MomoWatchUITests` rewritten **3/3** (settling-in fresh launch; fixture-seeded W1 content + ≥44 pt targets; restore-within-budget GENUINE disk restore) — green at the final tree by the implementer (1056/104), the §33 reviewer (1056/104 incl. the Bite C re-run + 3 own probes, all sha256-restored, 28-file sweep byte-identical), and the orchestrator's §19 gate. **MomoKit coverage floor HOLDS post-TASK-041: 91.48 % lines** (137/1608 missed; regions/functions 91.53 %/89.84 % per REVIEW-TASK-041 Part 7 — the task record transposes the two; floor 80 %; down from 92.47 % as disclosed — the store's defensive legs + consumption guards). TASK-040 (prior): baseline 949/97 @ `c5430f3` → +46 tests / +3 suites — `WatchReceivePlanTests` NEW (8: decide matrix, per-epoch max-semantics recording, FIFO exactly-once stream, 3-seed shuffles at-most-once, no-op-leaves-untouched, the F-1 `AppModelPlanCore` propagation loop, decode compatibility), `WatchResetMarkerTests` NEW (13: roundtrip, garbled→nil, `markerOutlivesTheErasedStoreTree`, compatibility pins), `MomoWatchWiringScanTests` 14→21 (+7 guard-4 `markerDirectoryViolations` fixtures both directions + the standing real-tree test), `StoreRulesPinnedTests` +4 (marker-directory rules) — green at the final tree by the implementer (988/100), the §33 reviewer (988/100 ×3 incl. coverage runs), the fix agent (995/100; the guard bite EXACTLY ONE red), the delta reviewer (own D1/D2 probes), and the orchestrator's §19 gate (restoration sha256s re-verified; `git diff Sources/MomoCore/` empty). **MomoKit coverage floor HOLDS post-TASK-040: 92.47 % lines** (91.65 % regions / 92.62 % functions; floor 80 %; up from 91.42 % — re-measured on the exact tree per the standing gotcha). TASK-039 (prior): 949/97, baseline 945/96 @ `d49fe9d` → +4 tests / +1 suite — `MomoUIColorContrastTests` NEW (1 test: the 4 body-text pairs ≥ 4.5:1 on both grounds + documented-baseline equality legs as non-vacuity), `MomoCatalogScaffoldingTests` +1 (the 58-entry TASK-033-landing verbatim pin, B-1-bitten), `RigDisciplineTests` +2 (`homeLineLimitCensusMatchesDisclosure` + `homeStatusRowSpeaksMoodEnergyBeforeStage`, both reviewer-probe-bitten P-6/P-5) — green at the final tree by the implementer, the §33 reviewer (949/97 incl. 6 live bites + the disclosed P-6a aborted no-op probe, all sha256-restored; PLUS a compile-only `build-for-testing`), and the orchestrator's §19 gate (restoration sha256s re-verified, then 949/97 + app BUILD clean + FULL UI suite **35/35** — `MomoAccessibilityAuditUITests` 8/8 NEW (all 5 §10 surfaces; disclosure table exactly 2 rows), `MomoHomeUITests` 17, onboarding 4, settings 5, launcher 1; MomoWatch build **BUILD SUCCEEDED** zero warnings on Watch SE 3 44mm `8A854895`, watchOS 26.5). TASK-038 (prior): baseline 931/95 @ `bcecbed` → +14/+1 — `SettingsCopyKeyTests` NEW (7), `AppModelPlanTests` +4, `AppModelOnboardingPlanTests` +1, `RigDisciplineTests` +2 guards, `MomoSettingsUITests` +5; UI suite 27/27 at the reviewed tree. TASK-037 (prior): baseline 925/94 @ `3861c7b` → +6/+1 — 4 `RoomCopyKeyTests` + 1 R5 room guard + 1 scaffolding pin; app suite 22/22; Watch build green. TASK-036 (prior): baseline 907/92 @ `b8d181a` → +18 tests / +2 suites — `QuestMomentTests` + `MomoMomentQueueTests` NEW; `RigDisciplineTests` +3 (the R9e-g guards, both-direction fixtures), `MomoReduceMotionTwinTests` +1 fold assertion, `CatalogCopyLawTests`/scaffolding extended for the moment landing + the `.00` removal — green at the final tree by the implementer, the §33 reviewer (pre- AND post-bite, bites sha256-restored), and the orchestrator's §19 gate; app suite **21/21** on the pinned sim (368.3 s) — `MomoHomeUITests` 16/16 incl. the 4 new glass tests (M1 first-pat completion, M2 banner rise + verbatim line + tap-dismiss, M2 auto-fade, M3 warm note), legacy 1/1; MomoWatch build **BUILD SUCCEEDED** (Watch SE 3 44mm `8A854895`, watchOS 26.5); zero warnings from touched files. TASK-035 (prior): baseline 887/91 @ `2264f35` → +19 implementation tests / +1 suite (`CareMomentTests`, NEW) + 1 R9d fix test — per-suite deltas in the TASK-035 record; TASK-034: baseline 868/88 @ `f1668b7` → +19 tests / +3 suites — `CanvasTouchTests` 11, `PatPlanBondTests` 2, `MomoTouchSemanticsTests` 4, `MomoHomeUITests` +3, the F-1 wiring guard −1 net accounted in `RigDisciplineTests`; green at the final tree by the implementer, the round-1 reviewer, the delta reviewer, and the orchestrator's §19 gate; app suite **17/17** on the pinned sim (315.3 s) — `MomoHomeUITests` 12/12, onboarding 4/4, legacy 1/1; MomoWatch build **BUILD SUCCEEDED** (Watch SE 3 40mm, watchOS 26.5 — R6's seam compiles into the watch app); zero warnings from touched files — the sole diagnostics are the machine-env `/opt/extra/lib` linker notice and 19 PRE-EXISTING actor-isolation warnings in diff-empty `MomoUITests/MomoUITests.swift` (F-3: latent debt re-emitted by a whole-module recompile; routed to TASK-039)). Character coverage floor: none exists — the delivery-plan floors are Core ≥ 90 % / Kit ≥ 80 %, both recorded; the determinism-property coverage per 05 §10.2 is RECORDED in the TASK-030 deliverable (audit tables A–E + the pure-file record with per-suite `@Test` counts — three stale counts corrected per REVIEW-TASK-030 NOTE-2). Standing scans (D-R1 import whitelist, banned vocabulary, token purity, engine purity, MomoKit discipline scans: ambient-time / ambient-path / imports; character discipline scanners extended to 21 pinned files in TASK-028, +2 RM scoping pins in TASK-029) green in-suite with no new exemptions. Art budgets re-measured at TASK-030 close (exact `getsize` bytes, reviewer-reproduced digit-for-digit): rig bucket 50,096/307,200 B (16 %; Body 5,642 / Ears 3,181 / Eyes 6,782 / FaceDetails 5,438 / FrontPaws 3,122 / Glyph 8,504 / Head 2,110 / LODGlance 13,233 / Tail 2,084; 51,906 incl. the 1,810 B hand-written `MomoRig.swift` anchor outside the bucket), room+props 12,861+7,786 = 20,647/256,000 B (8 %), generated total 70,743/1,572,864 B (4.5 %), all-40 MomoCharacter sources 351,863 B (22 %); largest source file 792 lines (≤ 800 rule; the director; MomoReduceMotion.swift is 516). **Kit coverage floor HOLDS: MomoKit 90.84 % lines** (was 90.81 % at TASK-043, 90.79 % at TASK-042, 91.48 % at TASK-041, 92.47 % at TASK-040, 91.42 % / 341/373 at TASK-031, and 88.86 % / TOTAL 691/77 when first RECORDED at TASK-024, epic AC-6 satisfied — the file set grew with the facade core, the sync core, the Watch store/consumption/assembly, and now the cascade seam + scan guards; the TASK-024 per-file table + missed-line classification stand; last MEASURED at TASK-043 immediately after the coverage run — trend stabilized after the TASK-041/042 defensive-leg dips). Measurement gotcha (2026-09-10): a plain `swift test` after a coverage run REBUILDS the xctest binary and orphans the profdata ("object is newer" / "no coverage data found") — re-run `swift test --enable-code-coverage` and measure IMMEDIATELY; the report reads `.build/debug/MomoPackageTests.xctest/Contents/MacOS/MomoPackageTests` with `-instr-profile .build/debug/codecov/default.profdata`, and per-file rows are `Filename total missed %` per metric with Lines FIRST. Core coverage record unchanged: **MomoCore 97.68 % lines** (TASK-020; the QuestTick 82–85 bimodality standing note applies to all future readings). Only build diagnostic: the pre-existing machine-level `/opt/extra/lib` linker search path. The TASK-021-recorded `EngineClockTests.swift:46` warning did NOT reproduce in any of today's six full runs incl. the instrumented recompile (REVIEW-TASK-024 OBS-2; standing record unchanged).

## Build Status
- **TASK-045 (`c43c92b`, docs-only):** Release builds for measurement — Momo (Release-iphonesimulator) + MomoWatch (Release-watchsimulator) BUILD SUCCEEDED on the pinned sims; the §33 reviewer re-ran both scheme builds + Watch UI 9/9 green at the reviewed tree; zero production diff (`git diff HEAD --stat` = the task file only at review time), so every frozen surface is diff-empty by tree identity and the standing build records carry over unchanged.
- **§14 EPIC-008 merge (`d460a24`):** the §14 mandated gate — `swift test` **1161/113 green on the MERGED state** BEFORE push; the merged tree is content-identical to the branch tip (`git diff feature/EPIC-008-watch-sync main` EMPTY), so the branch-tip records (both schemes BUILD SUCCEEDED, `MomoWatchUITests` 9/9, app UI 35/35, MomoKit coverage 90.84 %) carry over identically to the merge; pushed `fffa6cf..d460a24`.
- **TASK-044 (`491777f`):** `xcodebuild -scheme MomoWatch` (Watch SE 3 44mm `8A854895`, watchOS 26.5) + `xcodebuild -scheme Momo` (iPhone SE 3rd gen `1F25E487`) — **BUILD SUCCEEDED** both (implementer, §33 reviewer, and the orchestrator's §19 gate); zero warnings from touched files (only the machine-env `/opt/extra/lib` ld notice ×2 + one benign appintentsmetadataprocessor note); `MomoWatchUITests` **9/9** (2 new reset-seam flows + 7 existing, xcresult-verified passed 9/failed 0/skipped 0). App UI suite unchanged at 35/35 (no iPhone UI surface change — frozen surfaces incl. `Apps/Momo/**` diff-EMPTY; the `WatchSweepPlan` seam is package-side additive, pbxproj untouched).
- **TASK-043 (`0bd6044`):** `xcodebuild -scheme MomoWatch` (Watch SE 3 44mm `8A854895`, watchOS 26.5) + `xcodebuild -scheme Momo` (iPhone SE 3rd gen `1F25E487`) — **BUILD SUCCEEDED** both (implementer, §33 reviewer, and the orchestrator's §19 gate); zero warnings from touched files (only the machine-env `/opt/extra/lib` ld notice ×2 + one benign appintentsmetadataprocessor note); `MomoWatchUITests` **7/7** (2 new cascade flows + 5 existing, xcresult-verified passed 7/failed 0/skipped 0). App UI suite unchanged at 35/35 (no iPhone UI surface change — frozen surfaces incl. `Apps/Momo/**` diff-EMPTY; the `WatchCascade` seam is package-side, pbxproj untouched).
- **TASK-042 (`faa8761`):** `xcodebuild -scheme MomoWatch` (Watch SE 3 44mm `8A854895`, watchOS 26.5) + `xcodebuild -scheme Momo` (iPhone SE 3rd gen `1F25E487`) — **BUILD SUCCEEDED** both (implementer, §33 reviewer, and the orchestrator's §19 gate); zero warnings from touched files (only the machine-env `/opt/extra/lib` notice + one benign appintentsmetadataprocessor note); `MomoWatchUITests` **5/5** (2 new pat flows + 3 existing). App UI suite unchanged at 35/35 (no iPhone UI surface change — **`Apps/Momo/**` diff-EMPTY** for TASK-042, the contract's hard pin held).
- **TASK-041 (`4407544`):** `xcodebuild -scheme MomoWatch` (Watch SE 3 44mm `8A854895`, watchOS 26.5) + `xcodebuild -scheme Momo` (iPhone SE 3rd gen `1F25E487`) — **BUILD SUCCEEDED** both (implementer, §33 reviewer, and the orchestrator's §19 gate); zero warnings from touched files (only the machine-env `/opt/extra/lib` notice + one benign appintentsmetadataprocessor note); `MomoWatchUITests` **3/3** (26.45 s). App UI suite unchanged at 35/35 (no iPhone UI surface change — the only iPhone-side diff is the `+Watch` extension's character-DTO threading).
- **Build baseline established (TASK-009, `b28ccb4`):** both schemes build on pinned simulators — BUILD SUCCEEDED; both apps launch with placeholder shells.
- **Test harness live (TASK-010, `a9b9993`):** `swift test` + `xcodebuild test` green for all five test targets on pinned sims; coverage 90.81 % lines TOTAL (informational).
- **Design system landed (TASK-011, `a2a36b7`):** both builds green; both UI smoke tests green; rendering pixel-verified exact against token hex on both canvases (iPhone light+dark, watch). Reviewer reproduced all of it independently.
- **TASK-040 (`0719129`):** `xcodebuild -scheme Momo` (iPhone SE 3rd gen `1F25E487`) + `xcodebuild -scheme MomoWatch` (Watch SE 3 44mm `8A854895`, watchOS 26.5) — **BUILD SUCCEEDED** both (implementer, §33 reviewer, and the orchestrator's §19 gate); zero warnings from touched files; the WC wrappers compile per ADR-013 (app-target placement, no entitlement/Info.plist/capability changes — R4's zero-requirements determination). App UI suite unchanged at 35/35 (no UI surface change in TASK-040).

## Repository Status
- Branch: **`feature/EPIC-009-release-readiness`** (cut from the merged `main` @ `244b7ea`; epic file `fa1a479`; TASK-045 contract + status `2996fa1`; TASK-045 docs commit `c43c92b`; TASK-045 closeout `caf6051`; TASK-046 contract + status `1f2d350`; the TASK-047 contract + this status edit land as `(this commit)`). **`main` @ `244b7ea`, pushed** (`d460a24..244b7ea`; `main` @ `d460a24` = the §14 EPIC-008 merge, pushed `fffa6cf..d460a24`; `origin/main` re-checked at merge time — the owner's PR #17 `fffa6cf` had merged the epic branch through the TASK-041 closeout `2e65019`, so the merged remainder was 8 commits: TASK-042 contract `692b5d2` / feat `faa8761` / closeout `898fe09`, TASK-043 feat `0bd6044` / closeout `91c523f`, TASK-044 contract `6bc8c40` / feat `491777f` / closeout `149f1a8`; merged `--no-ff`, merged tree content-identical to the branch tip [diff EMPTY], `swift test` **1161/113** green on the merged state BEFORE push, no force-push). `feature/EPIC-008-watch-sync` retained at `149f1a8` (audit precedent; cut from the merged `main` @ `b73e3eb`; epic file at `a54be43`, TASK-040 contract at `c5430f3`, TASK-040 feat at `0719129` + closeout at `b1576b7`, TASK-041 contract at `fffbc5f`, TASK-041 atomic feat at `4407544` + closeout at `2e65019`, the TASK-042 contract at `692b5d2`, the TASK-042 atomic feat at `faa8761` + closeout at `898fe09`, the TASK-043 atomic feat at `0bd6044` + closeout at `91c523f`, the TASK-044 contract at `6bc8c40` + the TASK-044 atomic feat at `491777f` + closeout at `149f1a8`). `feature/EPIC-007-iphone-home` retained at `8604f5b` (audit precedent); `feature/EPIC-006-character` retained at `3930f5a` (audit precedent).
- Clean/Dirty: **dirty BY DESIGN at `(this commit)`** — the working tree carries the RUNNING TASK-046 implementer's in-flight entries (`M .claude/tasks/active/TASK-046-edge-matrix.md` — its Implementation Notes fills — and `?? .claude/tasks/evidence/TASK-046/`), which MUST NOT be committed until its independent review passes (§10). Orchestrator-owned paths are clean after `(this commit)`.
- Uncommitted files: the TASK-047 contract (`.claude/tasks/active/TASK-047-a11y-audit.md`, NEW) and this status edit — committed as `(this commit)`; PLUS the TASK-046 implementer's two in-flight entries listed above — excluded from orchestrator commits, land with the TASK-046 atomic commit after review.
- Remote sync: `main` in sync through `244b7ea` (unchanged); `feature/EPIC-009-release-readiness` in sync through `1f2d350` (pushes `d460a24..2996fa1` + `2996fa1..c43c92b` + `c43c92b..caf6051` + `caf6051..1f2d350` landed; the TASK-047 contract push `(this push pending)` follows); `feature/EPIC-008-watch-sync` retained in sync through the closeout `149f1a8` (pushes `b1576b7..fffbc5f` + `fffbc5f..4407544` + `4407544..2e65019` + `2e65019..692b5d2` + `692b5d2..faa8761` + `faa8761..898fe09` + `898fe09..0bd6044` + `0bd6044..91c523f` + `91c523f..6bc8c40` + `6bc8c40..491777f` + `491777f..149f1a8` all landed); `feature/EPIC-007-iphone-home` retained in sync through the closeout `8604f5b` (push `614053d..8604f5b`); EPIC-006 (`3930f5a`), EPIC-005 (`1d3819b`), EPIC-004 (`b75f25a`), EPIC-003 (`a334cba`) feature branches retained (audit precedent).

## Important Context for Next Agent
- Read first: `CLAUDE.md`, `project.md`, `docs/product/06-delivery-plan.md` (§3 tables = backlog of record), then doc chain 01→02 (amended)→03→04→05 + ADR-001…008.
- Per-task cycle: fresh Jupiter agent → implement (no commit) → fresh adversarial reviewer (writes review file, replies confirmation-only) → fix loop (fresh fixer + fresh verifier when material) → atomic commit with TASK-ID → push → status update. Agents inherit Jupiter by omitting the model override (an explicit `fable` override fails in this environment — discovered 2026-09-08).
- **git mv gotcha (TASK-044 closeout, standing):** `git mv` stages the INDEX blob, NOT working-tree edits — any unstaged working-tree fills (task-note fills, reviewer verdicts, closeout edits) are silently DROPPED from the commit. The tell: the rename shows "(100 %)" similarity when edits should make it <100 %, and the file shows Modified after the commit. Fix: `git add <newpath>` + `git commit --amend` (safe only while unpushed — TASK-044's `a102d08` → `491777f` amend). Future moves of dirty files: plain `mv` + `git add`, or verify the committed blob content after the move (`git show HEAD:<path> | grep <fill-marker>`).
- **Merge rule (owner authorization, 2026-09-08, recorded in CLAUDE.md §14):** after a task/epic passes its independent review and findings are addressed, merge the feature/epic branch **directly into `main`** and push — **no PR** — then continue into the next epic without waiting for the owner. Never merge unreviewed code; never merge before required tests pass (§19). (Supersedes the owner's earlier PR-based integrations, PRs #1–#3.)
- **EPIC-004 merge episode (2026-09-09):** the owner merged the epic branch to `main` themselves via PRs #5 (`0a9bd82`, through `8cf46e2`) and #6 (`00f5d77`, through `8201b6d`) while the session was finishing TASK-019/020 — the first `git push main` of the session's own `--no-ff` merge was rejected (remote ahead). Reconciliation: local main reset to `origin/main` (the local merge was content-redundant with the pushed branch tip), then the REMAINDER (`8201b6d..b75f25a`: TASK-019 impl, TASK-020, housekeeping) merged `--no-ff` as `04d07d6` and pushed; merged tree verified content-identical to the branch tip (`git diff` empty) and 369/43 green. Lesson: **before any epic merge, `git fetch` + check `origin/main` — the owner may have PR-merged the branch mid-epic; merge only the missing remainder and never force-push.** §14 direct-merge authorization stands.
- **No-PR operating rule re-affirmed + PR #9 precedent (owner, 2026-09-10):** the owner noticed "some agent still tries to create a pull request" — investigation: agents never create PRs (`gh` is not installed; GitHub shows its own "Compare & pull request" banner on branch push, and the OWNER merged PR #9 — the EPIC-007 epic+contract commit `a410813` — from that banner via the web UI). Standing disposition: reviewed+approved work merges directly to `main` per §14, and the fetch-before-merge lesson above is standing law (PRs #5/#6 EPIC-004, #9 EPIC-007).
- **Wrapper-referencing pins are tautological (REVIEW-TASK-031 MINOR-1):** a pin asserting `output == wrapper(deeperWrapper(...))` proves only internal consistency — any shared bug cancels itself, and the tautology survived a green suite and a first-pass read. Pin shape against the engine's OWN public API with LITERAL arguments, and pin the constant itself: `seed == DaySeed.make(petID: literal, localDayKey: literal, epoch: 1, salt: .choreography)` plus `choreographyEpoch == 1`.
- **Schedule-then-await placement law (REVIEW-TASK-031 MINOR-2):** in an executor that keeps ONE cancellable schedule cancelled-and-re-derived per apply, derive the new schedule BEFORE any awaited effect — under two racing applies the survivor must be the schedule the LATEST apply to start derived (each plan reads the freshest state at its own start), never a one-event-stale resume. General rule: an ordering decision in an async apply loop must name the race it resolves.
- **`choreographyEpoch` is single-sourced** at `AppModelPlanCore.choreographyEpoch = 1` (the app layer's one seed-epoch site, the `copyEpoch` precedent) — re-points when the idle-variant catalog lands its own epoch convention.
- **llvm-cov profdata orphaning (2026-09-10):** a plain `swift test` after a coverage run rebuilds the xctest binary → "profile data may be out of date - object is newer". Re-run `swift test --enable-code-coverage` and measure immediately (bundle path + column layout recorded under Test Status).
- "(this commit)" convention: task files record `(this commit)` in Completion Evidence; the real hash lands in status.md's Recent Commits via the housekeeping commit (see `39d6bab`/`1746a98` precedent).
- **Reset-marker placement law (TASK-040 F-1, standing):** the §6.6 erase marker lives OUTSIDE the store tree — `StoreRules.watchResetMarkerDirectory()` = the Application Support root — and BOTH production sites (launch load via `MomoAppModel.loadResetMarkerEraseCount()` in `MomoAppModel+Watch.swift`; erase save in `MomoAppModel+Settings.swift`) MUST derive through that ONE rule. Guard 4 (`markerDirectoryViolations`) pins it structurally (comment-stripped, both-direction fixtures, EXACTLY-ONE-red bite). Never point either site at the store tree — that exact divergence was REVIEW-TASK-040's F-1 HIGH (the marker could never survive a relaunch; the suite was blind to it, Probe C). **Watch-side consumption LANDED (TASK-041):** `WatchResetConsumption.decide` (pure, count-based: incoming > consumed → wipe + settle + record; ≤ → no re-wipe) + `MomoWatchSnapshotPersister.consumeWipe` fusing wipe-then-record in ONE mailbox step; the journal-wipe leg is TASK-042's (no journal exists on the Watch yet). The sync-state load at init reads the store tree via `SyncStateStore(directory:)` — consistent with its persist path by CONVENTION, not yet guard-pinned; a later contract may extend guard 4 to it.
- **Executor line budget (TASK-040 F-8, standing):** `MomoAppModel.swift` stands at EXACTLY 800/800 — zero headroom. Every further executor change lands in same-target extension files (the `+Settings`/`+Canvas`/`+Celebrations`/`+Watch` pattern) riding the `internal` seams annotated in the main file.
- **Structural-guard family: 25 members** (12 post-TASK-039 + TASK-040's guard 4 marker-directory + TASK-041's three Watch scans — persist-on-receive census / no-engine tokens / AOD-glyph branch, in `MomoWatchGlanceScanTests`+`WatchGlanceScan` alongside `MomoWatchWiringScanTests` at 21 tests — + TASK-042's three in `MomoWatchPatScanTests`+`WatchPatScan` — journal single-writer census / pat-flow order + single send leg / haptic-seam census — + TASK-043's three in `MomoWatchQuestScanTests`+`WatchQuestScan` — quest-slot census / recompute-legs census / no-settings census — + TASK-044's three in `MomoWatchSweepScanTests`+`WatchSweepScan` + `MomoWatchClockScanTests`+`WatchClockScan` — the launch-sweep arm/flush order + count-recheck pins, the two-leg send census, the D20 ambient-clock banned-token scan; R3's bare-spelling widening and R6's two-leg enumeration extend EXISTING members).
- **Baselines post-TASK-044:** `swift test` **1161/113**; MomoKit **90.84 % lines** (floor 80); MomoCore 97.68 % unchanged; app UI suite 35/35 unchanged; **MomoWatchUITests 9/9** (new baseline); catalog epoch 4 unchanged (TASK-044 added ZERO copy keys — the all-done line rides the existing shared key pending the owner's D2/F-1 ruling); `MomoWatchAppModel.swift` 761/800; `watchResetMarkerEraseCount` rides every context while non-zero until the Watch's consumed-count catches up (consumption + journal-wipe live since TASK-041/042).
- **Same-residue copy-epoch trap (REVIEW-TASK-033 NOTE-2; LANDED in TASK-034 as predicted):** a pool bump REQUIRES the §4.10 epoch bump, but the epoch bump only moves `LineSelection`'s draw SOMETIMES — a SAME-RESIDUE bump (epoch 2→3 also draws index 2 for the `CopySelectionPinnedTests` fixture pet/day over ten-line pools) leaves `rawKeysPinned` GREEN while `poolCountsPinned` and the dedicated `copyEpochIsThree` literal fail. TASK-034's mutation bite EXECUTABLY confirmed the shape (epoch flip 3→2 → exactly `copyEpochIsThree` failed, `rawKeysPinned` stayed green). **TASK-035's epoch 3→4 is the OPPOSITE shape — NOT same-residue:** epoch 4 draws index 7 (seed `0xda4187ce48ff0bb7`, draw `0xfdb4bc3ce6f541d3`), so the ten-line slot pins MOVE `.02`→`.07` (green suites go red — do not misread the re-pins as a defect), while the touch pin `.02` SURVIVES (mod5=2) and a pool-6 care-moment pin would be `.01` (mod6=1). Epoch 5 reserve (if a future task draws): seed `0x658c91a15edf4503`, mod10=9 / mod5=4 / mod6=5 — RETIRED for TASK-036 (the moment namespace adjudicated FIXED; epoch stays 4). Derivation replica validated against epochs 2/3's known-correct values before trusting 4/5.
- **ADR-prose obligations must be lifted into contract R-items (REVIEW-TASK-041 F-1, standing):** ADR-014 specified a DEBUG-loud nil-character log, but the obligation lived only in the ADR's prose — no R-item, no test, no guard traced it, so the implementation degraded silently and nothing went red. The behavior was correct and pinned; only the diagnostics leg fell through. Rule: when an ADR (or any normative record) specifies a build-time behavior, the contract must lift EACH clause into a numbered requirement with its own verification, however small — prose obligations do not self-execute. (F-2's "skipped vs held" wording divergence is the same species: the ADR's prose described a render shape the view deliberately improved on, unrecorded.)
- **Green suites are structurally blind to view→rig WIRING (TASK-034 F-1, standing):** the implementation's `reactionMotion()` factory was correct and fully headless-tested, but NO call site existed — `HomeView` omitted the argument and the frozen identity default served every frame, so nothing ever rendered while every suite stayed green (UI tests asserted navigation/labels/no-alerts, not rendered motion; headless tests sampled the director directly). Rule: any wiring leg whose failure mode is "the default silently serves" needs a STRUCTURAL GUARD — read the view source in a package test and pin the exact construction (the `RigDisciplineTests.mentionsHomeReactionWiring`/`homeWiresReactionMotion` shape, non-vacuity pinned in both stub directions, mutation-bitten). A rendered-motion UI assertion was REJECTED as structurally flaky (XCUITest cannot see canvas pixels; the idle animation defeats screenshot diffing) — that guard remains routed to TASK-039 to attempt there, with the structural guard as the accepted fallback.
- **A comment contradicting its code is a §25 defect even when the behavior is benign (TASK-034 F-2):** `.inactive` routed through `backgrounded()` beneath a doc comment saying it triggers nothing — the behavior was defensible but the claim didn't match the code, and a partial disclosure didn't cure it. Fix shape: split the case to match the normative table (`.background` only runs backgrounding), rewrite the comment to state the rationale, and disclose the behavioral delta honestly (here: a mid-`.inactive` boundary now proceeds to its fold — adjudicated acceptable: consumed not replayed, `@MainActor` serialization, idempotent catch-up).
- **Subagent completion notifications can be lost (TASK-033 review, 2026-09-10):** the reviewer agent went idle twice with its work finished but its completion notification unprocessed — do not wait indefinitely on a background-task notification: read the agent's evidence/log files directly (its /tmp workspace) and ping it with the concrete facts so it closes in one round.
- **Concurrency-test discipline (TASK-021 flake, 2026-09-09):** Swift actor job execution AND task-group child scheduling give NO FIFO guarantee — only the total order itself. Never pin WHICH element of a concurrently-submitted workload lands last; pin σ-independent invariants (membership, distinctness, and — with a stepping test clock — exact tick ADJACENCY, which additionally proves the convergence / no-silent-loss clause: k completed saves ⇒ last tick at base+(k−1), so a frozen or dropping store shifts the arithmetic). A suite that can fail under machine load is itself a defect (§19): reproduce deliberately with a repeated-run loop, record the green streak (counts + logs) before committing, and treat a CHANGES_REQUIRED from a delta-verification as a gate, not a formality.
- **SwiftUI modifier order (TASK-026 MINOR-1, 2026-09-09):** SwiftUI modifiers apply to the content IN LISTING ORDER — first-listed applies FIRST (it is the innermost wrapper); last-listed applies LAST. So `.offset(...).rotationEffect(...).scaleEffect(...)` = translate, then rotate, then scale, and stage triplets listed `[body, head, ear]` run ANCESTOR-first. Both intuitions ("outermost = listed first" and "outermost = listed last") are easy to get backwards — the orchestrator did exactly that during verification and was corrected only by a numeric probe (breath-only EXACT-equal; head6°+ear10° Δ(0.556, 6.01)). Rule: settle composition-order questions with a numeric point-mapping probe against the real algorithm, never by re-deriving SwiftUI semantics from memory; and keep one channel the SOLE non-identity freedom until the two compositions are reconciled (TASK-027 blocking item).
- **Vision-transport stale-cache rule (TASK-026 Disclosure 10, re-confirmed):** on this transport, a vision read that contradicts a pixel probe is a cache artifact, not a render defect — fresh-URL re-encodes of byte-different images produce the real reads. Pixel probes remain ground truth for evidence adjudication.
- **Real decoders only for image evidence (TASK-029, 2026-09-10):** adjudicate PNG evidence through a real decoder (ImageIO/CoreGraphics) — the orchestrator's hand-rolled Python zlib/unfilter reader reported false `gray=False` on 100 %-pure-gray evidence; the decoder was the bug (a real ImageIO decode returned nonGray=0 on every sampled PNG). Extends the vision-transport stale-cache rule: when a probe contradicts a verified claim, suspect the probe's tooling first.
- **Disposition diagnosis rule (TASK-022 NITPICK-1, 2026-09-09):** a reviewer's finding CLASSIFICATION is itself an unverified claim — verify the diagnosis personally before applying the recommended disposition. TASK-022's NITPICK-1 was filed "prose-only, the code is Set-first" when the code was actually Set-LAST (the prose was right and the CODE was the defect: duplicates at the window boundary silently dropped a distinct day). The orchestrator's live bite-proof (revert the fix → strengthened pin fails; hash-proven restore) settled which side was wrong. Rule: for any non-obvious finding, reproduce the claimed defect or its absence with a discriminating test BEFORE dispositioning; "mechanical/prose-only" is not a reason to skip that check. (TASK-026 exercise: the same rule cut BOTH ways — MINOR-1's "comments are false" survived the orchestrator's probe, and the orchestrator's own earlier "equivalent" derivation did not. TASK-029 exercise: dead-code suspicion about `reduceMotionPlayMotion`'s post-payoffEnd branch was resolved by reading the director's :712-718 prune — the held concern was a non-finding, and the proof is recorded in the task file.)
- **Contract-phrase precision (TASK-028 MINOR-3, 2026-09-10):** an ambiguous contract sentence is itself a defect seed — TASK-028's "a newer L3 displaces queued L3s" phrasing seeded a VACUOUS implementation (queue machinery that could never engage; every arrival superseded). The implementer faithfully built the ambiguity. Rule: every normative contract law must name its OBSERVABLE (what a test can pin) — queues need formation semantics, not just bounds. The tightened TASK-028 queue law (chain-in-turn, ≤ 2 pending, drop-oldest absorbed + re-chained, L2/hide clears silently, exactly-once per rendered run) is now the standing shape for any future bounded-queue law. Related TASK-028 lesson: the fixer's self-caught re-chain defect (survivors chained to the DROPPED slot's end → dead air) was caught by chain PINS, not inspection — formation pins are the teeth that make queue laws real.
- **Record-count propagation + executable recipes (TASK-030, 2026-09-10):** two record-accuracy defects in a deliverable whose entire purpose is accuracy: (1) a wrong count recorded in one task record ("11 corpus + 7 adversarial" twin streams; actual 13 = 8 named + a 5-value hold sweep) PROPAGATED verbatim into the next task's records and the epic — when a recorded number is found wrong, grep for it across `.claude/tasks/` and fix every carrier at once; (2) a task file's own mutation-bite recipe can be INERT against the test it advertises (the recorded `yawnEnabled`-only flip left the §3.5 anchor green because `headNodEnabled` is the same Drowsy-only gate) — verify a recorded recipe by EXECUTION before recording it, and name every load-bearing gate. The §3.5 anchor itself survived both: the two-gate bite fails on exactly the predicted Low+Drowsy/Low+Exhausted pair (Low's 0.95 × 0.97 = 0.9215 clamps back to the 0.95 posture floor — the unique-pair mechanism).
- **Fold-director architecture (TASK-028, standing; extended TASK-029):** character interaction is a PURE timeline-injected fold (`MomoReactionDirector.plan(events:at:)` → overlay + reports), twin-equal under whole-trajectory comparison, reports emitted exactly-once at the first fold past each completion instant (never wall-clock). Engine tolerance covers the fold latency. Any future character-layer timing work composes through this fold — do not add imperative animation state outside it. Reduce Motion composes as a RENDER-ONLY policy alongside the fold (`MomoReduceMotion.swift` reads the SAME folded state; the fold itself takes no flag) — never add RM branches inside the fold.
- **`FileManager.moveItem` does not replace (TASK-023 MAJOR, 2026-09-09):** `moveItem(at:to:)` fails Code-516 over an existing destination — an atomic-rename commit point must either VACATE the name first (SnapshotStore's generation rotation) or use `replaceItemAt` when the destination exists (SyncStateStore's replace-when-present/move-when-absent; IntentJournal.prune's replace). Any future persisted-file writer in this repo must pick one of those two shapes and pin the double-save. EPIC-008 routing (REVIEW-TASK-023 O1+O5): sync wiring must keep ONE writer per journal/sync-state file on a single dispatch path (append is a whole-file read-modify-write; temp names are fixed), and must apply intents through `SyncState.shouldApply` (the accessor-driven epoch-scoped wrapper) — never resolve watermarks at call sites via the raw `WatchSyncGate`.
- **INV-10 property shape (REVIEW-TASK-023 O2, adjudicated):** exactly-once is pinned under the §6.1 FIFO contract stream (+duplicates/replays/redeliveries); under seeded shuffles the guard legitimately BLOCKS unseen laggards (seq 5 first → watermark 5 → seq 3 blocked by `>`), so the suite pins at-most-once there. Do not "strengthen" the shuffle pin to full application — that pins a falsehood against §6.4.
- **REVIEW-TASK-024 routings + standing obligation (2026-09-09):** (1) **OBS-1 routing** — two 05 §5.2 save/load elements are untestable headlessly and were routed by the audit, never dropped: "main thread never blocks on I/O beyond launch's initial read" → app-layer property owned by **EPIC-007/EPIC-008** (the store is an actor, serialization by construction and pinned); "NSFileProtectionComplete" → **EPIC-002 VERIFY-AT-BUILD register** (app-target entitlement, outside MomoKit). (2) **OBS-3 standing obligation** — the NFR-7 parity pin `migratedStateEqualsTheFreshlyBuiltState` has teeth ONLY over the populated fixture fields (days ledger, settings, pet identity, stamps, PetState dimensions); the fixture carries `processedIntents: []` / `pendingHandshake: nil` / `lastGreeting: nil`, so when the FIRST real schemaVersion bump ships, that fixture MUST carry a populated belt/handshake/greeting through the walk (MINOR-1's narrowed claim becomes a test obligation then). (3) The audit-table standard is now precedent: re-derive matrices from the docs BEFORE comparing, prove one mapping's executable truth via a mutation bite.
- **Character pipeline doctrine (TASK-025, 2026-09-09):** the pipeline's `verify_geometry.py` is a SELF-CONSISTENCY gate only — it checks the source against the same landmarks it emits from, so a moved landmark passes it (mutation-proven: pipeline exit 0 while the Swift pin failed by name). The Swift pins over the committed constants are the real landmark authority; keep the double-entry (pipeline = source self-consistency, Swift pins = truth). **Winding/fill-rule questions are settled by numeric analysis** (shoelace signed areas, winding-number scans, `Path.contains` at seam points), never by visual inspection — vision models self-contradicted across all three evidence PNGs while pixel forensics (zero background pixels in the disputed junction rects) and winding math agreed; the orchestrator's own crop-based visual read of M2 was wrong and is recorded as a verification miss in the task file. The committed evidence PNGs are byte-exact projections of the committed constants (re-render + hash-compare), which is what makes pixel forensics authoritative for the shipped bytes. Nonzero fill rule: counter-wound overlapping subpaths cancel (winding 0 = a hole); `HOLE_COMPOUNDS = ("window",)` is the one intentional hole compound.
- **TASK-025 routings — carried obligations:** **O7** (ADR-001's "no bounce-loops anywhere" is an animation-layer rule — pin it explicitly in TASK-026's contract); **O2** (eyes read drowsy to an independent judge even after the lid fix — verify the calm-vs-asleep margin on real device surfaces in TASK-026+); **O3** (tail reads weak at evidence scale — refinement candidate in TASK-026+); **O6** (04 §3.1 lower-lid pose shapes relaxed/upturned/flattened must land as constants in TASK-027's animation layer); **O4** (glyph legibility must be re-judged at true 24–32 pt in the complication surfaces → EPIC-008); **O8** (budget-bucket basis: the rig bucket counts `MomoRig+*` only; the hand-written `MomoRig.swift` anchor sits outside all buckets — understand the basis when reading budget numbers); O5 (room-composition nits: sparkle/window collision fixed on canvases; rug/window contrast noted) recorded, no routing.
- Branch model: each epic on `feature/EPIC-00X-slug` cut from `main`; one atomic commit per task, pushed after each task; epic merges to `main` at DoD (delivery plan §1.5, §6.1). EPIC-002's branch now exists with TASK-008 as its first commit.
- EPIC-002 is DONE — downstream tasks inherit: package (`Sources/<Module>` + `Tests/<Module>Tests`), pbxproj with TestAction-equipped schemes, live scanners (import whitelist + banned-vocabulary, now actively scanning `Apps/Shared/MomoCopy.xcstrings`), design tokens in `MomoCharacter` (hex ONLY in the 2 palette files — keep it that way; `TokenPurityTests` enforces), copy lookup via `CopyKey`/`MomoCopy` with injected bundle (new UI code must not touch `Bundle.main` for copy or hardcode hex). Engine/read-model tasks own real copy pools (index ≥ 01). Known watchOS lesson: `xcodebuild build` does NOT install to the simulator — `simctl install` before `simctl launch`/screenshots; flexible aspect-ratio shapes can collapse in tight watch VStacks (use deterministic frames). All five ADR-008 matrix simulators are provisioned and available.
- Coverage floors (Core 90 %, Kit 80 %) recorded per task, enforced by TASK-020/024; NO MomoCharacter floor exists — TASK-030 RECORDED the determinism-property coverage per 05 §10.2 (audit tables A–E + the pure-file record + the §8.3 budget re-measure).
- Philosophy guardrail: Cute × Calm × Minimal × Alive × Premium. No punishment. MVP scope protection (§22/§24); scope-creep proposals route to KEEP/LATER/REJECT with the orchestrator (plan R10).

## §35 Health Checkpoint (2026-09-10, post-TASK-030 / EPIC-006 close)
- **Epic progress**: EPIC-006 COMPLETE 6/6 — TASK-030 (`4f8e3ac`) closed the epic; the §14 merge to `main` is the next action, then EPIC-007 authoring. **Build/test health**: 825/82 green at the TASK-030 close (impl ×3, reviewer ×2 incl. the two-stage bite restore, orchestrator incl. the §19 gate); zero warnings; production diff ZERO through the whole audit cycle (`Sources/MomoCharacter/` sha256-verified pristine before/after the sanctioned bite). **Architecture drift**: none — TASK-030 was audit-only; the reviewer body-read every table-C row and found no existence-grepped mappings; frozen modules untouched.
- **TODO debt (§26)**: census CLEAN — zero TODO/FIXME/HACK/XXX/TEMP in Sources//Tests//Tools (re-census 2026-09-10 08:56 UTC; TASK-030's diff hygiene grep introduced no markers).
- **File budgets**: largest file 792/800 (director); MomoReduceMotion.swift 516; all others ≤ 500. Art budgets re-measured at close (exact `getsize` bytes, reviewer-reproduced digit-for-digit): rig bucket 50,096/307,200 B (16 %; 51,906 incl. the 1,810 B `MomoRig.swift` anchor outside the bucket per O8), room+props 20,647/256,000 B (8 %), generated total 70,743/1,572,864 B (4.5 %), all-40 MomoCharacter sources 351,863 B (22 %).
- **Git cleanliness**: clean at this housekeeping commit; every epic branch retained; no force-pushes. **Task documentation quality**: REVIEW records + task files current through TASK-030; inherited miscounts corrected at ALL carriers (the TASK-029 stream counts in the epic + completed-TASK-029 records, per REVIEW-TASK-030 MINOR-2/NOTE-3). **Product scope**: held (audit task — no feature surface; no §22/§24 creep). **Technical debt**: standing Known Issues unchanged; no new entries from the checkpoint.

## §35 Health Checkpoint (2026-09-10, post-TASK-031 / EPIC-007 1/9)
- **Epic progress**: EPIC-007 1/9 — the LANE A × LANE B convergence opened on the facade (infrastructure behind the placeholder shell by design; TASK-032/033 surface the product). **Build/test health**: 851/85 green ×3 post-disposition; zero source warnings (machine-env `ld` notice disclosed); coverage floors hold (Core 97.68 %, Kit 91.42 %); BUILD SUCCEEDED + both sim launches verified (fresh-store and relaunch). **Architecture drift**: none — D-R5 held by construction (`MomoAppModel.swift` is the only app-target file importing MomoCore, grep-verified in the verification battery); frozen modules byte-untouched; the Watch push seam is a documented no-op; zero WatchConnectivity code exists.
- **TODO debt (§26)**: census CLEAN at marker level (re-census 2026-09-10 post-TASK-031; the only substring hits are `HEADER_TEMPLATE` ×2 in `Tools/character-pipeline/emit_swift.py` — the "TEMP" inside "TEMPLATE", not markers). **File budgets**: new sources well under 800 (`MomoAppModel.swift` 375, `AppModelPlan.swift` 221, `NextBoundary.swift` 190, `AppModelLaunch.swift` 43, `AppModelPlanTests.swift` 463); largest file unchanged (director 792). **Git cleanliness**: clean at this housekeeping commit; epic branch in sync through `257a58f`; no force-pushes. **Product scope**: held (§22/§24 — no visible product change this task). **Technical debt**: no new Known Issues; the Kit coverage reading superseded (91.42 % is current).

## §35 Health Checkpoint (2026-09-10, post-TASK-032 / EPIC-007 2/9)
- **Epic progress**: EPIC-007 2/9 — the front door is REAL: fresh install → onboarding S1→S2→S3 → Home (placeholder content until TASK-033 composes it). **Build/test health**: 857/86 green post-disposition; UI battery 5/5 (incl. the new onboarding UI pins); zero Swift/package warnings (the two full-build environment warnings recorded under Known Issues); BUILD SUCCEEDED + two-store launch evidence reproduced independently by the reviewer. **Architecture drift**: none — D-R5 held (`MomoAppModel.swift` still the only app file importing MomoCore; views bind through the model); frozen modules byte-untouched (`Sources/MomoCore/`, `Sources/MomoCharacter/`); `MomoCopy.xcstrings` key set unchanged (onboarding copy is authored view literals, R11); zero WatchConnectivity code; the Watch push seam remains a documented no-op. **Doc-chain**: the backlog is fully discharged (`c0ad5ec`) — OBS-C is the ONLY open doc-chain item.
- **TODO debt (§26)**: census CLEAN across the new files (reviewer-verified: no TODO/FIXME/HACK/TEMP in any TASK-032 artifact). **File budgets**: all new files ≤ 237 lines (`AppModelOnboardingPlanTests.swift`); views ≤ 97. **Git cleanliness**: clean at this housekeeping commit; branch in sync through `53e433f`; no force-pushes. **Product scope**: held — exactly the three UX §3 steps, zero extras (UX-7 no dots honored; the S1 button routes no intent, UX-6). **Technical debt**: one new Known-Issues entry (NOTE-3 environment warnings, epic-level candidate).

## §35 Health Checkpoint (2026-09-10, post-TASK-033 / EPIC-007 3/9)
- **Epic progress**: EPIC-007 3/9 — Home is COMPOSED (catalog landing + read-models + the five-region view + the UI battery); the interaction half (TASK-034 touch, 035 feed/play/care, 036 quest moments) is next, then Room/Settings (037/038) and consolidation + audit (039). **Build/test health**: 868/88 + app 10/10 green at the final tree (implementer, reviewer ×2, orchestrator §19 gate post-disposition); zero source warnings (the two standing environment warnings under Known Issues); frozen modules byte-untouched except the contracted R4 carveout. **Architecture drift**: none — D-R5 held (views bind through `MomoAppModel`; MomoKit imports MomoCore only, never MomoCharacter); no new WatchConnectivity; the Watch push seam remains a no-op. **TODO debt (§26)**: census CLEAN across all new TASK-033 files (0 markers). **File budgets**: new files 15–319 lines (largest `MomoHomeUITests.swift` 319; `HomeReadModel.swift` 215, `HomeView.swift` 132); largest source file unchanged (director 792/800). **Git cleanliness**: clean at this commit; no force-pushes. **Product scope**: held — exactly UX §5.1's S4 regions, no Collection/customization entries (AC-4 red line), WORDS never numbers. **Technical debt**: REVIEW-TASK-033 MINOR-1 routed to TASK-039; OBSERVATION-A recorded as an owner item; no new Known Issues.

## §35 Health Checkpoint (2026-09-11, post-TASK-034 / EPIC-007 4/9)
- **Epic progress**: EPIC-007 4/9 — Home is ALIVE (composition + full touch/petting interactivity); the care families (TASK-035) and quest moments (TASK-036) are next, then Room/Settings (037/038) and consolidation + audit (039). **Build/test health**: 887/91 + app 13/13 green at the final tree (implementer, round-1 reviewer, delta reviewer, orchestrator §19 gate — four independent reproductions); zero source warnings (the two standing environment warnings under Known Issues); frozen modules byte-untouched (`Sources/MomoCharacter/` diff EMPTY through the whole cycle incl. both bites); MomoCore diff = the CopyRules carveout only. **Architecture drift**: none — D-R5 held (the view touches the director only through the app model's factory); MomoKit imports no MomoCharacter; no new WatchConnectivity; the Watch push seam remains a no-op. One review-cycle event, not drift: F-1 exposed that UI suites are structurally blind to view→rig wiring — the structural-guard precedent (read the view source, pin the construction) is now in `RigDisciplineTests` and is the standing shape for future wiring legs. **TODO debt (§26)**: census CLEAN across all TASK-034 files (0 markers, round-1 + delta reviews both grepped). **File budgets**: new files 190/198/405 lines; `MomoAppModel.swift` 563/800 (largest app file); largest source file unchanged (director 792/800). **Git cleanliness**: clean at this commit; no force-pushes. **Product scope**: held — exactly UX §5.1's gesture map + UX-8's a11y surface, no petting rewards (G2 zero-bond pinned), no new chips (TASK-035's). **Technical debt**: N1 (superseded-maturation cancel, cosmetic) carried in the task file; O1 apostrophe normalization rides TASK-035; rendered-motion UI assertion routed to TASK-039; no new Known Issues.

## §35 Health Checkpoint (2026-09-11, post-TASK-035 / EPIC-007 5/9)
- **Epic progress**: EPIC-007 5/9 — the care-interaction loop is COMPLETE (composition + touch + feed/play/care); quest moments + celebrations (TASK-036) are next, then Room/Settings (037/038) and consolidation + audit (039), then the §14 merge. **Build/test health**: 907/92 + app 17/17 + Watch build green at the final tree (implementer, round-1 reviewer, delta reviewer — three independent reproductions of the package gate); zero warnings from touched files (F-3 pre-existing `MomoUITests.swift` debt disclosed and routed); frozen surfaces held (`Sources/MomoCore/` = the CopyRules carveout ONLY; `Sources/MomoCharacter/` = the two disclosed R6 files ONLY; pbxproj byte-identical with HEAD). **Architecture drift**: none — D-R5 held (everything routes through `MomoAppModel`); MomoKit imports no MomoCharacter; no WatchConnectivity; the Watch push seam remains a no-op. The R6 seam was the epic-pre-authorized ONE `MomoCharacter` touch, disclosed and minimal (+42 lines across two files, exhaustive no-default switches forcing the census). **TODO debt (§26)**: census CLEAN (the impl's diff hygiene grep found no markers; both reviewers swept). **File budgets**: `MomoAppModel.swift` grew (drain + fingertip + pill window + care-moment memory — largest app file; still ≤ 800); director unchanged at 792/800. **Git cleanliness**: clean at this feat commit; no force-pushes. **Product scope**: held — exactly FR-6/7/8 + UX §5.2–5.4, no meal UI, no cooldown/currency (D18), no new pills, no rendered react text (rule 7), nap-label glass-pin gap honestly disclosed instead of faked (§25). **Technical debt**: F-2 (instance-state double-start guard) recorded in the task file; F-3 + N-3 added to Known Issues; no new owner items (the care-moment retention rides the existing OBSERVATION-A owner item).

- **2026-09-11 checkpoint (post-TASK-036)**: **Epic progress**: EPIC-007 6/9 — the Home experience is emotionally complete (composition + touch + care + quest moments + celebrations); Room (037) and Settings (038) remain, then consolidation + audit (039), then the §14 merge. **Build/test health**: 925/94 + app 21/21 + Watch build green at the final tree (implementer, §33 reviewer pre- and post-bite, orchestrator §19 gate — three-plus independent reproductions); zero warnings from touched files; frozen surfaces held (`Sources/MomoCore/`, `CopyRules`, `LineSelection`, `MomoMoments`, `MomoReduceMotion` source all diff-absent; MomoCharacter = the two disclosed R1 files only). **Architecture drift**: none — the two-doors moment architecture landed exactly as adjudicated (greetings displayState-born, event moments FIFO'd at the delivery door); UX-10 once-semantics stayed engine-owned (presentation stateless); D-R5 held; the Watch push seam remains a no-op; MomoKit imports no MomoCharacter. **Structural-guard family**: now 8 members (R9a-g + the TASK-034 wiring guard) — F-2's finding (a guard blind to one mutation shape) is the family's known trade; the negative-probe practice is now standing reviewer methodology. **TODO debt (§26)**: census clean (reviewer duty-F swept). **File budgets**: director grew but stays ≤ 800 lines; new files small (`QuestMomentSupport` ~100 lines). **Git cleanliness**: clean at the closeout commit; no force-pushes. **Product scope**: held — M1 auto/no-claim/no-modal, M2 once-ever per stage with the engine's `highestCelebratedStage` owning once-ness, M3 warm note (no gating, per PRD §5.4 MUST NOT gate); haptics calm (light impact / success notification only, settings-gated). **Technical debt**: F-2 → TASK-039 (verbatim routed); F-3–F-6 review notes are records only; the doc-errata note for 05 §4.9's moment selection-class rides the orchestrator backlog.

- **2026-09-11 checkpoint (post-TASK-038)**: **Epic progress**: EPIC-007 8/9 — all three tabs are REAL (Home alive, Room static, Settings = the full FR-19 surface); TASK-039 (consolidation + audit) remains, then the §14 merge. **Build/test health**: 945/96 + app 27/27 + Watch build green at the exact reviewed tree (implementer's five gates, §33 reviewer's ×2 + bites + probe + build-for-testing, orchestrator's restoration re-verification + §19 gate); zero warnings from touched files; frozen walls held (`Sources/MomoCore/`, `Sources/MomoCharacter/` diff-absent; epoch 4 untouched; MomoKit touched only at `AppModelPlan` + the new `SettingsCopyKeys`). **Architecture drift**: none — settings changes ride the plan core (pre-engine triggers, the `onboardingCompleted` precedent extended to 8); erase is the one sanctioned executor-level store lifecycle (launch-path-symmetric, delete-first, no-persist; reviewer independently re-derived the adjudication); D-R5 held (views never touch `AppModelPlan`); the Watch push seam remains a no-op. **Structural-guard family**: now 10 members (+ the settings inventory/forbidden-vocab guard + the erase-sequence guard); the attachment-blindness trade record now has TWO probes against it (TASK-037 P-1, TASK-038 P-1) — both caught by design with glass backstops; hardening cluster (037 N-1 + 038 N-1 + 036 F-2) routed to TASK-039. **TODO debt (§26)**: census clean (both agents' sweeps; only "TEMPLATE" prose matches). **File budgets**: NEW files all small (`SettingsView` 161, `MomoAppModel+Settings` 82, `SettingsCopyKeys` 73, `MomoSettingsUITests` 342); **`MomoAppModel.swift` 948 ln — over the 800 budget (888 pre-existing + 60 disclosed extension seams) — the TASK-039 consolidation candidate now REAL**. **Git cleanliness**: clean at this closeout commit; no force-pushes. **Product scope**: held — FR-19 exactly (four-row inventory, sound absent by the doc's own conditional, MUST-NOT red lines guard-enforced); catalog 113 keys, D12 held (zero literals in views). **Technical debt**: N-1 → TASK-039 (rides F-2 + 037 N-1); N-2/N-4 records; N-3 cosmetic → TASK-039 sweep candidate; `MomoAppModel.swift` budget → TASK-039; no new Known Issues.

- **2026-09-11 checkpoint (post-TASK-037)**: **Epic progress**: EPIC-007 7/9 — all three tabs are REAL (Home alive, Room static, Settings the only placeholder); TASK-038 (Settings + rename + erase) remains, then consolidation + audit (039), then the §14 merge. **Build/test health**: 931/95 + app 22/22 + Watch build green at the final tree (implementer, §33 reviewer ×2 runs + bites, orchestrator §19 gate); zero warnings from touched files; frozen surfaces held (`Sources/MomoCore/`, `Sources/MomoCharacter/`, `CopyRules`, `LineSelection` all diff-absent; MomoKit touched only at `HomeCopyKeys`). **Architecture drift**: none — the room is deliberately state-free and clock-free (static means static); D-R5 held; the Watch push seam remains a no-op. **Structural-guard family**: now 9 members (R9a-g + the TASK-034 wiring guard + the R5 room guard); N-1 (attachment blindness) joins the scan-family trade record — the glass UI test is the designed backstop, and the negative-probe practice again proved its worth (P-1 caught the guard's envelope, not the implementation). **TODO debt (§26)**: census clean (reviewer duty swept; only "TEMPLATE" prose matches). **File budgets**: `RoomView.swift` 121 lines; all new files small. **Git cleanliness**: clean at this closeout commit; no force-pushes. **Product scope**: held — FR-3 exactly (static scene, zero interactivity, no customization UI, AC-4 red line untouched); tab labels catalogized (D12), zero hardcoded strings in `RootTabView`. **Technical debt**: N-1 → TASK-039 (rides F-2); N-2/N-3 records only; no new Known Issues.

## §35 Health Checkpoint (2026-09-11, post-TASK-039 / EPIC-007 9/9)
- **Epic progress**: EPIC-007 COMPLETE 9/9 AND MERGED — TASK-039 (`614053d`) closed the epic; the §14 merge landed as `b73e3eb` (owner PR #16 `ad0199e` reconciled — the remainder was 2 commits; merged tree content-identical to the branch tip, 949/97 green on the merged state, pushed `ad0199e..b73e3eb`); EPIC-008 authored READY (0/5) on `feature/EPIC-008-watch-sync`. **Build/test health**: 949/97 + app suite 35/35 + Watch build green at the final tree (implementer, §33 reviewer incl. 6 bites + 1 disclosed aborted probe, orchestrator §19 gate — three-plus independent reproductions); zero warnings from touched files; frozen walls held (`Sources/MomoCore/`, `Sources/MomoCharacter/`, `Apps/Shared/MomoCopy.xcstrings`, `docs/` all diff-absent; epoch 4 untouched; zero new copy keys). **Architecture drift**: none — the R8 split is mechanical move-semantics (every removed block verbatim in `+Canvas`/`+Celebrations`; exactly 7 access-level insertions); D-R5 held; the Watch push seam remains a no-op. **Structural-guard family**: now 12 members (+ the lineLimit census + the status-row declaration-order pin); the attachment-blindness trade record gained its FIRST CURE — 037 P-1 and 038 P-1 both RE-BITTEN post-hardening (B-3/B-4), discharging the hardening cluster (036 F-2 + 037 N-1 + 038 N-1) with teeth; the residual blindness (N-4's closed census file list) routed to the guard-family follow-up. **TODO debt (§26)**: census clean (the reviewer's raw grep over every touched file — zero real markers). **File budgets**: `MomoAppModel.swift` 775 + `+Canvas` 128 + `+Celebrations` 117 — all ≤ 800, the 948 overage CURED; `RigDisciplineTests.swift` 1032 ln (test file; the 800 budget is authored for production files — orchestrator observation, optional split). **Git cleanliness**: clean at this closeout commit; no force-pushes. **Product scope**: held — consolidation + audit evidence + three launch-blocking FR-20 remediations (rename-field label, `.isStaticText` on composited display text, AX-conditional line caps), zero product-surface or copy change. **Technical debt**: N-1/N-2 recorded rides-next-touch, N-4 guard-family follow-up, F-2 owner item (gates EPIC-008's state-in-words template), the on-device VoiceOver rotor walk remains the ONE manual audit gap.

## §35 Health Checkpoint (2026-09-12, post-TASK-045 / EPIC-009 1/6)
- **Epic progress**: EPIC-009 1/6 — the entire 05 §12 budget matrix is now MEASURED with recorded evidence (25-file set under `.claude/tasks/evidence/TASK-045/`); 5 rows PASS on sim surfaces, R2/R6 device legs BLOCKED per §25 with structural attestations recorded, no budget value edited anywhere, ZERO production diff. **Build/test health**: 1161/113 green (orchestrator re-run at closeout); both builds + Watch UI 9/9 re-run by the reviewer; 35/35 app UI CITED on unchanged inputs. **Architecture drift**: none — docs/evidence only; frozen surfaces diff-empty by tree identity. **Git cleanliness**: clean at the closeout; no force-pushes. **Disk**: the host volume is the standing risk (~790 MiB free at review close; owner notified; the ModuleCache deletion disclosed in Known Issues). **Technical debt**: none added; the owner device-pass backlog grew by TASK-045's BLOCKED rows (R1 device launch, R2 fps trace, R3/R4 device memory, R6 energy gauge, R8 stopwatch, R9 physical pair, R7 physical Watch). **Next**: TASK-046 (§10.5 edge-case matrix execution) — the last pre-audit engineering leg before the a11y/privacy/final-test/release-gate sequence.

## §35 Health Checkpoint (2026-09-12, post-EPIC-008 §14 merge — 7 of 7 engineering epics COMPLETE)
- **Epic progress**: EPIC-008 COMPLETE 5/5 and MERGED (`d460a24`, pushed `fffa6cf..d460a24`) — ALL 7 engineering epics (002–008) plus all product-definition epics are DONE and merged; the remaining Phase-1 scope is EPIC-009 — Release Readiness (TASK-045…050). **Build/test health**: 1161/113 green on the MERGED state before push (the §14 gate; threefold reproduction on the branch [implementer + §33 reviewer + orchestrator] plus the orchestrator's merged-state re-run); merged tree content-identical to the branch tip; both builds, UI 9/9, and coverage 90.84 % records carry over by tree identity. **Architecture drift**: none — the Watch runs no engine; O1 one-writer; D20 injected clocks; D-R3 no app-to-app dependency; §27 zero entitlements; the 25-member structural-guard family held throughout. **TODO debt (§26)**: clean at the TASK-044 close (reviewer swept every changed file). **Git cleanliness**: one `--no-ff` merge commit; zero force-pushes ever. **Product scope**: held across the epic — W1 glance, offline pat, live cascade, and the erase-reset chain shipped with no game-heavy drift; non-MVP ideas remain recorded, not implemented (§24). **Open owner items (§36, non-blocking)**: the three device-pass obligations (WC-frame-under-suspension on paired hardware; live AOD; haptic FEEL); the D2/F-1 W1 all-done copy ruling (still OPEN — honestly recorded at merge; UX §9's iPhone M3 row misquotes the PRD string against §5.5 either way); the state-in-words template delta; TASK-029 RM acknowledge-static; the Content+Energetic static collision; OBSERVATION-A greeting domination; E4 name clearance at TASK-050; the ADR-015 heart-delta visual-design backlog. **Technical debt**: the guard-family hardening cluster (incl. REVIEW-TASK-044 F-3 `await`-spelling pin), stale `AppModelPlan.swift` doc comments, the UX §6.1 "~40 %" doc-erratum, the sync-state init-load convention note, NOTE-1 scanner extensions; MomoKit coverage trend 92.47 → 91.48 → 90.79 → 90.81 → 90.84 % (floor 80 holds).

## §35 Health Checkpoint (2026-09-12, post-TASK-044 / EPIC-008 5/5 COMPLETE)
- **Epic progress**: EPIC-008 COMPLETE 5/5 — TASK-044 (`491777f`) closed the epic: the §10.4 matrix is fully audited with its two real gaps closed (the launch-sweep drain; the erase-reset Watch-UI E2E), the three device obligations are honestly BLOCKED-recorded to the owner's device-pass backlog, and the F-2/F-3 guard ride-alongs from TASK-043 landed. The §14 merge to `main` is the next action, then EPIC-009 authoring. **Build/test health**: 1161/113 + both builds zero touched-file warnings + MomoWatchUITests 9/9 at the final tree (implementer, §33 reviewer incl. 4 own sha256-restored probes, orchestrator §19 gate — three independent reproductions); frozen walls held (`Sources/MomoCore/` + `Sources/MomoCharacter/` + `Apps/Momo/**` + `Apps/Shared/` + `Momo.xcodeproj/` all diff-EMPTY; pbxproj untouched — the sweep seam is package-side additive). **Architecture drift**: none — the sweep arms at activation and flushes at the two receive decision legs after the awaited consumption (the reviewer adjudicated arm-then-flush as the contract's own compliant reading); the Watch still runs no engine; O1 one-writer held (`pendingEvents` rides the persister extension); D20 enforced structurally for the first time Watch-wide (the banned-token scan); no timers. **Structural-guard family**: 25 members (+3: the sweep arm/flush + count-recheck pins, the two-leg send census, the ambient-clock banned-token scan; R3/R6 extended existing members). **TODO debt (§26)**: census clean (implementer + reviewer sweeps). **File budgets**: `MomoWatchAppModel.swift` 761/800; new files small (`WatchSweepPlan.swift` 56 ln, Transport 162, Persister+Journal 128). **Git cleanliness**: one atomic feat commit `491777f` (amended once while unpushed — the git-mv index-blob catch, lesson recorded under Important Context); no force-pushes. **Product scope**: held — audit + test-coverage + guard task; zero copy keys, zero UI surface change, zero scope creep; the three device obligations honestly BLOCKED (§25), nothing sim-claimed. **Technical debt**: F-3's `await`-spelling tightening → the guard-family hardening cluster; F-2's consume-suspension window accepted as an honestly-disclosed residual (warm-apply class); the D2/F-1 all-done copy ruling remains the one owner-gated item, recorded honestly at merge. **MomoKit coverage**: 90.81 → **90.84 %** (floor holds).

## §35 Health Checkpoint (2026-09-12, post-TASK-043 / EPIC-008 4/5)
- **Epic progress**: EPIC-008 4/5 — the first vertical slice is now LIVE end-to-end: the Watch quest line renders the shared cascade re-run over the snapshot's carried quest inputs under the Watch's local hour at four event legs (no timers), provenance-pinned against the real builder chain, and the UX-13 guards close the surface (display-only slot, no settings view). Only TASK-044 (§10.4 matrix + erase-reset E2E + F-R1 launch sweep + F-2/F-3 guard rides + hardware obligations) remains, then the §14 merge. **Build/test health**: 1128/110 + both builds zero touched-file warnings + MomoWatchUITests 7/7 at the final tree (implementer, §33 reviewer incl. 3 own sha256-restored probes, orchestrator §19 gate — three independent reproductions); frozen walls held (`Sources/MomoCore/` + `Sources/MomoCharacter/` + `Apps/Momo/**` + `Apps/Shared/` + `Momo.xcodeproj/` all diff-EMPTY; pbxproj untouched — the seam is package-side). **Architecture drift**: none — the cascade is ONE shared derivation consumed through a zero-logic seam; the Watch still runs no engine; the recompute is event-driven only with injected clocks (D20); `MomoWatchAppModel.swift` 600/800. **TODO debt (§26)**: census clean (reviewer swept every changed file). **File budgets**: all touched/new files ≤ 600 ln; `GlanceView.swift` 274 ln. **Git cleanliness**: one atomic feat commit `0bd6044`; no force-pushes. **Product scope**: held — identical-at-0/1/2-of-3 and all-done-swap-only-on-completion come from the shared cascade (UX §6.1); no claim/complete affordance; no error/freshness surface (§10.4/UX-9). **Technical debt**: F-1 (Major) W1 all-done copy → OWNER item, resolve before EPIC-008 closeout; F-2/F-3 guard ride-alongs → TASK-044; F-4/F-5 guard tolerances + F-6 frozen-fallback note recorded; F-7 the TASK-043 contract's own Context mis-citation (orchestrator-owned — lift doc citations from verified sources, not memory). **MomoKit coverage**: 90.79 → 90.81 % (stabilized; floor holds).

## §35 Health Checkpoint (2026-09-12, post-TASK-042 / EPIC-008 3/5)
- **Epic progress**: EPIC-008 3/5 — the first vertical slice is now INTERACTIVE end-to-end: the Watch pat works fully OFFLINE (immediate micro-reaction + journaled intent with epoch-scoped monotonic seq + `transferUserInfo` drain + toggle-honoring haptic), and the iPhone applies each intent exactly once through the TASK-040 receive path. Remaining: TASK-043 (live cascade + UX-13), TASK-044 (matrix + E2E + F-R1 sweep + hardware), §14 merge. **Build/test health**: 1102/108 + both builds zero touched-file warnings + MomoWatchUITests 5/5 at the final tree (implementer, §33 reviewer incl. 3 own sha256-restored probes + an 18-file sweep, orchestrator §19 gate — three independent reproductions); frozen walls held (**`Apps/Momo/**` diff-EMPTY** — the contract's hard pin; `Sources/MomoCore/` + `Sources/MomoCharacter/` untouched; pbxproj +8 registration-only, D-R3 re-read at source). **Architecture drift**: none — O1 one-writer held (every journal touch incl. the pending-count READ routes through `MomoWatchPersister+Journal.swift`, census-guarded); ADR-015 landed as adjudicated (existing-clip sampler seam, local estimation strict-`==`, haptic types resolved at build); ADR-013 per-target duplication held. **TODO debt (§26)**: none (reviewer swept every changed file). **File budgets**: all new files ≤ 303 ln; `MomoAppModel.swift` untouched at 800/800. **Git cleanliness**: one atomic feat commit `faa8761`; no force-pushes. **Product scope**: held — haptics fire ONLY on pat + estimated completion (UX §6.3 "anything else = nothing" structurally enforced by the seam census); the heart visual stays an owner backlog item (ADR-015). **Technical debt**: F-R1 drain-gap sweep → TASK-044; F-R2 stale comments → TASK-043 ride-along; haptic FEEL on-wrist verification → TASK-044 hardware obligation (§25); MomoKit coverage 91.48 → 90.79 % (disclosed, floor holds — trend watched at TASK-043).

## §35 Health Checkpoint (2026-09-11, post-TASK-041 / EPIC-008 2/5)
- **Epic progress**: EPIC-008 2/5 — the first vertical slice is RENDER-COMPLETE end-to-end (iPhone pushes snapshots; the Watch persists + renders W1 + consumes the reset marker); the interactive half (TASK-042 pat/journal/drain, TASK-043 cascade/propagation, TASK-044 matrix/E2E/hardware) remains, then the §14 merge. **Build/test health**: 1056/104 + both builds zero touched-file warnings + MomoWatchUITests 3/3 at the final tree (implementer, §33 reviewer incl. 4 probes, orchestrator §19 gate — three independent reproductions); zero warnings from touched files; frozen walls held (`Sources/MomoCore/` diff EMPTY — epoch 4 untouched; `Sources/MomoCharacter/` untouched; `Apps/Momo/MomoAppModel.swift` 800/800 byte-identical). **Architecture drift**: none — ADR-014 landed exactly as adjudicated (additive-OPTIONAL DTO, SHARED derivation, pure kit assembly, no schema bump); ADR-013's per-target duplication held (receive-only twin, no target dependency — D-R3 re-read at source); O1 one-writer held (exactly two persist legs, census-guarded); the Watch runs no engine (guard (b) green). **TODO debt (§26)**: census clean (the reviewer swept every changed production + test file — zero markers). **File budgets**: largest new file `MomoWatchAppModel.swift` 333 ln; all new files well under 800; director unchanged 792/800. **Git cleanliness**: clean at the feat commit; no force-pushes. **Product scope**: held — exactly W1 (words + stage + quest line + inert pat targets), no numbers/bars/freshness indicator (UX-9), no pat capture yet (§22/§24), no error surface (UX §9 degrade pinned). **Technical debt**: F-1/F-2 → TASK-042 (folded into Next Tasks); F-3 UX §6.1 "~40 %" doc-erratum → backlog; F-4 cosmetic nit recorded; SE-has-no-AOD live observation → TASK-044; MomoKit coverage dipped 92.47 → 91.48 % (disclosed, floor holds — watch the trend at TASK-042/043).

## Exact Next Action
The TASK-046 implementer (`task046-impl`) IS RUNNING (R0/R1/R3 done per its tracker; §19 gates mid-run; disk-floor watch armed). **Await its §28 handoff** (final report ending with a freshly-typed marker line: HANDOFF-COMPLETE, space, TASK-046) → authenticate the handoff at source (its gates re-run personally: `swift test` exact counts, both builds, UI counts via xcresulttool, frozen-surface diff-empty, porcelain = exactly its expected entries, stash 0) → **dispatch the fresh independent `review-task-046` reviewer** (§10/§33: re-derive the §10.5 checklist from `docs/architecture/05-technical-architecture.md` §10.5 BEFORE reading Implementation Notes or the new tests; body-read every pointer; own probes, never reusing the implementer's; re-run all gates). Then: fix loop if CHANGES_REQUIRED → atomic commit (`test(...)`/`docs(qa)` type as the diff dictates) + push → closeout (mv to completed/, epic 2/6, status refresh) → ONLY THEN dispatch TASK-047 (contract READY: `.claude/tasks/active/TASK-047-a11y-audit.md`; baselines refresh at dispatch — it inherits TASK-046's new suite counts).
