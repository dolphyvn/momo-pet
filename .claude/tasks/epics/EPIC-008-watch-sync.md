# EPIC-008 — Watch App & Sync

## Objective
Complete the FIRST VERTICAL SLICE: iPhone state reaches the Watch, and an offline Watch pat applies exactly once on the iPhone. MomoWatch leaves its placeholder shell and becomes **W1** — the one glanceable surface (mood in words + bond stage word + the single quest line + the pat) rendering the last-synced `WatchSnapshot` instantly from local storage — while the iPhone-side WC session pushes a latest-wins context on every state change and applies journaled pats exactly once. The Watch runs **no engine** (ADR-003): it renders iPhone-issued snapshots through the shared pure derivations and queues pat intents; the iPhone stays authoritative (D5/§21).

## User / Product Value
The Watch is Momo's independent companion surface (§2, §12, §44; PR6 — "independent value, not a phone mirror"): raise the wrist, see how Momo feels in words, pat from anywhere — offline, iPhone away, whatever — and the affection lands exactly once when the phone returns. No guilt copy, no freshness indicator, no sync error ever exists (UX-9; FR-18 AC-4). This closes the delivery plan's spine (§4.2): "Watch receives state" (TASK-040 + TASK-041) — and with it the complete first vertical slice.

## Scope
- **iPhone-side WatchConnectivity session (TASK-040, 05 §6.1–6.4; ADR-003):** activates the `.pushWatchSnapshot` seam reserved since TASK-031 (currently a no-op case at `MomoAppModel.swift`) — context push on every state change via the MomoKit builder (latest-wins `WatchSnapshot`: DisplayState + quest inputs + haptics flag + epoch-scoped watermark); intent receive path: fold-to-now → apply iff UUID unseen ∧ `watchSeq > watermark` for that intent's `watchSessionEpoch` (0-init on unseen epoch, accessor-driven — never resolved at call sites) → the next snapshot carries the watermark; `sendMessage` reachable-only nudges are optimizations, the context/journal paths are the correctness paths; the **§6.6 erase reset-marker leg** lands here as additive context content (TASK-038's routed leg — the `WatchSnapshot` DTO carries no reset field yet; schema-versioned additive evolution per 05 §5.5); **WC VERIFY-AT-BUILD transport behaviors resolved and recorded** (background latency, context coalescing/launch delivery, any required background capability — the 05 Appendix B item this task owns). Wrapper code lives in the `Momo` app target per ADR-013 (duplicated per target, plain over DRY); pure logic is already in MomoKit (`WatchSyncGate`, `SyncState`, `SyncStateStore`, `IntentJournal`, `WatchSnapshotBuilder` — TASK-023, 88.86 % floor). Kit-level integration tests: apply-exactly-once over duplicate/redelivery streams.
- **W1 glance + snapshot persistence + AOD (TASK-041, FR-17; UX §6.1, §6.5; NFR-9):** W1 renders the last-synced snapshot instantly from the local store (`watch-snapshot.json` + one `.prev`, written on every receive AND every background transition — 05 §5.6); mood in words + stage word + quest line + pat targets; LOD-glance rig foreground, **static glyph in AOD** (FR-17 AC-5); no numbers, no bars, no freshness indicator (UX-9); the calm "settling in" line pre-first-sync (UX §9); **O4 true-size judgment owed** (REVIEW-TASK-025 OBS-4: glyph legibility re-judged at real 24–32 pt on W1/AOD — not the debug canvas's large scale); the Watch restore path must not block the main thread beyond the KB-scale snapshot read (OBS-1's app-layer leg, REVIEW-TASK-024); **F-2 GATE:** the state-in-words template pins the SHIPPED strings (the TASK-039 pattern) and the owner's copy decision on the "You two are {stage}." delta updates the wording when it lands — the owner item gates the template's final form, not the task's start. MomoWatchUITests: restore ≤ ~2 s assertion; AOD static rendering.
- **The Watch pat (TASK-042, FR-17 AC-1/2; UX §6.2–6.4; 04 §6.4):** pat via pet canvas or Pat pill (UX-11); immediate local micro-reaction state-distinct per wakefulness (bounce awake; **stir + tiny heart, stays asleep**) + subtle haptic honoring the synced toggle — fully offline; intent journaled with `watchSessionEpoch` + monotonic `watchSeq`; `transferUserInfo` drain with epoch-matched journal prune on the snapshot watermark; haptic rules per UX §6.3 exactly (pat = single soft tick; on-wrist quest completion = gentle celebratory double; **anything else = nothing**); raise-to-pat ≤ 5 s (FR-17 AC-1).
- **Watch cascade + settings/haptics propagation (TASK-043, 05 §4.8 consumer; UX-13):** quest line = `makeDisplayState` cascade output (the SINGLE shared derivation — zero Watch-side logic); "All done — see you soon" state; quest line is display-only unless pat-completable (Q7), identical at 0/1/2-of-3 and swapping only on completion (UX §6.1); haptics toggle arrives via snapshot; **no Watch settings surface exists** (UX-13).
- **Sync test coverage — §10.4 matrix + device obligations (TASK-044):** every §10.4 row as a named test (context re-render; offline pat exactly-once = FR-18 AC-1; same counters/quests = AC-2; foreground-reconnect freshness = AC-3; replay/duplicate no-op + bond non-regression = AC-4/INV-3; epoch-reset flows; termination/relaunch); **erase reset-marker E2E** (completes TASK-038's AC-2: Watch wipes snapshot + journal at next sync → settling-in line); **device obligations: WC frame delivery under suspension verified on PAIRED HARDWARE** (05 §10.4; §25 — never claimed from simulators alone; R9: if hardware is unavailable, record BLOCKED evidence and gate release on it).

## Non-Goals
New engine semantics (EPIC-004 is frozen — the Watch and the receive path CONSUME `reduce` through the facade, never edit it); new character motion (EPIC-006 is frozen — W1 consumes the LOD-glance/glyph view API); widgets/complications (Phase 2 reservation — the cascade already renders, the surface does not exist); notifications, HealthKit, iCloud/CloudKit, any cloud endpoint (D6/K7, FR-18 AC-5); a Watch settings surface (UX-13); performance-budget measurement (TASK-045/EPIC-009 — the TASK-041 restore ≤ ~2 s UI assertion is the contract-mandated structural exception); the full both-device accessibility audit (TASK-047/EPIC-009 — TASK-041–043 carry per-surface a11y groundwork per NFR-6 as it lands); multi-watch or non-§6.6 sync topologies; monetization/streaks/ads (§24).

## Dependencies
- EPIC-007 merged `main` @ `b73e3eb` (the §14 merge): TASK-031's app-model facade + the reserved `.pushWatchSnapshot` seam; TASK-038's erase executor + its routed reset-marker leg.
- TASK-023's pure sync logic already in MomoKit (headlessly proven; 88.86 % coverage floor held at REVIEW-TASK-024).
- TASK-025 art (glyph/LOD-glance constants), TASK-026 LOD tiers + clock pause, TASK-019 `DisplayState` + cascade (TASK-043), TASK-021/022 store primitives.
- **ADR-013** standalone-watch packaging: WC wrappers duplicated per app target; `WKCompanionAppBundleIdentifier` already declared; **no target dependency may ever appear** (D-R3 review check on EVERY pbxproj change). **ADR-003** transport strategy; ADR-006/ADR-008 deployment pins (watchOS 26.0, `TARGETED_DEVICE_FAMILY = 4`).
- **Inherited review routings (all verified at source this epic-authoring pass):** O1+O5 (REVIEW-TASK-023) — one writer per journal/sync-state file on a single dispatch path; apply through the accessor-driven `SyncState.shouldApply`, never resolving watermarks at call sites. OBS-1 (REVIEW-TASK-024) — main-thread non-blocking I/O app-layer leg. O4 (REVIEW-TASK-025) — glyph true-size judgment. F-2 (REVIEW-TASK-039) — owner copy decision gates the state-in-words template's final wording (TASK-041).
- MomoWatch currently builds **zero-warning** on the pinned Watch SE 3 44 mm `8A854895` (watchOS 26.5) — the placeholder shell is the starting point.

## Tasks
Branch: `feature/EPIC-008-watch-sync` (from `main` @ `b73e3eb`).

| TASK | Title | Size | Depends on |
|---|---|---|---|
| TASK-040 | iPhone-side WC session: context push through the reserved seam + intent receive path + reset-marker context leg + WC VERIFY-AT-BUILD record (05 §6.1–6.4; ADR-003) | L | TASK-031 (merged), TASK-023 (merged) |
| TASK-041 | MomoWatch W1 glance + snapshot persistence + AOD (FR-17; UX §6.1, §6.5; NFR-9; O4; F-2-gated template) | L | TASK-040, TASK-026 (merged), TASK-019 (merged) |
| TASK-042 | The Watch pat: offline-first micro-reaction + journaled intents + drain (FR-17 AC-1/2; UX §6.2–6.4; 04 §6.4) | M | TASK-041 |
| TASK-043 | Watch cascade + settings/haptics propagation (05 §4.8 consumer; UX-13) | S | TASK-041 |
| TASK-044 | Sync test coverage — §10.4 matrix + erase reset E2E + paired-device obligations | L | TASK-040…043 |

## Acceptance Criteria
1. The iPhone pushes a latest-wins context on every state change through the `.pushWatchSnapshot` seam; the plan core's fixed side-effect order is preserved and the seam remains the ONLY new engine exit (D-R5); the context carries DisplayState, quest inputs, the haptics flag, and the epoch-scoped watermark.
2. Watch-originated intents apply EXACTLY ONCE on the iPhone: fold-to-now → unseen-UUID ∧ strictly-greater-watermark (per-epoch, 0-init) → next snapshot carries the watermark; duplicates, replays, and redeliveries are no-ops (INV-10); hello is device-agnostic against the intent's own `dayKey`; expired-dayKey intents apply current-state effects with day attribution dropped.
3. W1 renders the last-synced snapshot instantly from local storage (persisted on every receive + background transition), with mood/stage in words and the single quest line — no numbers, no bars, no freshness indicator, no error surface, ever; the settling-in line shows pre-first-sync.
4. The Watch pat is fully offline-first: immediate micro-reaction + toggle-honoring haptic (state-distinct; asleep = stir + heart, stays asleep), intent journaled with epoch + monotonic seq, drained reliably; raise-to-pat ≤ 5 s; haptics follow UX §6.3 exactly (nothing outside pat + on-wrist completion).
5. The Watch runs no engine: the quest line is the shared cascade output; all bond/counters/quests move only on the iPhone; bond cannot regress on either device (INV-3); the haptics toggle travels via snapshot; no Watch settings surface exists.
6. Reset flows hold end-to-end: erase sets the reset marker in the next context → the Watch wipes snapshot + journal at next sync → settling-in line (TASK-038's AC-2 completes); epoch re-initialization (re-pair/reinstall/new Watch) never starves pats; a stale-epoch watermark never prunes a newer journal (INV-10).
7. AOD pauses all looping animation behind the static glyph (FR-17 AC-5); the glyph reads correctly at true 24–32 pt on W1/AOD (O4 closed); restore ≤ ~2 s asserted in the Watch UI suite.
8. Full sync suites green including the §10.4 matrix; the paired-device obligation is executed on hardware or BLOCKED recorded (no simulator-only claim — §25).

## Test Requirements
- **MomoKitTests:** kit-level integration tests with the wrapper (apply-exactly-once over duplicate/redelivery streams); the pure-logic floor (MomoKit ≥ 80 %, measured 88.86 %) must not dip.
- **MomoWatchUITests:** W1 restore timing ≤ ~2 s; AOD static rendering; offline pat flow; journal persistence across termination; cascade renders sanely over stale quest inputs; erase reset-marker E2E.
- Package suites stay green (`swift test`); `Sources/MomoCore/` untouched (frozen); `Sources/MomoCharacter/` untouched except where the W1 view seam genuinely lacks a consumer surface (any such touch disclosed + minimal, the EPIC-007 precedent).
- D-R3 (no app-to-app target dependency) re-checked on every pbxproj change; D-R5 scans and the standing discipline suites (import whitelist, banned vocabulary, token purity) stay green with no new exemptions; any NEW Watch copy (settling-in line, template wording) lands through the catalog law with the banned-vocabulary scan covering it.
- WC VERIFY-AT-BUILD transport behaviors resolved and recorded at TASK-040; device-frame delivery evidence is TASK-044's deliverable (§25).

## Definition of Done
All five tasks DONE per CLAUDE.md §18; `swift test` + MomoWatchUITests green; independent reviews APPROVED per §10; atomic TASK-ID commits on `feature/EPIC-008-watch-sync`, pushed; epic merged to `main` per §14; the first vertical slice demonstrable end-to-end on the pinned simulators (paired-device obligations executed on hardware, or BLOCKED recorded with evidence); orchestrator status update.

## Status
**READY (0/5)** — epic authored 2026-09-11 on `feature/EPIC-008-watch-sync` (from merged `main` @ `b73e3eb`, the §14 EPIC-007 merge commit). EPIC-007 is COMPLETE 9/9 and merged. Inheritance verified at source: O1+O5 (REVIEW-TASK-023), OBS-1 (REVIEW-TASK-024), O4 (REVIEW-TASK-025), F-2 (REVIEW-TASK-039, owner-gated on TASK-041's template wording — TASK-040 is NOT blocked). **Next: TASK-040 contract → fresh Jupiter dispatch.**
