# REVIEW-TASK-046 — §10.5 edge-case matrix execution (EPIC-009)

APPROVED_WITH_MINOR_NOTES

Reviewer: fresh independent §10/§33 review agent (Jupiter), 2026-09-12. Charter: adversarial — disprove the implementer's verdicts at body level before approval. Base tree: `feature/EPIC-009-release-readiness` @ `5d9d43a`; diff under review = the modified task file + the untracked evidence set (porcelain shows exactly those two entries; verified before any reviewer write).

## Method (independence discipline held)

1. Read the task file's Objective/Requirements ONLY. Implementation Notes, Completion Evidence, and evidence 00-06 were NOT opened until after step 2.
2. Re-derived the ten-row checklist from the normative sources at source: `docs/architecture/05-technical-architecture.md` :622-635 (§10.5, every row verbatim), FR-11 AC-3 (`02-mvp-prd.md` :310), NFR-7 (:396). For each row, wrote down what a REAL test with teeth must assert before reading any implementer claim.
3. Then read the R0 table + dispositions + evidence, and body-verified every pointer against the cited source. Every finding below is evidenced by a body read or a command this reviewer ran personally.

## The reviewer's independently-derived checklist (with teeth requirements)

| # | §10.5 row (verbatim, from :624-635) | What a real test must assert |
|---|---|---|
| 1 | "Timezone change mid-day — Core fold test: one rollover to the new dayKey, no duplicates (FR-11 AC-3)" | A fold under calendar A then re-evaluation of the SAME instant under calendar B produces exactly one new dayKey and identical day records (no duplicate reset, no lost/zeroed quest set). |
| 2 | "Daylight-saving change — Core fold test across a DST boundary night" | Named real DST nights (fall-back 25 h day, spring-forward 23 h day); night segment spans asserted in SECONDS (36000/28800, not the naive 9 h); a dynamics-visible value that a wrong-length night would change. |
| 3 | "Date rollover — Core fold test at local midnight (in-session + catch-up paths)" | In-session: exactly-once on the evaluate/interaction/report event paths with replay no-ops. Catch-up: a stored mark folded at a much later instant appends exactly the landing day. If a launch-time fold path exists, it must be exercised too. |
| 4 | "No Health permission — n/a Phase 1" | No HealthKit import; zero permission strings in Info.plists/entitlements/pbxproj. |
| 5 | "Notification permission denied — n/a Phase 1" | No notification flow; zero permission strings. |
| 6 | "Watch unavailable — iPhone full functionality with zero Watch-dependent code paths (sync is fire-and-forget) — Core + UI" | (a) a failed/absent push cannot alter the interaction path (ordering + non-throwing seam + swallowed failures); (b) zero Watch-dependent code outside the sync seam; (c) the full loop completes with the link dead, with an explicit no-error-surface assertion. |
| 7 | "Offline device — everything (both devices) — UI smoke offline" | Both devices' loops complete with no error surface; an honest account of what "offline" means; no staged network cut claimed as performed. |
| 8 | "Fresh installation — onboarding → Enter atomic write; day-1 Q6-forced generation — UI + Core" | Q6 forced on day 1 through pool AND generator; the Enter write rides the ONE pet-state save site; that site's atomic commit pinned; the write survives process death at the glass. |
| 9 | "Upgrade — store migration v(n)→v(n+1) chain; fresh vs upgrade parity (NFR-7) — Kit" | Version-order walks, missing-hop unreadability (never half-migrated), checksum fall-through WITH a negative control, re-persist at current version, migrated ≡ fresh as whole Equatable values. |
| 10 | "Long inactivity — 7-day absence fold: calm attractors, bond unchanged, warm return greeting (FR-12) — Core" | Absent dayKeys have NO records; landing day full reset; bond paused; calm landing numeric; the missedYou bound has a real discriminator (one minute short ⇒ different greeting). |

## Per-row verification (evidence personally gathered)

**Row 1 — ALREADY-COVERED, CONFIRMED.** `TimeFoldTests.timezoneChangeCausesAtMostOneRollover` :552-575: Tokyo fold mints exactly one landing dayKey "2026-09-09" (count==2, last-keyed); the SAME instant re-evaluated under America/New_York asserts `nyRecheck.newState.days == tokyoFold.newState.days` — a duplicate reset or a second record would break the equality or the count. Executor leg verified at source: `MomoAppModel.swift:407-415` registers `UIApplication.significantTimeChangeNotification` (the quoted comment is verbatim at :402-403) and `significantTimeChangeOccurred()` :750-755 re-derives the calendar then folds through the SAME `apply(trigger:)` → `AppModelPlanCore.plan` path. The trigger-site census below shows there is no other fold door.

**Row 2 — ALREADY-COVERED, CONFIRMED with real teeth.** `dstFallBackNightIsTenHours` :176-207: night = 2026-10-31 22:00 EDT → 2026-11-01 07:00 EST, asserted `== 36000` (a REAL boundary crossing — the repeated hour is inside); discriminator: energy 78 at 05:00 lands `77.9` at 1e-9. Arithmetic check: a 9 h anchor yields 78 + (85−78)/9×2 − 1.5 = 78.0556, which fails the pin by ~0.156 — the discriminator bites. `dstSpringForwardNightIsEightHours` :209-236: `== 28800` across the skipped hour; pin `78.25` (9 h anchor → 78.0556 — fails). Both wakefulness `.waking` assertions present. Attack (v) resolved: the numeric discriminators genuinely discriminate a wrong-length night.

**Row 3 — ALREADY-COVERED, CONFIRMED (disposition (d) verified true at body level).** In-session: `midnightRolloverExactlyOnce` :386-403 (one record keyed to the landing day; re-evaluation of the SAME instant asserts `!again.changed`) and `midnightRolloverExactlyOnceAcrossFoldedSpans` :410-463 — all THREE event paths (evaluate, interaction fold-to-intent-timestamp, report fold-to-now), each with a replay no-op. Catch-up: the reviewer independently hunted launch-time fold paths — full `apply(trigger:)` site census over Apps/Momo finds exactly: petRenamed, hapticsToggled, interaction (:471), characterReport (:478), scheduledBoundary (:729), foreground (:737), significantTimeChange (:754), onboardingCompleted, +Watch's receive interaction (:180). The init :341-418 does ONLY the generation-presence probe (:376) + `store.load` (:379) — no engine fold at launch; `AppModelLaunchTests` :23-74 pins exactly that (presence, never content; corruption falls through invisibly). The cold-launch first-open rides `.active` via MomoApp.swift :49-63 (`scenePhaseChanged(to:)` re-read at first appearance — the TASK-033 discovery comment), i.e. the SAME `.active` → `foregrounded()` → `.foreground` trigger (:735-738) — so "no separate launch fold path exists" is TRUE and the catch-up shapes are the evaluate-path folds over a stored mark, exercised by :386-403 and the multi-day folds :469-502/:504-528 (stored 10:00 mark → landing day exactly once, `.waking` at :518).

**Rows 4/5 — N/A-PHASE-1, CONFIRMED.** Both Info.plists read in full (Apps/Momo, Apps/MomoWatch): ZERO permission keys of any kind. No `.entitlements` files exist anywhere in Apps/ or the project. pbxproj: zero hits for HealthKit/permission/background-mode/entitlement patterns; zero `INFOPLIST_KEY` entries at all. Zero `import HealthKit` / `import UserNotifications` / `UNUserNotificationCenter` in production code. The 05 §11.4 zero-permission posture holds; no permission dialog can exist. Attack (viii) resolved.

**Row 6 — ALREADY-COVERED, CONFIRMED (one wording finding, F-1).**
- (a) Ordering/seam: builder verified at `AppModelPlan.swift:249-252` — BOTH `.persist` and `.pushWatchSnapshot` are appended iff `outcome.changed`; `watchSeamReservedOnlyWhenChanged` :132-156 pins the push as LAST on a changed plan and — per its OWN comment :152-154 — the second scenario labeled `unchanged` is a no-new-greeting plan whose fold-stamp movement keeps `changed == true` (hence `[.persist, .pushWatchSnapshot]`). The truly-`changed == false` case (EMPTY plan — no persist, NO push) is pinned by `duplicateIntentIsTotalNoOp` :101-127 (`replay.steps == []`). The transport protocol's `send(contextData:)` is void, non-throwing (Apps/Momo/MomoWatchTransport.swift:35), and the live conformance swallows every failure as a logged drop (:96-115: nil session / unactivated / throwing `updateApplicationContext`). The executor (:564-654) runs steps in the plan's fixed order with the push last (:649-652); `pushWatchSnapshot()` (+Watch :112-138) is non-throwing with its own logged-drop guards. NO push outcome can propagate into the interaction path — verified, not assumed.
- (b) Zero Watch-dependent code paths: `import WatchConnectivity` occurs in exactly TWO files (the ADR-013 per-target twins Apps/Momo/MomoWatchTransport.swift, Apps/MomoWatch/MomoWatchTransport.swift); one-site `LiveWatchTransport()` at MomoApp.swift:35; MomoAppModel+Watch.swift :77/:85 are comment-only header citations (verified); Sources/MomoCore: ZERO Watch tokens (reviewer grep); the 4 real-tree wiring guards verified at :312/:320/:327/:334 of MomoWatchWiringScanTests (file carries exactly 21 tests, as the table says); O5 containment re-run by the reviewer (comment-stripped Apps/Momo, zero `WatchSyncGate` tokens).
- (c) UI: `testFirstPatCompletesTheMorningWish` MomoHomeUITests.swift:438-463 — quest row pending → pat → completion in words → Home intact → `XCTAssertEqual(app.alerts.count, 0, …)` at :462. Verified at body level.
- (d) The app-target transport failure legs having no headless suite is the accepted D-7 posture — verified against REVIEW-TASK-040 F-6 ("D-7 accepted as disclosed. No app-target test target exists…").

**Row 7 — ALREADY-COVERED, CONFIRMED (disclosure honest).** The reviewer ran an INDEPENDENT, BROADER census than evidence 02's — 23+ tokens (import Network, URLSession, NSURLSession, NWConnection, NWPathMonitor, NWListener, CFSocket, CFNetwork, SCNetworkReachability, getaddrinfo, gethostby, connect(, bind(, listen(, dataTask, asyncBytes, WKWebView, SFSafariViewController, CloudKit, MultipeerConnectivity, CoreBluetooth, ExternalAccessory, NSStream, NetService, Process(, posix_spawn) over comment-stripped ALL production Swift in Sources/ + Apps/: **ZERO hits**. 05 §11.2 makes WC the only outbound channel; the WC link non-delivering in this sim environment is an authenticated record (TASK-045 evidence 09:37 — `WCErrorDomain Code=7006 "Watch app is not installed."` verbatim, run while the app stayed fully functional). Both devices' loops completed at the glass fresh this session (implementer) AND again under the reviewer's own re-runs (below), each with the zero-alerts pin in the suite. The disclosed "no host-level network cut" is the honest call: with zero network-capable code a cut alters no behavior, and faking one would have been the §25 violation. Attack (iii) resolved — the structural argument carries the row without a cut.

**Row 8 — ALREADY-COVERED, CONFIRMED (one-save-site claim exact).** Core: `unknownPriorsForceQ6` :119-131 (empty AND one-prior pools all contain Q6; generator over 20 seeds) and `freshInstallDayOneContainsQ6` :323-340 (first fold creates day 1 with Q6, Q1 first, current epoch). Plan: `completionTransform` :35-54 asserts the Enter steps EXACTLY `[.persist, .pushWatchSnapshot]` (:47). Executor: the reviewer's census of every `.save(` call site in Apps/ + Sources/ shows the pet-state `SnapshotStore` has EXACTLY ONE call site — `MomoAppModel.swift:605` — and the erase path (MomoAppModel+Settings.swift :94-130 body-read) saves ONLY the reset-marker store (a different store, outside the store tree), deletes the directory, and by its own documented step 5 does "NO persist — the first write remains the onboarding completion write". The store's atomic commit is real (production save = write-temp-then-same-directory-rename, SnapshotStore.swift:226/237-247) and pinned by SnapshotStoreTests :225-253 (generation demotion), :257-295 (crash-window matrix incl. the interrupted save's own generation surviving), :281-288 (orphaned temp ignored). Glass: `testCompletedStoreLaunchesStraightToHome` :87-102 (Enter → Home → TERMINATE → relaunch straight Home, no onboarding step), `testKillBeforeEnterRestartsOnboardingFromS1` :70-85, `testFreshStoreFlowsThroughThreeStepsToHome` :19-66 — all body-verified. Attack (iv) resolved: no second save site weakens the claim; the Enter-specific wording is exactly right.

**Row 9 — ALREADY-COVERED, CONFIRMED.** All eight MigrationChainTests citations body-read: :136-141 (v0→current through a real SnapshotStore), :143-153 (missing hop ⇒ unreadable ⇒ next generation serves), :180-189 (multi-hop in VERSION order regardless of declaration order → "Momo123"), :191-198 (mid-walk missing hop ⇒ whole walk nil — never half-migrated), :240-255 (bad checksum ⇒ falls through, "never serve apply(payload)"), :257-262 (the NEGATIVE CONTROL: valid checksum DOES migrate — real, present), :266-286 (re-persist at current version; chain-independent read), :290-300 (NFR-7 parity: `upgraded == freshlyBuilt` as whole Equatable values). Attack (vi) resolved. Note: the shipped production chain is empty by design (:157) — the row EXECUTES the chain mechanism with injected steps, exactly per the task's constraint ("does not add a version").

**Row 10 — ALREADY-COVERED, CONFIRMED.** `absentDayLedgerIsEmpty` :469-502: all six absent dayKeys asserted ABSENT; landing day full reset (zero counters, hello un-awarded, epoch-current set with Q1); bond 130 in → 130 out (:476/:495). `sevenDayAbsenceAddsOnlyLandingDay` :504-528: only the landing record; `.waking` at :518; energy exactly `80.5` at 1e-9 (:521); mood ∈ (floor, attractor] (:522-523); idempotent re-fold (:526-527). Greeting bounds REAL, not vacuous: `missedYouEvenAtNight` :39-48 — at exactly `missedYouAfterHours` the selection is `.missedYou` even inside the night window, and ONE MINUTE SHORT it is `.nightGlance` (a 60-second discriminator between two different greetings); `missedYouOutranksFreshMorning` :54-60 — a 36 h absence landing in daytime is missedYou, with the dayKey-change precondition asserted at :58. Attack (vii) resolved.

**Misattribution hunt outcome:** zero — no test is cited for a claim its body does not make; no described teeth are absent; every name-precise pointer resolves to the cited content (three sub-range imprecisions recorded as F-3; none misattributes).

**Environment honesty:** `xcrun simctl list pairs` (reviewer-run) shows exactly one pair `9DC3AD8E-1237-44F8-99D3-7DCF7478853C` binding the two pinned sims — matching the disclosure ("(active, disconnected)" at review time vs the recorded "(active, connected)"; connection state drifts, the pair's existence matches). No row verdict depends on the pairing flag: row 6's evidence is structural (transport failure legs, wiring scans, plan pins) plus the UI suite runs, and the WC non-delivery is independently authenticated via TASK-045 evidence 09. The stale contract note is disclosed first in the Implementation Notes, not buried.

## Gate re-run table (all personally executed by this reviewer)

| Gate | Command (abridged) | Result | Evidence |
|---|---|---|---|
| Package suite | `swift test` | ✔ **1161 tests in 113 suites passed**, exit 0 — digit-for-digit baseline | reviewer run 2026-09-12 |
| iPhone build | `xcodebuild -scheme Momo -destination 'id=1F25E487…' build` | ** BUILD SUCCEEDED **, exit 0 | reviewer run |
| Watch build | `xcodebuild -scheme MomoWatch -destination 'id=8A854895…' build` | ** BUILD SUCCEEDED **, exit 0 | reviewer run |
| App UI suite | `xcodebuild test -scheme Momo -only-testing:MomoUITests` | **35 passed / 0 failed**, result "Passed", TEST SUCCEEDED, exit 0 — pinned iPhone SE 3 (iOS 26.5) `1F25E487…`; xcresult `Test-Momo-2026.09.12_10-40-13-+0100` | reviewer run, xcresulttool-verified |
| Watch UI suite | `xcodebuild test -scheme MomoWatch -only-testing:MomoWatchUITests` | **9 passed / 0 failed**, result "Passed", TEST SUCCEEDED, exit 0 — pinned Watch SE 3 44mm (watchOS 26.5) `8A854895…`; xcresult `Test-MomoWatch-2026.09.12_10-38-21-+0100` | reviewer run, xcresulttool-verified |
| Implementer Watch xcresult | xcresulttool on `Test-MomoWatch-2026.09.12_09-07-27-+0100.xcresult` (DerivedData Momo-fwvjsusqmrvbwedbdyzqgeffymoo) | 9 passed / 0 failed on the pinned Watch sim; finishTime matches evidence 04's 09:08:53.326 | authenticated |
| Repo hygiene | `git status --porcelain` / `git stash list` / `git diff HEAD` over Sources/, Apps/, Momo.xcodeproj/, Tests/, MomoUITests/, MomoWatchUITests/ | porcelain = exactly the task file (M) + evidence dir (??); stash 0; frozen surfaces AND all test targets diff-empty vs HEAD | reviewer run (before any reviewer write) |
| App UI authentication fallback | not needed — re-run personally (35/0 above) | supersedes the text-only record | — |

## Findings

- **F-1 · Minor (record wording, row 6a):** the audit cell's parenthetical "the push step … appears only on changed plans (`changed.steps.last == .pushWatchSnapshot`; unchanged == exactly `[.persist, .pushWatchSnapshot]`)" is internally inconsistent AS WORDED: the builder gates the push on `outcome.changed` (AppModelPlan.swift:252), so a plan equal to `[.persist, .pushWatchSnapshot]` IS a changed plan — the test's local `unchanged` means "no new greeting/moments", and its own comment (:152-154) says the stamp movement "keeps it `changed`". The truly-unchanged case (empty plan, no push) is pinned by `duplicateIntentIsTotalNoOp` :101-127. Coverage is real and correct; the CELL misdescribes the test in a way that could make a future maintainer see a table-vs-builder contradiction and "fix" one of them. Fix: add the gloss or cite the duplicate-intent pin in the cell. Not a coverage failure (charter criterion: the coverage itself is verified real).
- **F-2 · Note (record wording, row 6a):** "with `fixedOrder` :23-… pinning persist→response→moments→push" — that test pins `[.persist, .deliverMoments, .pushWatchSnapshot]` (response is nil on evaluate paths, per its own header comment :20-22); the persist→response→push order is pinned by `midRoundPlayRidesTheChangedPlan` :82-89. The load-bearing claim (response/moments effects delivered BEFORE the push executes) is verified across the two tests together.
- **F-3 · Note (pointer drift, rows 6/8):** SnapshotStoreTests ":225-244 generation demotion order" actually spans :225-253 and ":257-290 crash-window shapes" spans :257-295; MomoOnboardingUITests ":87-104" is :87-102; the transport protocol is named `MomoWatchTransporting` (the cell's "`WatchTransport.send(contextData:)`" carries the right shape via the file-derived name). Every pointer resolves and no claim is misattributed; recorded for name-precision hygiene only.
- **F-4 · Note (robustness, row 7):** evidence 02's census token list is narrower than a maximal one (no NSURLSession/NWConnection/CFSocket/SCNetworkReachability/getaddrinfo/connect/bind/WKWebView/CloudKit/CoreBluetooth/NSStream/Process variants). The reviewer's broader census (26 tokens, comment-stripped, all production Sources/ + Apps/) returns ZERO hits, so the structural conclusion stands unchanged; noted so future offline audits start from the wider list.

No Major findings. No Blocker findings. No coverage gap survived attack: all eight ALREADY-COVERED rows carry real, body-verified teeth; both N/A rows are factually n/a; the zero-new-tests verdict is the honest, evidence-proven outcome the task contract explicitly permits (TASK-024 precedent), not laziness — every candidate gap the contract hypothesized was individually falsified at body level ((a) the fire-and-forget pin exists; (b) the structural offline proof; (c) the Enter write rides the one pinned atomic save site; (d) no launch fold path exists to test).

## Probe hygiene

All probes were the reviewer's own; no implementer probe was reused. The network census was re-derived with a broader token list (not the evidence-02 list); the simctl pairs check, the `.save(` call-site census, the trigger-site census, the O5 containment grep, and the MomoCore Watch-token grep were all run fresh. No repo file was touched by any probe (the working tree still shows exactly the two expected entries after all probes). The census temp file was deleted after use. The reviewer's two UI re-runs left only toolchain-standard xcresults in DerivedData (nothing extracted to /tmp, nothing to clean).

## Verdict

**APPROVED_WITH_MINOR_NOTES** — F-1 (and optionally F-2/F-3) should be folded into the audit cell wording at closeout; none gates the commit. The R0 audit table survives adversarial body-reading on all ten rows; all gates are green on the reviewer's own runs; the zero-new-tests outcome is proven, not asserted.
