TASK-045 evidence — session environment and honesty basis
=========================================================
Date: 2026-09-12 (measurements 2026-09-08..09-12 session window)
Host: macOS (Darwin 25.5.0), Xcode 26.x, simulator runtimes iOS 26.5 + watchOS 26.5.

Pinned simulators (all measurements on these):
  iPhone SE (3rd generation)      1F25E487-A78E-464C-95AF-0BD1A9B3E1BE
  iPhone 17 Pro                   8E57D4E0-FAA2-499F-8FC6-E0B50AE855BA
  Apple Watch SE 3 (40mm)         F0A75761-CB62-4812-A1C4-8E682A3F02FB
  Apple Watch SE 3 (44mm)         8A854895-225C-411B-89C1-B03337BFE957
  Apple Watch Series 11 (42mm)    EB5715E3-E528-4139-B768-2898AF583EC5

Builds under test (built this session from the working tree, zero repo changes):
  Momo.app      Release iphoneos-simulator build (DerivedData Release-watchsimulator)
  Momo.app      Debug build (for app UI suites)
  MomoWatch.app Release watchsimulator build
  MomoWatch.app Debug build (for watch UI suites)
Raw device inventory captured with `xcrun xctrace list devices`:
  raw-xctrace-list-devices.txt
  -> exactly ONE physical iPhone (JNJGYD4G9Q, 8DE821A1-0104-52AC-9AE6-4CB1DC690FA3),
     NO paired physical Watch. This is the basis for every §25 BLOCKED device-leg
     verdict in rows R2/R6 (and the device-side qualifiers of R1/R3/R4).

Honesty notes (§25):
- Every number below is a SIMULATOR measurement unless explicitly marked otherwise.
  No number in this evidence set was measured on physical hardware.
- Memory rows use the host-side `/usr/bin/footprint <pid>` on the simulator app's
  host process (phys_footprint). footprint/vmmap do not exist inside the iOS 26.5
  sim runtime (`simctl spawn ... which footprint` -> not found) — the host tool on
  the host process is the same phys_footprint counter.
- Clock note: `log stream` prints host LOCAL time; `date -u` timestamps in the
  session notes are UTC. The offset observed this session was exactly +1h
  (SESSION-START 04:07:42 UTC == stream 05:07:42). Harmless; both recorded.
- Disk-pressure event: mid-session the host root volume hit 100% (ENOSPC),
  blocking tooling. I deleted ONLY: my own /tmp/task045 artifacts (hitches trace,
  watch xcresult bundle, both xcarchives — all values already recorded), this
  project's regenerable DerivedData intermediates, and the two throwaway probe
  projects' DerivedData. Sim runtime volumes (24G) and sim devices (13G) were NOT
  touched. The volume remained ~97-100% full — the owner should free disk space;
  recorded as a known issue.
