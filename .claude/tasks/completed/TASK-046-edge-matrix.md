# TASK-046 — §32 edge-case matrix execution (05 §10.5)

## Parent Epic

EPIC-009 — Polish, QA & Release Readiness (epic file `.claude/tasks/epics/EPIC-009-release-readiness.md`, TASK-046 row). Normative source: `docs/architecture/05-technical-architecture.md` §10.5 (matrix at :622-635); FR-11 AC-3; NFR-7.

## Objective

Every row of the normative 05 §10.5 edge-case matrix EXECUTED — not merely mapped. An R0 body-level audit table maps each of the ten rows to its existing name-precise, body-verified tests; rows already covered are RECORDED with fresh green execution pointers from this session; real execution gaps are CLOSED with new tests in the correct suite; the two n/a-Phase-1 permission rows are recorded as such with citations. Builds on the TASK-020 (Core fold matrix) and TASK-024 (Kit store/migration suites) work; fills real execution gaps rather than duplicating green tests — which explicitly permits a zero-new-tests outcome IF the audit table proves it.

## Context

- Standing baselines at task start: `swift test` 1161/113 green; MomoKit coverage 90.84 % lines (floor 80); both builds BUILD SUCCEEDED on pinned sims; `MomoWatchUITests` 9/9; app UI suite 35/35. TASK-045 (05 §12 budgets) just closed DONE (`c43c92b`, review APPROVED_WITH_MINOR_NOTES).
- **Pinned simulators:** iPhone SE 3rd gen `1F25E487-A78E-464C-95AF-0BD1A9B3E1BE` (primary); Watch SE 3 44mm `8A854895-225C-411B-89C1-B03337BFE957` (watchOS 26.5, build/UI baseline); EPIC-009 matrix legs when needed: Watch SE 3 40mm `F0A75761-CB62-4812-A1C4-8E682A3F02FB`, Series 11 42mm `EB5715E3-E528-4139-B768-2898AF583EC5`, iPhone 17 Pro `8E57D4E0-FAA2-499F-8FC6-E0B50AE855BA`. Verify with `xcrun simctl list devices available` and use what the host reports. The pinned iPhone sims are UNPAIRED — that is exactly the posture R1a's "Watch unavailable" leg wants; use it as-is and record it.
- TASK-020's Core fold coverage (name-precise pointers observed at contract time in `Tests/MomoCoreTests/TimeFoldTests.swift` — VERIFY AT BODY LEVEL, never trust this contract's line numbers blindly): :552 "a timezone change mid-day causes AT MOST one rollover" · :176/:209 the named 2026 DST fall-back/spring-forward nights · :386/:410 midnight rollover exactly-once in-session across evaluate/interaction/report paths · :469/:504 absent-day ledger + seven absent days (FR-12) · :530 backward-clock folds nothing. `GreetingSelectionTests` :39/:54 the missedYou warm-return legs.
- TASK-024's Kit coverage: `MigrationChainTests` carries the upgrade row — version-order walks, missing-hop unreadability, checksum fall-through, re-persist-at-current, and :290 "a state loaded through migration equals the equivalent freshly-built state (NFR-7 parity)".
- **Candidate REAL GAPS (R0 must verify each; this is a starting hypothesis, NOT a verdict):**
  - (a) **Watch unavailable** — "iPhone full functionality with zero Watch-dependent code paths (sync is fire-and-forget) — Core + UI": the contract-time census found NO named unavailable/fire-and-forget test. TASK-045's R9 census (WC 7006 "not installed" tolerated while the app stayed fully functional) is adjacent evidence, not a §10.5 execution.
  - (b) **Offline device** — "everything (both devices) — UI smoke offline": no named offline UI smoke found. Both apps have zero network code, so the honest execution is a UI smoke on sims with no network reach, recording that the loop completes and no error surface appears.
  - (c) **Fresh installation** — "onboarding → Enter atomic write; day-1 Q6-forced generation — UI + Core": plan tests (`AppModelOnboardingPlanTests`), the onboarding UI suite, and day-1 Q6 generation tests exist by name; whether the Enter-path write's ATOMICITY is asserted anywhere (the Watch store's temp+rename atomicity is pinned in SnapshotStoreTests — the iPhone Enter path may not be) needs a body check.
  - (d) **Date rollover catch-up** — §10.5 says "in-session + catch-up paths": :410 covers the in-session legs; whether a launch-after-midnight catch-up leg exists needs a body check (fold-at-launch logic may live in the wakefulness/fold path).
- EPIC-009 standing discipline: physical-hardware legs attempt-then-BLOCKED per §25; nothing sim-claimed as device; budget values (none in this task) and product scope untouched.

## Requirements

- **R0 — the §10.5 audit table (mandatory first; TASK-044 R0 precedent).** One row per §10.5 matrix row (all ten), each carrying: the §10.5 normative text VERBATIM; every existing test that covers it by NAME-PRECISE `file:line` citation; a BODY-READ verification (open the test body — confirm it exercises the row's semantics, not a name-match); and a verdict: **ALREADY-COVERED** (with a fresh green execution pointer from this session's run), **GAP** (what exactly is missing), or **N/A-PHASE-1** (rows 4-5, with the §10.5 + 05 §11.4 citations). The table lands in this file's Implementation Notes. Suite misattribution is a review finding (REVIEW-TASK-044 F-1 precedent).
- **R1 — close the gaps R0 actually finds** (the candidate list above is hypotheses, not obligations). For each real GAP: new tests in the CORRECT suite (Core fold → `Tests/MomoCoreTests/`; Kit store/sync → `Tests/MomoKitTests/`; UI smoke → the app UI test target), minimum diff, each new test carrying a one-line provenance comment citing its §10.5 row. Expected shapes, to verify then implement only what is real:
  - **R1a Watch unavailable:** a UI smoke completing the core loop on an unpaired iPhone sim (full functionality; no Watch-dependent stall), plus a Core/Kit-level pin that snapshot pushes are failure-tolerant (a failed push never propagates into the interaction path — fire-and-forget). Do NOT weaken any sync guard; do NOT add production code paths.
  - **R1b Offline device:** the honest UI smoke offline — pinned sims, connectivity absent, both apps' loops complete, no error surface. Record exactly what was executed and what "offline" means on a simulator (no reachability); do not fake a network cut that wasn't performed.
  - **R1c Fresh install:** the Enter-atomic-write leg if R0 confirms it missing — assert the onboarding completion write reaches disk only atomically, reusing the store's existing atomic-write seam. Do not invent a new write path.
  - **R1d Date-rollover catch-up:** only if R0 shows a real hole after the body read.
- **R2 — n/a rows:** record "No Health permission" and "Notification permission denied" as n/a-Phase-1 exactly per §10.5 (no HealthKit; no notification flows; zero Info.plist permission strings per 05 §11.4 — no permission dialog can exist).
- **R3 — execution evidence:** every ALREADY-COVERED row re-executed green in this session's `swift test` run (or the relevant UI suite) with the pointer recorded; every new test proven non-vacuous per house style (mutation bite or fixture-negative control; a simple assertion test needs a truthful one-line rationale if a bite is impractical).
- **R4 — stop-on-failure discipline:** no production-source changes are expected. If any row FAILS against production behavior, STOP, record a BLOCKED finding in Implementation Notes, and end the handoff — the orchestrator adjudicates (a frozen-surface bug is escalated to a new adjudicated task, never fixed opportunistically — epic constraint).
- **R5 — §19 gates at session end:** `swift test` green (record exact counts); both scheme builds BUILD SUCCEEDED on the pinned sims; the app UI suite green if the app UI target was touched (new baseline = 35 + additions); the Watch UI suite only if touched. Repo hygiene: porcelain shows exactly the expected task-doc entries; stash 0.
- **R6 — honest recording:** every executed leg carries a surface label (sim name/OS version); nothing device-claimed; n/a rows and any attempt-then-BLOCKED legs recorded per §25; evidence files under `.claude/tasks/evidence/TASK-046/` (small text files; no traces/bundles). DISK: the host volume is at ~critical free space — check `df -h /` before heavy legs; keep evidence small; clean probe artifacts; if ENOSPC threatens, record it and stop rather than truncate evidence.

## Files / Areas Likely Affected

- `Tests/MomoCoreTests/` (fold gaps), `Tests/MomoKitTests/` (unavailable-push tolerance / store legs), the app UI test target (offline/unavailable smokes), this task file, `.claude/tasks/evidence/TASK-046/`.
- **Production surfaces expected UNTOUCHED:** `Sources/MomoCore/`, `Sources/MomoKit/`, `Sources/MomoCharacter/`, `Apps/**`, `Momo.xcodeproj/`.

## Dependencies

- TASK-020 (Core fold matrix) DONE, TASK-024 (Kit suites) DONE — both merged. Depends on nothing else in EPIC-009 (TASK-045 DONE).

## Constraints

- Frozen surfaces stay frozen (epic constraint): `Sources/MomoCore/`, `Sources/MomoCharacter/`, `Apps/Momo/**`, `Apps/Shared/MomoCopy.xcstrings`, `Momo.xcodeproj/`. (`Tests/` are NOT frozen.)
- Scope control (§22/§24): execute the matrix; no new features; no new production code paths; no schema changes — the upgrade row EXECUTES the existing migration chain, it does not add a version.
- All agents Jupiter. The implementer does NOT commit (§9).
- Verify every claim at source; report actual state (§25).

## Acceptance Criteria

- AC-1: the R0 audit table exists, complete over all ten §10.5 rows, every pointer name-precise and body-verified.
- AC-2: every row lands ALREADY-COVERED (fresh green execution pointer), GAP→closed (the new test, non-vacuous), or N/A-PHASE-1 (citation). No row unmapped.
- AC-3: all §19 gates green at the final tree; exact counts recorded in Completion Evidence.
- AC-4: zero production-source diff; frozen surfaces diff-empty.
- AC-5: the §28 handoff is complete and truthful — no sim claimed as device, no n/a row claimed as executed, no gap claimed closed that wasn't.

## Required Tests

- New: whatever R0's real gaps demand (expected 2-8 tests across Core/Kit/UI). If R0 closes with zero gaps, record that honestly with the full table.
- Standing: `swift test` (1161/113 + additions); both builds; app UI suite if touched; Watch UI suite if touched.

## Review Requirements

- Independent fresh §10/§33 reviewer (Jupiter, not primed): re-derives the §10.5 row list from `05-technical-architecture.md` §10.5 BEFORE reading this file's Implementation Notes or the R0 table; body-reads every pointer; runs own probes (at minimum: one misattribution hunt per audit row class + one non-vacuity check per new test); re-runs `swift test` + both builds. Findings recorded in `.claude/tasks/reviews/REVIEW-TASK-046.md`; CHANGES_REQUIRED/BLOCKED gates the commit (§10/§11).

## Git Requirements

- Implementer: NO commit. Orchestrator: one atomic commit after review approval — `<type>(<scope>): TASK-046 <summary>` — carrying the task record, REVIEW-TASK-046, and the evidence set; push; status.md updated (§12/§13).

## Status

DONE-pending-push (2026-09-12): implementation complete (R0 ten-row §10.5 audit table — 8 ALREADY-COVERED / 2 N/A-PHASE-1 / ZERO gaps → the epic-permitted zero-new-tests outcome; all §19 gates green; zero production/test-target diff); independent review **APPROVED_WITH_MINOR_NOTES** (REVIEW-TASK-046 — zero misattributions, zero coverage gaps; F-1/F-2/F-3 wording precision applied at closeout, coverage unchanged); the orchestrator authenticated the §28 handoff at source (Watch UI xcresult personally queried 9/9; hygiene/porcelain verified) and the reviewer personally re-ran every gate (swift test 1161/113 digit-for-digit, both builds, app UI 35/0 + Watch UI 9/0). Atomic commit (this commit); push (this push pending). Final DONE state recorded in the completed/ copy.

## Implementation Notes

Implementer session 2026-09-12 (fresh Jupiter agent, no commit per §9). Environment delta vs this contract's Context, disclosed first: **the pinned sims are NO LONGER UNPAIRED** — `xcrun simctl list pairs` shows one active pair `9DC3AD8E-1237-44F8-99D3-7DCF7478853C` binding the two pinned sims (both Booted), created during the EPIC-008/009 device-matrix work (TASK-045 R9's census ran on it). The pair was left in place (unpair/re-pair would churn shared sim state other evidence references — TASK-045 F-8 drift precedent). The row-6 substance does not depend on the pairing flag: in this sim environment WC delivery fails regardless (WC 7006 "not installed", recorded verbatim at TASK-045 R9 while the app stayed fully functional), so every UI execution this session still runs the full loop against a non-delivering sync link. Disk: 755 MiB free at session start (contract-disclosed critical pressure); evidence kept text-only.

### R0 — the §10.5 audit table (all ten rows; normative text verbatim from 05-technical-architecture.md :624-635)

| # | §10.5 row (verbatim) | Covering test(s) — name-precise, body-verified | Verdict |
|---|---|---|---|
| 1 | "Timezone change mid-day — Core fold test: one rollover to the new dayKey, no duplicates (FR-11 AC-3)" | `TimeFoldTests.timezoneChangeCausesAtMostOneRollover` TimeFoldTests.swift:552-575 — BODY: a day folds under the Asia/Tokyo calendar mints exactly one new dayKey ("2026-09-09"); the SAME instant re-evaluated under America/New_York finds the dayKey present and returns identical days (`nyRecheck.newState.days == tokyoFold.newState.days`) — AT MOST one rollover, no duplicate, exactly FR-11 AC-3. Executor leg: MomoAppModel.swift:402-415 registers `UIApplication.significantTimeChangeNotification` ("new day, timezone, DST — the OS's one notification covers the row's cases") and re-folds through the same plan core. Fresh green: session `swift test` (evidence 01). | ALREADY-COVERED |
| 2 | "Daylight-saving change — Core fold test across a DST boundary night" | `dstFallBackNightIsTenHours` TimeFoldTests.swift:176-207 — BODY: the named 2026-11-01 America/New_York night segments [day, night, day], night span 22:00 EDT→07:00 EST == 36000 s (TEN real hours, not 9), plus a dynamics-visible discriminator: entering at 05:00 with energy 78 lands 77.9 (a 9 h anchor would leave 78.055… — the comment names the discrimination). `dstSpringForwardNightIsEightHours` :209-236 — the 2026-03-08 night == 28800 s (EIGHT hours), discriminator 78.25. Both REAL DST boundary nights with body-verified teeth. Fresh green: evidence 01. | ALREADY-COVERED |
| 3 | "Date rollover — Core fold test at local midnight (in-session + catch-up paths)" | In-session: `midnightRolloverExactlyOnce` TimeFoldTests.swift:386-403 (23:00→01:00 appends exactly one record; re-evaluating the SAME instant does not double-award, `!again.changed`) and `midnightRolloverExactlyOnceAcrossFoldedSpans` :410-463 (BODY: the once-only rollover asserted on ALL THREE event paths — evaluate, interaction fold-to-intent-timestamp, report fold-to-now — each with a replay no-op). Catch-up: there is NO separate launch fold path to cover — AppModelLaunchTests.swift:23-74 pins launch as a GENERATION-PRESENCE probe + store load only, and the executor's `.active` leg is the documented catch-up fold (MomoAppModel.swift:429 comment verbatim: "`.active` is the catch-up fold") submitting the SAME engine evaluate over the stored mark through AppModelPlanCore (:737). The catch-up SHAPES are therefore the evaluate-path folds over a stored mark, exercised by :386-403 and by the multi-day catch-up folds :469-502 / :504-528 (stored 10:00 mark → landing day, exactly one landing record, `.waking` at :518). Fresh green: evidence 01. | ALREADY-COVERED |
| 4 | "No Health permission — n/a Phase 1 (no HealthKit); Phase 2 test inherits §7's empty-data stance" | §10.5 :629 (verbatim above) + 05 §11.4 :663-665: "No entitlements file entries in Phase 1 … No Info.plist permission strings exist, so no permission dialog can appear". The repo/build no-HealthKit proof is TASK-048's AC-3 audit. | N/A-PHASE-1 |
| 5 | "Notification permission denied — n/a Phase 1; Phase 2 inherits §9's silent-decline stance" | §10.5 :630 (verbatim above) + 05 §11.4 (same zero-permission-strings structure). No notification flows exist in Phase 1; TASK-048 owns the repo/build sweep. | N/A-PHASE-1 |
| 6 | "Watch unavailable — iPhone full functionality with zero Watch-dependent code paths (sync is fire-and-forget) — Core + UI" | (a) Fire-and-forget ordering (Core/Kit): `watchSeamReservedOnlyWhenChanged` AppModelPlanTests.swift:132-156 — BODY (wording precision per REVIEW-TASK-046 F-1/F-2/F-3 — coverage unchanged): the push step is the plan's LAST step and exists only when `outcome.changed` (builder gate AppModelPlan.swift:252) — the test's changed case ends `changed.steps.last == .pushWatchSnapshot`, and its scenario labeled `unchanged` is the NO-NEW-GREETING/no-moments case whose fold-stamp movement still keeps `changed == true`, hence exactly `[.persist, .pushWatchSnapshot]` (the test's own comment :152-154); the truly-`changed == false` plan is EMPTY (no persist, no push), pinned by `duplicateIntentIsTotalNoOp` :101-127 (`replay.steps == []`). `fixedOrder` :23 pins `[.persist, .deliverMoments, .pushWatchSnapshot]` (the response step is nil on evaluate paths — its header :20-22) and `midRoundPlayRidesTheChangedPlan` :82-89 pins persist→response→push on the interaction path — together: the response/moments effects are delivered BEFORE the push executes, so no push outcome can propagate into the interaction path; the step is void and non-throwing by the `MomoWatchTransporting.send(contextData:)` protocol signature. (b) Zero Watch-dependent code paths: WC tokens in Apps/Momo live ONLY in the sync seam — MomoWatchTransport.swift (the one real WC code file), a one-site `LiveWatchTransport()` construction in MomoApp.swift:35, and comment-only citations in MomoAppModel+Watch.swift:77/:85; Sources/MomoCore has zero Watch tokens; the wiring-scan guards (MomoWatchWiringScanTests, 21 tests incl. 4 real-tree guards :312/:320/:327/:334) + O5 containment pin the sync decisions to the plan core. (c) Full functionality at the UI: the whole 35-test app UI suite runs on the pinned sim where WC delivery fails (7006-class, TASK-045 R9 verbatim census) — the unavailable-link posture — and completes; the sharpest named pointer is `testFirstPatCompletesTheMorningWish` MomoHomeUITests.swift:438-463 — BODY: quest row pending → pat → quest completes in words → Home intact → the EXPLICIT zero-error-surface pin `XCTAssertEqual(app.alerts.count, 0, …)` at :462. (d) The transport's failure-swallow legs (nil session / unactivated / throwing send → logged drop, MomoWatchTransport.swift:96-115, source-verified this session) are app-target code with no headless test target — the accepted D-7 posture (REVIEW-TASK-040 F-6); their behavior is executed at the glass by every UI run here. Fresh green: evidence 01 (plan pins) + 03 (UI leg). | ALREADY-COVERED |
| 7 | "Offline device — everything (both devices) — UI smoke offline" | The offline posture is STRUCTURAL and was recorded, not staged (evidence 02): the census grep (`import Network|URLSession|NWPathMonitor|CFNetwork|URL\(string|dataTask|asyncBytes`) over Sources/** + Apps/Momo + Apps/MomoWatch + Apps/Shared returns ZERO hits — no network-capable code exists to disable; no reachability is consulted. 05 §11.2 makes the WC paired link the ONLY outbound channel, and in this sim environment it does not deliver (7006-class). Loop completions at the glass, fresh this session: iPhone evidence 03 (incl. the zero-alerts pin :462); Watch evidence 04 (MomoWatchUITests 9/9 incl. `testPatPillTapKeepsTheGlanceRendered` :163 — the offline-first pat flow completing with the glance rendered — and `testFreshLaunchShowsTheSettlingInLineOnly` :50). DISCLOSED: no host-level network cut was performed (it would alter no behavior — zero code reads the network — and the host is shared); "offline" here = every outbound channel the apps have is genuinely absent/non-delivering, verified, not simulated. | ALREADY-COVERED |
| 8 | "Fresh installation — onboarding → Enter atomic write; day-1 Q6-forced generation — UI + Core" | Day-1 Q6 (Core): `unknownPriorsForceQ6` QuestGenerationTests.swift:120-131 — BODY: empty/short/unknown priors all force Q6 in the candidate pool AND through the generator itself over 20 seeds; `freshInstallDayOneContainsQ6` :323-340 — BODY: the very first fold creates day 1 with Q6 in its set, Q1 first, current questGenEpoch. Enter atomic write: the composition chain, each link body-verified — `completionTransform` AppModelOnboardingPlanTests.swift:35-54 (the Enter plan's steps are EXACTLY `[.persist, .pushWatchSnapshot]`); MomoAppModel.swift:605 is the executor's ONE `store.save` call site (grep-verified — the Enter write rides the single persist step); SnapshotStoreTests pins that store's atomic commit for EVERY save (:225-253 generation demotion order, :257-295 crash-window shapes, :281-288 orphaned temp ignored by the read path — ranges per REVIEW-TASK-046 F-3); executed at the glass: `testCompletedStoreLaunchesStraightToHome` MomoOnboardingUITests.swift:87-102 — BODY: onboard → Enter → Home, TERMINATE, relaunch → straight Home (the write reached disk and survived process death) + `testKillBeforeEnterRestartsOnboardingFromS1` :70 (kill before Enter restarts) + `testFreshStoreFlowsThroughThreeStepsToHome` :19. The atomicity itself is the store's pinned property on the ONE write path — no separate Enter write path exists to pin (a new one would require new production code, a frozen-surface change). Fresh green: evidence 01 (Core/Kit) + 03 (UI). | ALREADY-COVERED |
| 9 | "Upgrade — store migration v(n)→v(n+1) chain; fresh vs upgrade parity (NFR-7) — Kit" | MigrationChainTests.swift — all body-verified: :136-141 v0→current walk through a real SnapshotStore; :143-153 missing hop ⇒ generation unreadable, next generation serves (no error surface); :180-189 multi-hop walk applies steps in VERSION order regardless of declaration order ("Momo123"); :191-198 mid-walk missing hop ⇒ whole walk unreadable (never a half-migrated state); :240-255 checksum fail ⇒ falls through, never migrates + :257-262 the negative control (valid checksum DOES migrate); :266-286 migrated state re-persists at the current version and loads chain-independently; :290-300 NFR-7 parity — migrated ≡ freshly-built as whole Equatable values. Fresh green: evidence 01. | ALREADY-COVERED |
| 10 | "Long inactivity — 7-day absence fold: calm attractors, bond unchanged, warm return greeting (FR-12) — Core" | `absentDayLedgerIsEmpty` TimeFoldTests.swift:469-502 — BODY: six absent dayKeys have NO records, the landing day is a full reset (zero counters, hello un-awarded, quest set generated under the current epoch), bond PAUSES (130 in == 130 out, FR-12 AC-1). `sevenDayAbsenceAddsOnlyLandingDay` :504-528 — BODY: 7 absent days → only the landing day's record; the calm landing pinned numerically (`.waking` at 07:00 :518, energy exactly 80.5 :521, mood within (floor, attractor] :522-523, idempotent re-fold :526-527). Warm return greeting: `missedYouEvenAtNight` GreetingSelectionTests.swift:39-48 (36 h absence ⇒ `.missedYou` even inside the night window; one minute short ⇒ nightGlance — the bound has teeth) + `missedYouOutranksFreshMorning` :54-60 (a 36 h absence landing in daytime is missedYou, not freshMorning; dayKey-change assertion :58). Fresh green: evidence 01. | ALREADY-COVERED |

### Per-gap dispositions (the contract's four hypotheses — all resolved as COVERED, zero real gaps)

- (a) **Watch unavailable** — resolved COVERED: the fire-and-forget pin EXISTS (AppModelPlanTests.swift:132-156; the contract-time census missed it), the zero-Watch structural containment is real (census above), and the unavailable-link UI execution is the standing environment posture (TASK-045 R9) re-executed this session (evidence 03). The one surface with no headless suite is `LiveWatchTransport`'s failure legs — app-target code, the accepted D-7 posture (REVIEW-TASK-040 F-6), executed at the glass instead; closing it headlessly would require new app-target test infrastructure (new production-side surface) — out of scope per §22, recorded here.
- (b) **Offline device** — resolved COVERED structurally (evidence 02): zero network-capable code + the only channel non-delivering + both devices' loops completing at the glass. No fake cut performed; disclosure recorded in-line.
- (c) **Fresh-install Enter atomic write** — resolved COVERED by composition (table row 8): the Enter trigger carries exactly the persist step; the persist step is the ONE store.save site; the store's atomic commit is pinned; the UI relaunch test executes the write through the real path. The contract's hypothesis ("the iPhone Enter path may not be [pinned]") was falsified: SnapshotStoreTests IS the iPhone store's suite (the shared MomoKit store); the Watch-side `WatchSnapshotStore` pins are a different store.
- (d) **Date-rollover catch-up** — resolved COVERED (table row 3): no separate launch fold path exists to test; the catch-up fold is the same engine evaluate over the stored mark (executor evidence at MomoAppModel.swift:429/:737; AppModelLaunchTests pin the launch read as presence-probe-only), and the catch-up shapes are exercised by :386-403 and the multi-day folds :469-528.

### Tests added

**None.** R0 found zero real execution gaps; the epic row and contract explicitly permit the zero-new-tests outcome when the audit table proves it (TASK-024 precedent — the honest audit outcome). No test target was touched: the app UI suite and Watch UI suite gates therefore run as standing-baseline re-executions (fresh pointers for rows 6/7's UI legs), not additions. R3 non-vacuity: N/A for new tests (none); the cited existing tests' teeth are named in the table (e.g. the DST 9 h-anchor discriminators 77.9/78.25, the retry/replay no-ops, MigrationChainTests' checksum negative control :257-262, the missedYou one-minute-short bound, the zero-alerts pin :462).

### Gates run (§19) — exact outputs

All green on the standing tree (zero production/test-target diffs; full details + raw tails in evidence 01/03/04/05/06):

| Gate | Command (abridged) | Surface | Result |
|---|---|---|---|
| Package suite | `swift test` | host toolchain | ✔ Test run with **1161 tests in 113 suites passed** after 1.404 s, exit 0 — digit-for-digit baseline (evidence 01) |
| App UI suite | `xcodebuild test -scheme Momo -only-testing:MomoUITests` | iPhone SE 3, iOS 26.5 sim `1F25E487…` (pinned) | **Executed 35 tests, 0 failures**, TEST SUCCEEDED, exit 0 (evidence 03) |
| Watch UI suite | `xcodebuild test -scheme MomoWatch -only-testing:MomoWatchUITests` | Watch SE 3 44mm, watchOS 26.5 sim `8A854895…` (pinned) | **Executed 9 tests, 0 failures**, TEST SUCCEEDED, exit 0 (evidence 04) |
| iPhone build | `xcodebuild -scheme Momo build` | same pinned iPhone sim | ** BUILD SUCCEEDED **, exit 0 (evidence 05) |
| Watch build | `xcodebuild -scheme MomoWatch build` | same pinned Watch sim | ** BUILD SUCCEEDED **, exit 0 (evidence 05) |
| Repo hygiene | `git status --porcelain` / `git stash list` | — | exactly the task-doc entries (`M` task file + `??` evidence dir); **0 stashes**; branch `feature/EPIC-009-release-readiness` unchanged (evidence 06) |

The UI suites ran as standing-baseline re-executions (R5 makes them only-if-touched; zero targets touched — they were run anyway to hand rows 6/7 fresh at-the-glass green pointers, the stronger reading of the epic's "EXECUTED, not merely mapped"). Disk floor never breached: 755 MiB at start → 496-509 MiB during the app UI leg → eased to 95 GiB at session end (evidence 06); the 200 MiB kill-floor monitor armed mid-run stayed silent.

### BLOCKED findings

**None.** No §10.5 row failed against production behavior; R4 stop-on-failure never triggered.

## Reviewer Findings

Independent fresh §10/§33 review executed 2026-09-12 (full record: `.claude/tasks/reviews/REVIEW-TASK-046.md`). Checklist re-derived from 05 §10.5 + FR-11 AC-3 + NFR-7 BEFORE opening this file's Implementation Notes; every pointer body-read; all gates personally re-run: swift test 1161/113 exit 0 (digit-for-digit), both builds BUILD SUCCEEDED, app UI 35/0 and Watch UI 9/0 re-run by the reviewer on the pinned sims (xcresulttool-verified), the implementer's Watch xcresult authenticated, porcelain/stash/diff-empty hygiene confirmed. All eight ALREADY-COVERED rows survived adversarial body-reading with their teeth confirmed (DST 1e-9 discriminators bite a 9 h anchor at 78.0556; the missedYou one-minute-short bound; MigrationChainTests' checksum negative control + whole-value NFR-7 parity; the zero-alerts pin :462); both N/A rows verified factually zero-permission at plists/pbxproj/imports; the row-6/7 structural censuses reproduced with a BROADER token list (zero hits). Findings: F-1 Minor — the row-6(a) cell's "unchanged == exactly [.persist, .pushWatchSnapshot]" parenthetical misdescribes the test (the builder gates the push on `outcome.changed`; the test's `unchanged` means no-new-greeting while the stamp movement keeps `changed == true`; the truly-unchanged empty plan is pinned by duplicateIntentIsTotalNoOp :101-127) — wording fix at closeout, coverage intact; F-2/F-3/F-4 Notes (a fixedOrder order-gloss, three sub-range pointer imprecisions, census token-list breadth). Zero coverage gaps; zero misattributions; verdict **APPROVED_WITH_MINOR_NOTES** — does not gate the commit.

## Completion Evidence

(implementation-side record filled by the implementer session 2026-09-12; Reviewer Findings above remain the independent reviewer's property; commit/push evidence is the orchestrator's to append per §12/§13)

- **R0 audit table**: complete over all ten §10.5 rows, in Implementation Notes above — verbatim normative text, name-precise pointers, per-pointer body-read verification. Verdicts: 8 ALREADY-COVERED (rows 1,2,3,6,7,8,9,10) + 2 N/A-PHASE-1 (rows 4,5) + **0 GAP**. AC-1, AC-2 satisfied.
- **Tests added**: none — the zero-new-tests outcome the epic row permits, proven by the table. No test target touched (AC-4 honored for Tests/** and both UI targets).
- **Gates** (AC-3): all green with exact counts in the Gates table above — swift test 1161/113 (baseline match); app UI 35/35; Watch UI 9/9; both builds BUILD SUCCEEDED; porcelain = exactly the task-doc entries; stash 0.
- **Zero production diff** (AC-4): Sources/**, Apps/**, Momo.xcodeproj/ untouched — no tool in this session wrote to them; porcelain proves it.
- **Evidence set** (R6): `.claude/tasks/evidence/TASK-046/` — 00-environment.txt (df + surface labels + the DISCLOSED stale-"unpaired" environment delta: the pinned sims are now paired, pair `9DC3AD8E-1237-44F8-99D3-7DCF7478853C`, left in place with reasoning), 01-r0-package-suite.txt, 02-row6-7-offline-census.txt, 03-r5-app-ui-suite.txt, 04-r5-watch-ui-suite.txt, 05-r5-builds.txt, 06-repo-hygiene.txt. All text-only; every executed leg carries its sim name + OS version; nothing device-claimed; no network cut faked (row 7 executed structurally, disclosed in-line).
- **BLOCKED**: none.
- **Known issue for the reviewer**: the contract's "pinned iPhone sims are UNPAIRED" Context note is stale in the current environment (see 00-environment.txt) — the row-6 verdict does not rest on the pairing flag (WC delivery fails in this sim environment regardless; TASK-045 R9 verbatim), but the reviewer re-deriving the environment should expect the pair to exist.
- **Review** (closeout, orchestrator): `.claude/tasks/reviews/REVIEW-TASK-046.md` — **APPROVED_WITH_MINOR_NOTES**. The reviewer re-derived the checklist from 05 §10.5 + FR-11 AC-3 + NFR-7 BEFORE reading this file's Implementation Notes, body-read every pointer (zero misattributions), reproduced both structural censuses with BROADER token lists (zero hits), and personally re-ran every gate on the as-is tree: swift test 1161/113 exit 0 (digit-for-digit), both builds BUILD SUCCEEDED, app UI suite re-run **35/0** + Watch UI suite re-run **9/0** (xcresulttool-verified, pinned sims), the implementer's Watch xcresult authenticated, porcelain/stash/diff-empty hygiene confirmed. Findings disposition: F-1 (Minor, row-6(a) cell wording) + F-2 (fixedOrder gloss) + F-3 (three sub-range pointer imprecisions + the `MomoWatchTransporting` protocol name) **applied to the audit cells at closeout — coverage claims unchanged**; F-4 (evidence-02 census token breadth — the reviewer's own 26-token census returned ZERO hits, structural conclusion stands) recorded for future offline audits, no repo change. Zero coverage gaps; zero Major/Blocker findings.
- **Commit**: (this commit) — `docs(qa): TASK-046 §10.5 edge-case matrix execution evidence` (task file + evidence set 00-06 + REVIEW-TASK-046; no production or test code).
- **Push**: (this push pending) — `origin feature/EPIC-009-release-readiness`.

## Handoff protocol

End the final report with a freshly-typed marker line: the word HANDOFF-COMPLETE, one space, then TASK-046. Type it fresh in your own final message — exactly once, on its own line, at column 0, as the last line of the handoff.
